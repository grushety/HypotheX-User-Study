
### Setting: Light (n_trees=5, threshold=0.3) ###

Class: Cat

Simple Rules (Single Trees):

Tree 0 Rules:
if Swimming <= 4.50:
  if Detective Stories <= 2.50:
    if Swimming <= 2.50:
      Predict: Dog
    else (Swimming > 2.50):
      Predict: Cat
  else (Detective Stories > 2.50):
    if Running <= 8.50:
      if Swimming <= 0.50:
        if Detective Stories <= 6.00:
          Predict: Dog
        else (Detective Stories > 6.00):
          Predict: Cat
      else (Swimming > 0.50):
        Predict: Cat
    else (Running > 8.50):
      if Detective Stories <= 8.00:
        Predict: Dog
      else (Detective Stories > 8.00):
        if Gardening <= 1.50:
          if Gardening <= 0.50:
            if Art <= 12.00:
              Predict: Duck
            else (Art > 12.00):
              Predict: Cat
          else (Gardening > 0.50):
            Predict: Duck
        else (Gardening > 1.50):
          if Gardening <= 3.50:
            if Swimming <= 2.00:
              Predict: Duck
            else (Swimming > 2.00):
              if Detective Stories <= 11.00:
                if Detective Stories <= 9.50:
                  Predict: Cat
                else (Detective Stories > 9.50):
                  Predict: Duck
              else (Detective Stories > 11.00):
                Predict: Cat
          else (Gardening > 3.50):
            Predict: Cat
else (Swimming > 4.50):
  if Art <= 4.50:
    if Running <= 6.50:
      if Gardening <= 6.50:
        Predict: Duck
      else (Gardening > 6.50):
        if Detective Stories <= 9.00:
          Predict: Dog
        else (Detective Stories > 9.00):
          if Detective Stories <= 13.00:
            if Gardening <= 11.00:
              Predict: Duck
            else (Gardening > 11.00):
              Predict: Dog
          else (Detective Stories > 13.00):
            Predict: Dog
    else (Running > 6.50):
      if Gardening <= 3.50:
        Predict: Duck
      else (Gardening > 3.50):
        Predict: Dog
  else (Art > 4.50):
    if Gardening <= 9.50:
      if Swimming <= 7.50:
        if Detective Stories <= 8.00:
          Predict: Duck
        else (Detective Stories > 8.00):
          if Art <= 6.50:
            Predict: Dog
          else (Art > 6.50):
            if Art <= 13.50:
              if Gardening <= 4.50:
                Predict: Duck
              else (Gardening > 4.50):
                if Swimming <= 6.50:
                  if Art <= 10.00:
                    Predict: Duck
                  else (Art > 10.00):
                    Predict: Dog
                else (Swimming > 6.50):
                  Predict: Dog
            else (Art > 13.50):
              Predict: Duck
      else (Swimming > 7.50):
        if Running <= 13.50:
          if Running <= 4.50:
            if Swimming <= 10.50:
              Predict: Duck
            else (Swimming > 10.50):
              if Detective Stories <= 10.50:
                if Gardening <= 6.50:
                  Predict: Duck
                else (Gardening > 6.50):
                  Predict: Cat
              else (Detective Stories > 10.50):
                Predict: Cat
          else (Running > 4.50):
            if Swimming <= 9.50:
              if Gardening <= 4.50:
                Predict: Duck
              else (Gardening > 4.50):
                if Gardening <= 7.50:
                  Predict: Dog
                else (Gardening > 7.50):
                  Predict: Duck
            else (Swimming > 9.50):
              Predict: Duck
        else (Running > 13.50):
          if Gardening <= 4.00:
            Predict: Duck
          else (Gardening > 4.00):
            if Swimming <= 12.50:
              Predict: Dog
            else (Swimming > 12.50):
              Predict: Cat
    else (Gardening > 9.50):
      if Swimming <= 9.50:
        Predict: Dog
      else (Swimming > 9.50):
        if Detective Stories <= 12.00:
          if Swimming <= 12.50:
            Predict: Duck
          else (Swimming > 12.50):
            if Running <= 9.00:
              Predict: Duck
            else (Running > 9.00):
              Predict: Dog
        else (Detective Stories > 12.00):
          Predict: Dog

Tree 1 Rules:
if Art <= 4.50:
  if Running <= 4.50:
    if Art <= 0.50:
      Predict: Cat
    else (Art > 0.50):
      if Swimming <= 5.00:
        if Detective Stories <= 4.00:
          Predict: Dog
        else (Detective Stories > 4.00):
          Predict: Cat
      else (Swimming > 5.00):
        if Running <= 3.00:
          Predict: Dog
        else (Running > 3.00):
          if Art <= 1.50:
            Predict: Dog
          else (Art > 1.50):
            if Art <= 3.00:
              Predict: Duck
            else (Art > 3.00):
              Predict: Dog
  else (Running > 4.50):
    if Swimming <= 3.50:
      Predict: Cat
    else (Swimming > 3.50):
      if Swimming <= 11.50:
        if Gardening <= 4.50:
          if Detective Stories <= 11.00:
            Predict: Duck
          else (Detective Stories > 11.00):
            Predict: Dog
        else (Gardening > 4.50):
          if Running <= 6.50:
            if Gardening <= 10.50:
              Predict: Duck
            else (Gardening > 10.50):
              Predict: Dog
          else (Running > 6.50):
            Predict: Dog
      else (Swimming > 11.50):
        if Detective Stories <= 5.50:
          Predict: Duck
        else (Detective Stories > 5.50):
          if Running <= 10.50:
            if Gardening <= 4.00:
              Predict: Duck
            else (Gardening > 4.00):
              Predict: Dog
          else (Running > 10.50):
            if Gardening <= 4.00:
              Predict: Duck
            else (Gardening > 4.00):
              Predict: Dog
else (Art > 4.50):
  if Swimming <= 4.50:
    if Running <= 8.50:
      if Swimming <= 0.50:
        if Detective Stories <= 6.00:
          Predict: Dog
        else (Detective Stories > 6.00):
          Predict: Cat
      else (Swimming > 0.50):
        Predict: Cat
    else (Running > 8.50):
      if Running <= 10.50:
        if Detective Stories <= 5.50:
          Predict: Cat
        else (Detective Stories > 5.50):
          Predict: Duck
      else (Running > 10.50):
        if Art <= 8.50:
          Predict: Duck
        else (Art > 8.50):
          Predict: Cat
  else (Swimming > 4.50):
    if Gardening <= 5.50:
      if Running <= 13.50:
        if Swimming <= 12.50:
          Predict: Duck
        else (Swimming > 12.50):
          if Running <= 5.00:
            Predict: Cat
          else (Running > 5.00):
            Predict: Duck
      else (Running > 13.50):
        if Detective Stories <= 10.50:
          Predict: Duck
        else (Detective Stories > 10.50):
          if Art <= 8.50:
            Predict: Dog
          else (Art > 8.50):
            Predict: Cat
    else (Gardening > 5.50):
      if Running <= 4.50:
        Predict: Cat
      else (Running > 4.50):
        if Detective Stories <= 2.00:
          Predict: Dog
        else (Detective Stories > 2.00):
          if Running <= 9.50:
            if Swimming <= 10.50:
              if Art <= 5.50:
                Predict: Dog
              else (Art > 5.50):
                Predict: Duck
            else (Swimming > 10.50):
              Predict: Duck
          else (Running > 9.50):
            if Swimming <= 8.50:
              Predict: Dog
            else (Swimming > 8.50):
              if Running <= 10.50:
                Predict: Dog
              else (Running > 10.50):
                if Gardening <= 13.50:
                  if Detective Stories <= 7.50:
                    if Detective Stories <= 4.50:
                      Predict: Duck
                    else (Detective Stories > 4.50):
                      Predict: Dog
                  else (Detective Stories > 7.50):
                    Predict: Duck
                else (Gardening > 13.50):
                  Predict: Dog

Tree 2 Rules:
if Art <= 4.50:
  if Gardening <= 4.50:
    if Swimming <= 4.50:
      if Running <= 8.00:
        Predict: Cat
      else (Running > 8.00):
        Predict: Duck
    else (Swimming > 4.50):
      if Running <= 13.00:
        if Gardening <= 0.50:
          Predict: Duck
        else (Gardening > 0.50):
          if Running <= 8.50:
            Predict: Duck
          else (Running > 8.50):
            if Detective Stories <= 10.50:
              Predict: Dog
            else (Detective Stories > 10.50):
              Predict: Duck
      else (Running > 13.00):
        Predict: Dog
  else (Gardening > 4.50):
    if Swimming <= 13.50:
      if Running <= 6.50:
        if Running <= 5.50:
          Predict: Dog
        else (Running > 5.50):
          if Art <= 2.50:
            Predict: Dog
          else (Art > 2.50):
            if Swimming <= 10.00:
              if Swimming <= 4.00:
                Predict: Dog
              else (Swimming > 4.00):
                Predict: Duck
            else (Swimming > 10.00):
              Predict: Dog
      else (Running > 6.50):
        Predict: Dog
    else (Swimming > 13.50):
      if Running <= 7.00:
        Predict: Duck
      else (Running > 7.00):
        Predict: Dog
else (Art > 4.50):
  if Art <= 9.50:
    if Swimming <= 4.50:
      if Art <= 8.50:
        if Running <= 10.50:
          Predict: Cat
        else (Running > 10.50):
          Predict: Duck
      else (Art > 8.50):
        if Gardening <= 6.00:
          Predict: Duck
        else (Gardening > 6.00):
          Predict: Cat
    else (Swimming > 4.50):
      if Detective Stories <= 6.50:
        if Swimming <= 13.00:
          if Art <= 5.50:
            Predict: Dog
          else (Art > 5.50):
            if Gardening <= 9.50:
              Predict: Duck
            else (Gardening > 9.50):
              if Swimming <= 10.50:
                Predict: Duck
              else (Swimming > 10.50):
                Predict: Dog
        else (Swimming > 13.00):
          Predict: Dog
      else (Detective Stories > 6.50):
        if Running <= 9.50:
          if Gardening <= 10.50:
            Predict: Duck
          else (Gardening > 10.50):
            if Swimming <= 10.50:
              Predict: Dog
            else (Swimming > 10.50):
              Predict: Duck
        else (Running > 9.50):
          if Art <= 6.50:
            if Gardening <= 4.50:
              Predict: Duck
            else (Gardening > 4.50):
              Predict: Dog
          else (Art > 6.50):
            if Gardening <= 9.50:
              if Running <= 10.50:
                if Swimming <= 8.50:
                  Predict: Duck
                else (Swimming > 8.50):
                  Predict: Dog
              else (Running > 10.50):
                Predict: Duck
            else (Gardening > 9.50):
              Predict: Dog
  else (Art > 9.50):
    if Detective Stories <= 2.50:
      if Swimming <= 7.00:
        Predict: Cat
      else (Swimming > 7.00):
        Predict: Duck
    else (Detective Stories > 2.50):
      if Running <= 4.50:
        Predict: Cat
      else (Running > 4.50):
        if Swimming <= 7.50:
          if Gardening <= 11.50:
            if Gardening <= 2.00:
              if Swimming <= 2.50:
                Predict: Duck
              else (Swimming > 2.50):
                Predict: Cat
            else (Gardening > 2.00):
              Predict: Cat
          else (Gardening > 11.50):
            Predict: Dog
        else (Swimming > 7.50):
          if Swimming <= 9.50:
            if Gardening <= 4.50:
              Predict: Duck
            else (Gardening > 4.50):
              if Running <= 12.00:
                Predict: Duck
              else (Running > 12.00):
                Predict: Dog
          else (Swimming > 9.50):
            Predict: Duck

Tree 3 Rules:
if Running <= 4.50:
  if Gardening <= 10.00:
    if Art <= 5.50:
      if Swimming <= 10.50:
        if Gardening <= 4.00:
          Predict: Cat
        else (Gardening > 4.00):
          if Detective Stories <= 11.50:
            Predict: Cat
          else (Detective Stories > 11.50):
            Predict: Dog
      else (Swimming > 10.50):
        if Detective Stories <= 6.00:
          Predict: Dog
        else (Detective Stories > 6.00):
          Predict: Duck
    else (Art > 5.50):
      if Swimming <= 7.00:
        Predict: Cat
      else (Swimming > 7.00):
        if Running <= 2.50:
          if Gardening <= 2.50:
            Predict: Duck
          else (Gardening > 2.50):
            if Swimming <= 12.50:
              if Art <= 10.00:
                Predict: Duck
              else (Art > 10.00):
                Predict: Cat
            else (Swimming > 12.50):
              Predict: Cat
        else (Running > 2.50):
          Predict: Cat
  else (Gardening > 10.00):
    if Detective Stories <= 9.50:
      Predict: Dog
    else (Detective Stories > 9.50):
      if Art <= 5.50:
        Predict: Dog
      else (Art > 5.50):
        Predict: Cat
else (Running > 4.50):
  if Art <= 4.50:
    if Running <= 6.50:
      if Detective Stories <= 3.50:
        Predict: Duck
      else (Detective Stories > 3.50):
        Predict: Dog
    else (Running > 6.50):
      if Swimming <= 13.50:
        Predict: Dog
      else (Swimming > 13.50):
        if Running <= 12.50:
          if Gardening <= 5.50:
            Predict: Duck
          else (Gardening > 5.50):
            Predict: Dog
        else (Running > 12.50):
          Predict: Dog
  else (Art > 4.50):
    if Running <= 10.50:
      if Gardening <= 4.50:
        if Swimming <= 5.00:
          if Art <= 11.50:
            Predict: Duck
          else (Art > 11.50):
            if Running <= 9.50:
              Predict: Cat
            else (Running > 9.50):
              Predict: Duck
        else (Swimming > 5.00):
          Predict: Duck
      else (Gardening > 4.50):
        if Swimming <= 4.00:
          if Gardening <= 8.00:
            Predict: Cat
          else (Gardening > 8.00):
            if Art <= 7.50:
              Predict: Dog
            else (Art > 7.50):
              Predict: Cat
        else (Swimming > 4.00):
          if Art <= 5.50:
            if Running <= 8.50:
              Predict: Dog
            else (Running > 8.50):
              Predict: Duck
          else (Art > 5.50):
            if Detective Stories <= 3.00:
              Predict: Dog
            else (Detective Stories > 3.00):
              Predict: Duck
    else (Running > 10.50):
      if Gardening <= 4.50:
        if Swimming <= 4.50:
          if Gardening <= 3.50:
            Predict: Cat
          else (Gardening > 3.50):
            if Running <= 13.50:
              Predict: Cat
            else (Running > 13.50):
              Predict: Duck
        else (Swimming > 4.50):
          Predict: Duck
      else (Gardening > 4.50):
        if Art <= 9.50:
          if Gardening <= 6.50:
            if Running <= 13.50:
              Predict: Dog
            else (Running > 13.50):
              Predict: Duck
          else (Gardening > 6.50):
            if Detective Stories <= 4.50:
              if Art <= 7.00:
                Predict: Duck
              else (Art > 7.00):
                Predict: Dog
            else (Detective Stories > 4.50):
              Predict: Dog
        else (Art > 9.50):
          if Swimming <= 5.00:
            Predict: Cat
          else (Swimming > 5.00):
            if Detective Stories <= 12.00:
              if Running <= 12.00:
                Predict: Duck
              else (Running > 12.00):
                if Running <= 13.50:
                  Predict: Dog
                else (Running > 13.50):
                  Predict: Cat
            else (Detective Stories > 12.00):
              Predict: Dog

Tree 4 Rules:
if Swimming <= 4.50:
  if Running <= 13.50:
    if Running <= 9.00:
      if Detective Stories <= 5.50:
        if Running <= 4.00:
          Predict: Cat
        else (Running > 4.00):
          Predict: Dog
      else (Detective Stories > 5.50):
        Predict: Cat
    else (Running > 9.00):
      if Running <= 10.50:
        Predict: Duck
      else (Running > 10.50):
        if Art <= 12.00:
          if Art <= 7.50:
            if Gardening <= 3.00:
              Predict: Cat
            else (Gardening > 3.00):
              if Gardening <= 6.50:
                Predict: Duck
              else (Gardening > 6.50):
                Predict: Dog
          else (Art > 7.50):
            Predict: Cat
        else (Art > 12.00):
          if Gardening <= 3.00:
            Predict: Duck
          else (Gardening > 3.00):
            Predict: Cat
  else (Running > 13.50):
    Predict: Duck
else (Swimming > 4.50):
  if Gardening <= 4.50:
    if Running <= 13.50:
      if Running <= 3.50:
        if Detective Stories <= 11.00:
          Predict: Cat
        else (Detective Stories > 11.00):
          Predict: Duck
      else (Running > 3.50):
        Predict: Duck
    else (Running > 13.50):
      if Art <= 5.50:
        Predict: Dog
      else (Art > 5.50):
        Predict: Duck
  else (Gardening > 4.50):
    if Running <= 9.50:
      if Art <= 6.50:
        if Gardening <= 8.50:
          if Art <= 1.50:
            Predict: Dog
          else (Art > 1.50):
            if Art <= 2.50:
              Predict: Duck
            else (Art > 2.50):
              if Running <= 6.50:
                if Gardening <= 6.50:
                  Predict: Dog
                else (Gardening > 6.50):
                  Predict: Duck
              else (Running > 6.50):
                if Swimming <= 10.00:
                  if Gardening <= 7.00:
                    Predict: Dog
                  else (Gardening > 7.00):
                    Predict: Duck
                else (Swimming > 10.00):
                  Predict: Dog
        else (Gardening > 8.50):
          Predict: Dog
      else (Art > 6.50):
        if Running <= 4.50:
          Predict: Cat
        else (Running > 4.50):
          Predict: Duck
    else (Running > 9.50):
      if Art <= 4.50:
        Predict: Dog
      else (Art > 4.50):
        if Swimming <= 9.50:
          Predict: Dog
        else (Swimming > 9.50):
          if Running <= 13.50:
            if Detective Stories <= 12.00:
              if Swimming <= 12.50:
                Predict: Duck
              else (Swimming > 12.50):
                Predict: Dog
            else (Detective Stories > 12.00):
              Predict: Dog
          else (Running > 13.50):
            if Swimming <= 12.50:
              Predict: Duck
            else (Swimming > 12.50):
              Predict: Cat

Aggregated Rules (Across Forest):
Swimming <= 4.50 appeared in 42.9% of paths.
Art > 4.50 appeared in 49.0% of paths.

Class: Dog

Simple Rules (Single Trees):

Tree 0 Rules:
if Swimming <= 4.50:
  if Detective Stories <= 2.50:
    if Swimming <= 2.50:
      Predict: Dog
    else (Swimming > 2.50):
      Predict: Cat
  else (Detective Stories > 2.50):
    if Running <= 8.50:
      if Swimming <= 0.50:
        if Detective Stories <= 6.00:
          Predict: Dog
        else (Detective Stories > 6.00):
          Predict: Cat
      else (Swimming > 0.50):
        Predict: Cat
    else (Running > 8.50):
      if Detective Stories <= 8.00:
        Predict: Dog
      else (Detective Stories > 8.00):
        if Gardening <= 1.50:
          if Gardening <= 0.50:
            if Art <= 12.00:
              Predict: Duck
            else (Art > 12.00):
              Predict: Cat
          else (Gardening > 0.50):
            Predict: Duck
        else (Gardening > 1.50):
          if Gardening <= 3.50:
            if Swimming <= 2.00:
              Predict: Duck
            else (Swimming > 2.00):
              if Detective Stories <= 11.00:
                if Detective Stories <= 9.50:
                  Predict: Cat
                else (Detective Stories > 9.50):
                  Predict: Duck
              else (Detective Stories > 11.00):
                Predict: Cat
          else (Gardening > 3.50):
            Predict: Cat
else (Swimming > 4.50):
  if Art <= 4.50:
    if Running <= 6.50:
      if Gardening <= 6.50:
        Predict: Duck
      else (Gardening > 6.50):
        if Detective Stories <= 9.00:
          Predict: Dog
        else (Detective Stories > 9.00):
          if Detective Stories <= 13.00:
            if Gardening <= 11.00:
              Predict: Duck
            else (Gardening > 11.00):
              Predict: Dog
          else (Detective Stories > 13.00):
            Predict: Dog
    else (Running > 6.50):
      if Gardening <= 3.50:
        Predict: Duck
      else (Gardening > 3.50):
        Predict: Dog
  else (Art > 4.50):
    if Gardening <= 9.50:
      if Swimming <= 7.50:
        if Detective Stories <= 8.00:
          Predict: Duck
        else (Detective Stories > 8.00):
          if Art <= 6.50:
            Predict: Dog
          else (Art > 6.50):
            if Art <= 13.50:
              if Gardening <= 4.50:
                Predict: Duck
              else (Gardening > 4.50):
                if Swimming <= 6.50:
                  if Art <= 10.00:
                    Predict: Duck
                  else (Art > 10.00):
                    Predict: Dog
                else (Swimming > 6.50):
                  Predict: Dog
            else (Art > 13.50):
              Predict: Duck
      else (Swimming > 7.50):
        if Running <= 13.50:
          if Running <= 4.50:
            if Swimming <= 10.50:
              Predict: Duck
            else (Swimming > 10.50):
              if Detective Stories <= 10.50:
                if Gardening <= 6.50:
                  Predict: Duck
                else (Gardening > 6.50):
                  Predict: Cat
              else (Detective Stories > 10.50):
                Predict: Cat
          else (Running > 4.50):
            if Swimming <= 9.50:
              if Gardening <= 4.50:
                Predict: Duck
              else (Gardening > 4.50):
                if Gardening <= 7.50:
                  Predict: Dog
                else (Gardening > 7.50):
                  Predict: Duck
            else (Swimming > 9.50):
              Predict: Duck
        else (Running > 13.50):
          if Gardening <= 4.00:
            Predict: Duck
          else (Gardening > 4.00):
            if Swimming <= 12.50:
              Predict: Dog
            else (Swimming > 12.50):
              Predict: Cat
    else (Gardening > 9.50):
      if Swimming <= 9.50:
        Predict: Dog
      else (Swimming > 9.50):
        if Detective Stories <= 12.00:
          if Swimming <= 12.50:
            Predict: Duck
          else (Swimming > 12.50):
            if Running <= 9.00:
              Predict: Duck
            else (Running > 9.00):
              Predict: Dog
        else (Detective Stories > 12.00):
          Predict: Dog

Tree 1 Rules:
if Art <= 4.50:
  if Running <= 4.50:
    if Art <= 0.50:
      Predict: Cat
    else (Art > 0.50):
      if Swimming <= 5.00:
        if Detective Stories <= 4.00:
          Predict: Dog
        else (Detective Stories > 4.00):
          Predict: Cat
      else (Swimming > 5.00):
        if Running <= 3.00:
          Predict: Dog
        else (Running > 3.00):
          if Art <= 1.50:
            Predict: Dog
          else (Art > 1.50):
            if Art <= 3.00:
              Predict: Duck
            else (Art > 3.00):
              Predict: Dog
  else (Running > 4.50):
    if Swimming <= 3.50:
      Predict: Cat
    else (Swimming > 3.50):
      if Swimming <= 11.50:
        if Gardening <= 4.50:
          if Detective Stories <= 11.00:
            Predict: Duck
          else (Detective Stories > 11.00):
            Predict: Dog
        else (Gardening > 4.50):
          if Running <= 6.50:
            if Gardening <= 10.50:
              Predict: Duck
            else (Gardening > 10.50):
              Predict: Dog
          else (Running > 6.50):
            Predict: Dog
      else (Swimming > 11.50):
        if Detective Stories <= 5.50:
          Predict: Duck
        else (Detective Stories > 5.50):
          if Running <= 10.50:
            if Gardening <= 4.00:
              Predict: Duck
            else (Gardening > 4.00):
              Predict: Dog
          else (Running > 10.50):
            if Gardening <= 4.00:
              Predict: Duck
            else (Gardening > 4.00):
              Predict: Dog
else (Art > 4.50):
  if Swimming <= 4.50:
    if Running <= 8.50:
      if Swimming <= 0.50:
        if Detective Stories <= 6.00:
          Predict: Dog
        else (Detective Stories > 6.00):
          Predict: Cat
      else (Swimming > 0.50):
        Predict: Cat
    else (Running > 8.50):
      if Running <= 10.50:
        if Detective Stories <= 5.50:
          Predict: Cat
        else (Detective Stories > 5.50):
          Predict: Duck
      else (Running > 10.50):
        if Art <= 8.50:
          Predict: Duck
        else (Art > 8.50):
          Predict: Cat
  else (Swimming > 4.50):
    if Gardening <= 5.50:
      if Running <= 13.50:
        if Swimming <= 12.50:
          Predict: Duck
        else (Swimming > 12.50):
          if Running <= 5.00:
            Predict: Cat
          else (Running > 5.00):
            Predict: Duck
      else (Running > 13.50):
        if Detective Stories <= 10.50:
          Predict: Duck
        else (Detective Stories > 10.50):
          if Art <= 8.50:
            Predict: Dog
          else (Art > 8.50):
            Predict: Cat
    else (Gardening > 5.50):
      if Running <= 4.50:
        Predict: Cat
      else (Running > 4.50):
        if Detective Stories <= 2.00:
          Predict: Dog
        else (Detective Stories > 2.00):
          if Running <= 9.50:
            if Swimming <= 10.50:
              if Art <= 5.50:
                Predict: Dog
              else (Art > 5.50):
                Predict: Duck
            else (Swimming > 10.50):
              Predict: Duck
          else (Running > 9.50):
            if Swimming <= 8.50:
              Predict: Dog
            else (Swimming > 8.50):
              if Running <= 10.50:
                Predict: Dog
              else (Running > 10.50):
                if Gardening <= 13.50:
                  if Detective Stories <= 7.50:
                    if Detective Stories <= 4.50:
                      Predict: Duck
                    else (Detective Stories > 4.50):
                      Predict: Dog
                  else (Detective Stories > 7.50):
                    Predict: Duck
                else (Gardening > 13.50):
                  Predict: Dog

Tree 2 Rules:
if Art <= 4.50:
  if Gardening <= 4.50:
    if Swimming <= 4.50:
      if Running <= 8.00:
        Predict: Cat
      else (Running > 8.00):
        Predict: Duck
    else (Swimming > 4.50):
      if Running <= 13.00:
        if Gardening <= 0.50:
          Predict: Duck
        else (Gardening > 0.50):
          if Running <= 8.50:
            Predict: Duck
          else (Running > 8.50):
            if Detective Stories <= 10.50:
              Predict: Dog
            else (Detective Stories > 10.50):
              Predict: Duck
      else (Running > 13.00):
        Predict: Dog
  else (Gardening > 4.50):
    if Swimming <= 13.50:
      if Running <= 6.50:
        if Running <= 5.50:
          Predict: Dog
        else (Running > 5.50):
          if Art <= 2.50:
            Predict: Dog
          else (Art > 2.50):
            if Swimming <= 10.00:
              if Swimming <= 4.00:
                Predict: Dog
              else (Swimming > 4.00):
                Predict: Duck
            else (Swimming > 10.00):
              Predict: Dog
      else (Running > 6.50):
        Predict: Dog
    else (Swimming > 13.50):
      if Running <= 7.00:
        Predict: Duck
      else (Running > 7.00):
        Predict: Dog
else (Art > 4.50):
  if Art <= 9.50:
    if Swimming <= 4.50:
      if Art <= 8.50:
        if Running <= 10.50:
          Predict: Cat
        else (Running > 10.50):
          Predict: Duck
      else (Art > 8.50):
        if Gardening <= 6.00:
          Predict: Duck
        else (Gardening > 6.00):
          Predict: Cat
    else (Swimming > 4.50):
      if Detective Stories <= 6.50:
        if Swimming <= 13.00:
          if Art <= 5.50:
            Predict: Dog
          else (Art > 5.50):
            if Gardening <= 9.50:
              Predict: Duck
            else (Gardening > 9.50):
              if Swimming <= 10.50:
                Predict: Duck
              else (Swimming > 10.50):
                Predict: Dog
        else (Swimming > 13.00):
          Predict: Dog
      else (Detective Stories > 6.50):
        if Running <= 9.50:
          if Gardening <= 10.50:
            Predict: Duck
          else (Gardening > 10.50):
            if Swimming <= 10.50:
              Predict: Dog
            else (Swimming > 10.50):
              Predict: Duck
        else (Running > 9.50):
          if Art <= 6.50:
            if Gardening <= 4.50:
              Predict: Duck
            else (Gardening > 4.50):
              Predict: Dog
          else (Art > 6.50):
            if Gardening <= 9.50:
              if Running <= 10.50:
                if Swimming <= 8.50:
                  Predict: Duck
                else (Swimming > 8.50):
                  Predict: Dog
              else (Running > 10.50):
                Predict: Duck
            else (Gardening > 9.50):
              Predict: Dog
  else (Art > 9.50):
    if Detective Stories <= 2.50:
      if Swimming <= 7.00:
        Predict: Cat
      else (Swimming > 7.00):
        Predict: Duck
    else (Detective Stories > 2.50):
      if Running <= 4.50:
        Predict: Cat
      else (Running > 4.50):
        if Swimming <= 7.50:
          if Gardening <= 11.50:
            if Gardening <= 2.00:
              if Swimming <= 2.50:
                Predict: Duck
              else (Swimming > 2.50):
                Predict: Cat
            else (Gardening > 2.00):
              Predict: Cat
          else (Gardening > 11.50):
            Predict: Dog
        else (Swimming > 7.50):
          if Swimming <= 9.50:
            if Gardening <= 4.50:
              Predict: Duck
            else (Gardening > 4.50):
              if Running <= 12.00:
                Predict: Duck
              else (Running > 12.00):
                Predict: Dog
          else (Swimming > 9.50):
            Predict: Duck

Tree 3 Rules:
if Running <= 4.50:
  if Gardening <= 10.00:
    if Art <= 5.50:
      if Swimming <= 10.50:
        if Gardening <= 4.00:
          Predict: Cat
        else (Gardening > 4.00):
          if Detective Stories <= 11.50:
            Predict: Cat
          else (Detective Stories > 11.50):
            Predict: Dog
      else (Swimming > 10.50):
        if Detective Stories <= 6.00:
          Predict: Dog
        else (Detective Stories > 6.00):
          Predict: Duck
    else (Art > 5.50):
      if Swimming <= 7.00:
        Predict: Cat
      else (Swimming > 7.00):
        if Running <= 2.50:
          if Gardening <= 2.50:
            Predict: Duck
          else (Gardening > 2.50):
            if Swimming <= 12.50:
              if Art <= 10.00:
                Predict: Duck
              else (Art > 10.00):
                Predict: Cat
            else (Swimming > 12.50):
              Predict: Cat
        else (Running > 2.50):
          Predict: Cat
  else (Gardening > 10.00):
    if Detective Stories <= 9.50:
      Predict: Dog
    else (Detective Stories > 9.50):
      if Art <= 5.50:
        Predict: Dog
      else (Art > 5.50):
        Predict: Cat
else (Running > 4.50):
  if Art <= 4.50:
    if Running <= 6.50:
      if Detective Stories <= 3.50:
        Predict: Duck
      else (Detective Stories > 3.50):
        Predict: Dog
    else (Running > 6.50):
      if Swimming <= 13.50:
        Predict: Dog
      else (Swimming > 13.50):
        if Running <= 12.50:
          if Gardening <= 5.50:
            Predict: Duck
          else (Gardening > 5.50):
            Predict: Dog
        else (Running > 12.50):
          Predict: Dog
  else (Art > 4.50):
    if Running <= 10.50:
      if Gardening <= 4.50:
        if Swimming <= 5.00:
          if Art <= 11.50:
            Predict: Duck
          else (Art > 11.50):
            if Running <= 9.50:
              Predict: Cat
            else (Running > 9.50):
              Predict: Duck
        else (Swimming > 5.00):
          Predict: Duck
      else (Gardening > 4.50):
        if Swimming <= 4.00:
          if Gardening <= 8.00:
            Predict: Cat
          else (Gardening > 8.00):
            if Art <= 7.50:
              Predict: Dog
            else (Art > 7.50):
              Predict: Cat
        else (Swimming > 4.00):
          if Art <= 5.50:
            if Running <= 8.50:
              Predict: Dog
            else (Running > 8.50):
              Predict: Duck
          else (Art > 5.50):
            if Detective Stories <= 3.00:
              Predict: Dog
            else (Detective Stories > 3.00):
              Predict: Duck
    else (Running > 10.50):
      if Gardening <= 4.50:
        if Swimming <= 4.50:
          if Gardening <= 3.50:
            Predict: Cat
          else (Gardening > 3.50):
            if Running <= 13.50:
              Predict: Cat
            else (Running > 13.50):
              Predict: Duck
        else (Swimming > 4.50):
          Predict: Duck
      else (Gardening > 4.50):
        if Art <= 9.50:
          if Gardening <= 6.50:
            if Running <= 13.50:
              Predict: Dog
            else (Running > 13.50):
              Predict: Duck
          else (Gardening > 6.50):
            if Detective Stories <= 4.50:
              if Art <= 7.00:
                Predict: Duck
              else (Art > 7.00):
                Predict: Dog
            else (Detective Stories > 4.50):
              Predict: Dog
        else (Art > 9.50):
          if Swimming <= 5.00:
            Predict: Cat
          else (Swimming > 5.00):
            if Detective Stories <= 12.00:
              if Running <= 12.00:
                Predict: Duck
              else (Running > 12.00):
                if Running <= 13.50:
                  Predict: Dog
                else (Running > 13.50):
                  Predict: Cat
            else (Detective Stories > 12.00):
              Predict: Dog

Tree 4 Rules:
if Swimming <= 4.50:
  if Running <= 13.50:
    if Running <= 9.00:
      if Detective Stories <= 5.50:
        if Running <= 4.00:
          Predict: Cat
        else (Running > 4.00):
          Predict: Dog
      else (Detective Stories > 5.50):
        Predict: Cat
    else (Running > 9.00):
      if Running <= 10.50:
        Predict: Duck
      else (Running > 10.50):
        if Art <= 12.00:
          if Art <= 7.50:
            if Gardening <= 3.00:
              Predict: Cat
            else (Gardening > 3.00):
              if Gardening <= 6.50:
                Predict: Duck
              else (Gardening > 6.50):
                Predict: Dog
          else (Art > 7.50):
            Predict: Cat
        else (Art > 12.00):
          if Gardening <= 3.00:
            Predict: Duck
          else (Gardening > 3.00):
            Predict: Cat
  else (Running > 13.50):
    Predict: Duck
else (Swimming > 4.50):
  if Gardening <= 4.50:
    if Running <= 13.50:
      if Running <= 3.50:
        if Detective Stories <= 11.00:
          Predict: Cat
        else (Detective Stories > 11.00):
          Predict: Duck
      else (Running > 3.50):
        Predict: Duck
    else (Running > 13.50):
      if Art <= 5.50:
        Predict: Dog
      else (Art > 5.50):
        Predict: Duck
  else (Gardening > 4.50):
    if Running <= 9.50:
      if Art <= 6.50:
        if Gardening <= 8.50:
          if Art <= 1.50:
            Predict: Dog
          else (Art > 1.50):
            if Art <= 2.50:
              Predict: Duck
            else (Art > 2.50):
              if Running <= 6.50:
                if Gardening <= 6.50:
                  Predict: Dog
                else (Gardening > 6.50):
                  Predict: Duck
              else (Running > 6.50):
                if Swimming <= 10.00:
                  if Gardening <= 7.00:
                    Predict: Dog
                  else (Gardening > 7.00):
                    Predict: Duck
                else (Swimming > 10.00):
                  Predict: Dog
        else (Gardening > 8.50):
          Predict: Dog
      else (Art > 6.50):
        if Running <= 4.50:
          Predict: Cat
        else (Running > 4.50):
          Predict: Duck
    else (Running > 9.50):
      if Art <= 4.50:
        Predict: Dog
      else (Art > 4.50):
        if Swimming <= 9.50:
          Predict: Dog
        else (Swimming > 9.50):
          if Running <= 13.50:
            if Detective Stories <= 12.00:
              if Swimming <= 12.50:
                Predict: Duck
              else (Swimming > 12.50):
                Predict: Dog
            else (Detective Stories > 12.00):
              Predict: Dog
          else (Running > 13.50):
            if Swimming <= 12.50:
              Predict: Duck
            else (Swimming > 12.50):
              Predict: Cat

Aggregated Rules (Across Forest):
Swimming > 4.50 appeared in 49.4% of paths.
Art <= 4.50 appeared in 33.8% of paths.
Art > 4.50 appeared in 46.8% of paths.
Gardening > 4.50 appeared in 39.0% of paths.
Running > 4.50 appeared in 33.8% of paths.

Class: Duck

Simple Rules (Single Trees):

Tree 0 Rules:
if Swimming <= 4.50:
  if Detective Stories <= 2.50:
    if Swimming <= 2.50:
      Predict: Dog
    else (Swimming > 2.50):
      Predict: Cat
  else (Detective Stories > 2.50):
    if Running <= 8.50:
      if Swimming <= 0.50:
        if Detective Stories <= 6.00:
          Predict: Dog
        else (Detective Stories > 6.00):
          Predict: Cat
      else (Swimming > 0.50):
        Predict: Cat
    else (Running > 8.50):
      if Detective Stories <= 8.00:
        Predict: Dog
      else (Detective Stories > 8.00):
        if Gardening <= 1.50:
          if Gardening <= 0.50:
            if Art <= 12.00:
              Predict: Duck
            else (Art > 12.00):
              Predict: Cat
          else (Gardening > 0.50):
            Predict: Duck
        else (Gardening > 1.50):
          if Gardening <= 3.50:
            if Swimming <= 2.00:
              Predict: Duck
            else (Swimming > 2.00):
              if Detective Stories <= 11.00:
                if Detective Stories <= 9.50:
                  Predict: Cat
                else (Detective Stories > 9.50):
                  Predict: Duck
              else (Detective Stories > 11.00):
                Predict: Cat
          else (Gardening > 3.50):
            Predict: Cat
else (Swimming > 4.50):
  if Art <= 4.50:
    if Running <= 6.50:
      if Gardening <= 6.50:
        Predict: Duck
      else (Gardening > 6.50):
        if Detective Stories <= 9.00:
          Predict: Dog
        else (Detective Stories > 9.00):
          if Detective Stories <= 13.00:
            if Gardening <= 11.00:
              Predict: Duck
            else (Gardening > 11.00):
              Predict: Dog
          else (Detective Stories > 13.00):
            Predict: Dog
    else (Running > 6.50):
      if Gardening <= 3.50:
        Predict: Duck
      else (Gardening > 3.50):
        Predict: Dog
  else (Art > 4.50):
    if Gardening <= 9.50:
      if Swimming <= 7.50:
        if Detective Stories <= 8.00:
          Predict: Duck
        else (Detective Stories > 8.00):
          if Art <= 6.50:
            Predict: Dog
          else (Art > 6.50):
            if Art <= 13.50:
              if Gardening <= 4.50:
                Predict: Duck
              else (Gardening > 4.50):
                if Swimming <= 6.50:
                  if Art <= 10.00:
                    Predict: Duck
                  else (Art > 10.00):
                    Predict: Dog
                else (Swimming > 6.50):
                  Predict: Dog
            else (Art > 13.50):
              Predict: Duck
      else (Swimming > 7.50):
        if Running <= 13.50:
          if Running <= 4.50:
            if Swimming <= 10.50:
              Predict: Duck
            else (Swimming > 10.50):
              if Detective Stories <= 10.50:
                if Gardening <= 6.50:
                  Predict: Duck
                else (Gardening > 6.50):
                  Predict: Cat
              else (Detective Stories > 10.50):
                Predict: Cat
          else (Running > 4.50):
            if Swimming <= 9.50:
              if Gardening <= 4.50:
                Predict: Duck
              else (Gardening > 4.50):
                if Gardening <= 7.50:
                  Predict: Dog
                else (Gardening > 7.50):
                  Predict: Duck
            else (Swimming > 9.50):
              Predict: Duck
        else (Running > 13.50):
          if Gardening <= 4.00:
            Predict: Duck
          else (Gardening > 4.00):
            if Swimming <= 12.50:
              Predict: Dog
            else (Swimming > 12.50):
              Predict: Cat
    else (Gardening > 9.50):
      if Swimming <= 9.50:
        Predict: Dog
      else (Swimming > 9.50):
        if Detective Stories <= 12.00:
          if Swimming <= 12.50:
            Predict: Duck
          else (Swimming > 12.50):
            if Running <= 9.00:
              Predict: Duck
            else (Running > 9.00):
              Predict: Dog
        else (Detective Stories > 12.00):
          Predict: Dog

Tree 1 Rules:
if Art <= 4.50:
  if Running <= 4.50:
    if Art <= 0.50:
      Predict: Cat
    else (Art > 0.50):
      if Swimming <= 5.00:
        if Detective Stories <= 4.00:
          Predict: Dog
        else (Detective Stories > 4.00):
          Predict: Cat
      else (Swimming > 5.00):
        if Running <= 3.00:
          Predict: Dog
        else (Running > 3.00):
          if Art <= 1.50:
            Predict: Dog
          else (Art > 1.50):
            if Art <= 3.00:
              Predict: Duck
            else (Art > 3.00):
              Predict: Dog
  else (Running > 4.50):
    if Swimming <= 3.50:
      Predict: Cat
    else (Swimming > 3.50):
      if Swimming <= 11.50:
        if Gardening <= 4.50:
          if Detective Stories <= 11.00:
            Predict: Duck
          else (Detective Stories > 11.00):
            Predict: Dog
        else (Gardening > 4.50):
          if Running <= 6.50:
            if Gardening <= 10.50:
              Predict: Duck
            else (Gardening > 10.50):
              Predict: Dog
          else (Running > 6.50):
            Predict: Dog
      else (Swimming > 11.50):
        if Detective Stories <= 5.50:
          Predict: Duck
        else (Detective Stories > 5.50):
          if Running <= 10.50:
            if Gardening <= 4.00:
              Predict: Duck
            else (Gardening > 4.00):
              Predict: Dog
          else (Running > 10.50):
            if Gardening <= 4.00:
              Predict: Duck
            else (Gardening > 4.00):
              Predict: Dog
else (Art > 4.50):
  if Swimming <= 4.50:
    if Running <= 8.50:
      if Swimming <= 0.50:
        if Detective Stories <= 6.00:
          Predict: Dog
        else (Detective Stories > 6.00):
          Predict: Cat
      else (Swimming > 0.50):
        Predict: Cat
    else (Running > 8.50):
      if Running <= 10.50:
        if Detective Stories <= 5.50:
          Predict: Cat
        else (Detective Stories > 5.50):
          Predict: Duck
      else (Running > 10.50):
        if Art <= 8.50:
          Predict: Duck
        else (Art > 8.50):
          Predict: Cat
  else (Swimming > 4.50):
    if Gardening <= 5.50:
      if Running <= 13.50:
        if Swimming <= 12.50:
          Predict: Duck
        else (Swimming > 12.50):
          if Running <= 5.00:
            Predict: Cat
          else (Running > 5.00):
            Predict: Duck
      else (Running > 13.50):
        if Detective Stories <= 10.50:
          Predict: Duck
        else (Detective Stories > 10.50):
          if Art <= 8.50:
            Predict: Dog
          else (Art > 8.50):
            Predict: Cat
    else (Gardening > 5.50):
      if Running <= 4.50:
        Predict: Cat
      else (Running > 4.50):
        if Detective Stories <= 2.00:
          Predict: Dog
        else (Detective Stories > 2.00):
          if Running <= 9.50:
            if Swimming <= 10.50:
              if Art <= 5.50:
                Predict: Dog
              else (Art > 5.50):
                Predict: Duck
            else (Swimming > 10.50):
              Predict: Duck
          else (Running > 9.50):
            if Swimming <= 8.50:
              Predict: Dog
            else (Swimming > 8.50):
              if Running <= 10.50:
                Predict: Dog
              else (Running > 10.50):
                if Gardening <= 13.50:
                  if Detective Stories <= 7.50:
                    if Detective Stories <= 4.50:
                      Predict: Duck
                    else (Detective Stories > 4.50):
                      Predict: Dog
                  else (Detective Stories > 7.50):
                    Predict: Duck
                else (Gardening > 13.50):
                  Predict: Dog

Tree 2 Rules:
if Art <= 4.50:
  if Gardening <= 4.50:
    if Swimming <= 4.50:
      if Running <= 8.00:
        Predict: Cat
      else (Running > 8.00):
        Predict: Duck
    else (Swimming > 4.50):
      if Running <= 13.00:
        if Gardening <= 0.50:
          Predict: Duck
        else (Gardening > 0.50):
          if Running <= 8.50:
            Predict: Duck
          else (Running > 8.50):
            if Detective Stories <= 10.50:
              Predict: Dog
            else (Detective Stories > 10.50):
              Predict: Duck
      else (Running > 13.00):
        Predict: Dog
  else (Gardening > 4.50):
    if Swimming <= 13.50:
      if Running <= 6.50:
        if Running <= 5.50:
          Predict: Dog
        else (Running > 5.50):
          if Art <= 2.50:
            Predict: Dog
          else (Art > 2.50):
            if Swimming <= 10.00:
              if Swimming <= 4.00:
                Predict: Dog
              else (Swimming > 4.00):
                Predict: Duck
            else (Swimming > 10.00):
              Predict: Dog
      else (Running > 6.50):
        Predict: Dog
    else (Swimming > 13.50):
      if Running <= 7.00:
        Predict: Duck
      else (Running > 7.00):
        Predict: Dog
else (Art > 4.50):
  if Art <= 9.50:
    if Swimming <= 4.50:
      if Art <= 8.50:
        if Running <= 10.50:
          Predict: Cat
        else (Running > 10.50):
          Predict: Duck
      else (Art > 8.50):
        if Gardening <= 6.00:
          Predict: Duck
        else (Gardening > 6.00):
          Predict: Cat
    else (Swimming > 4.50):
      if Detective Stories <= 6.50:
        if Swimming <= 13.00:
          if Art <= 5.50:
            Predict: Dog
          else (Art > 5.50):
            if Gardening <= 9.50:
              Predict: Duck
            else (Gardening > 9.50):
              if Swimming <= 10.50:
                Predict: Duck
              else (Swimming > 10.50):
                Predict: Dog
        else (Swimming > 13.00):
          Predict: Dog
      else (Detective Stories > 6.50):
        if Running <= 9.50:
          if Gardening <= 10.50:
            Predict: Duck
          else (Gardening > 10.50):
            if Swimming <= 10.50:
              Predict: Dog
            else (Swimming > 10.50):
              Predict: Duck
        else (Running > 9.50):
          if Art <= 6.50:
            if Gardening <= 4.50:
              Predict: Duck
            else (Gardening > 4.50):
              Predict: Dog
          else (Art > 6.50):
            if Gardening <= 9.50:
              if Running <= 10.50:
                if Swimming <= 8.50:
                  Predict: Duck
                else (Swimming > 8.50):
                  Predict: Dog
              else (Running > 10.50):
                Predict: Duck
            else (Gardening > 9.50):
              Predict: Dog
  else (Art > 9.50):
    if Detective Stories <= 2.50:
      if Swimming <= 7.00:
        Predict: Cat
      else (Swimming > 7.00):
        Predict: Duck
    else (Detective Stories > 2.50):
      if Running <= 4.50:
        Predict: Cat
      else (Running > 4.50):
        if Swimming <= 7.50:
          if Gardening <= 11.50:
            if Gardening <= 2.00:
              if Swimming <= 2.50:
                Predict: Duck
              else (Swimming > 2.50):
                Predict: Cat
            else (Gardening > 2.00):
              Predict: Cat
          else (Gardening > 11.50):
            Predict: Dog
        else (Swimming > 7.50):
          if Swimming <= 9.50:
            if Gardening <= 4.50:
              Predict: Duck
            else (Gardening > 4.50):
              if Running <= 12.00:
                Predict: Duck
              else (Running > 12.00):
                Predict: Dog
          else (Swimming > 9.50):
            Predict: Duck

Tree 3 Rules:
if Running <= 4.50:
  if Gardening <= 10.00:
    if Art <= 5.50:
      if Swimming <= 10.50:
        if Gardening <= 4.00:
          Predict: Cat
        else (Gardening > 4.00):
          if Detective Stories <= 11.50:
            Predict: Cat
          else (Detective Stories > 11.50):
            Predict: Dog
      else (Swimming > 10.50):
        if Detective Stories <= 6.00:
          Predict: Dog
        else (Detective Stories > 6.00):
          Predict: Duck
    else (Art > 5.50):
      if Swimming <= 7.00:
        Predict: Cat
      else (Swimming > 7.00):
        if Running <= 2.50:
          if Gardening <= 2.50:
            Predict: Duck
          else (Gardening > 2.50):
            if Swimming <= 12.50:
              if Art <= 10.00:
                Predict: Duck
              else (Art > 10.00):
                Predict: Cat
            else (Swimming > 12.50):
              Predict: Cat
        else (Running > 2.50):
          Predict: Cat
  else (Gardening > 10.00):
    if Detective Stories <= 9.50:
      Predict: Dog
    else (Detective Stories > 9.50):
      if Art <= 5.50:
        Predict: Dog
      else (Art > 5.50):
        Predict: Cat
else (Running > 4.50):
  if Art <= 4.50:
    if Running <= 6.50:
      if Detective Stories <= 3.50:
        Predict: Duck
      else (Detective Stories > 3.50):
        Predict: Dog
    else (Running > 6.50):
      if Swimming <= 13.50:
        Predict: Dog
      else (Swimming > 13.50):
        if Running <= 12.50:
          if Gardening <= 5.50:
            Predict: Duck
          else (Gardening > 5.50):
            Predict: Dog
        else (Running > 12.50):
          Predict: Dog
  else (Art > 4.50):
    if Running <= 10.50:
      if Gardening <= 4.50:
        if Swimming <= 5.00:
          if Art <= 11.50:
            Predict: Duck
          else (Art > 11.50):
            if Running <= 9.50:
              Predict: Cat
            else (Running > 9.50):
              Predict: Duck
        else (Swimming > 5.00):
          Predict: Duck
      else (Gardening > 4.50):
        if Swimming <= 4.00:
          if Gardening <= 8.00:
            Predict: Cat
          else (Gardening > 8.00):
            if Art <= 7.50:
              Predict: Dog
            else (Art > 7.50):
              Predict: Cat
        else (Swimming > 4.00):
          if Art <= 5.50:
            if Running <= 8.50:
              Predict: Dog
            else (Running > 8.50):
              Predict: Duck
          else (Art > 5.50):
            if Detective Stories <= 3.00:
              Predict: Dog
            else (Detective Stories > 3.00):
              Predict: Duck
    else (Running > 10.50):
      if Gardening <= 4.50:
        if Swimming <= 4.50:
          if Gardening <= 3.50:
            Predict: Cat
          else (Gardening > 3.50):
            if Running <= 13.50:
              Predict: Cat
            else (Running > 13.50):
              Predict: Duck
        else (Swimming > 4.50):
          Predict: Duck
      else (Gardening > 4.50):
        if Art <= 9.50:
          if Gardening <= 6.50:
            if Running <= 13.50:
              Predict: Dog
            else (Running > 13.50):
              Predict: Duck
          else (Gardening > 6.50):
            if Detective Stories <= 4.50:
              if Art <= 7.00:
                Predict: Duck
              else (Art > 7.00):
                Predict: Dog
            else (Detective Stories > 4.50):
              Predict: Dog
        else (Art > 9.50):
          if Swimming <= 5.00:
            Predict: Cat
          else (Swimming > 5.00):
            if Detective Stories <= 12.00:
              if Running <= 12.00:
                Predict: Duck
              else (Running > 12.00):
                if Running <= 13.50:
                  Predict: Dog
                else (Running > 13.50):
                  Predict: Cat
            else (Detective Stories > 12.00):
              Predict: Dog

Tree 4 Rules:
if Swimming <= 4.50:
  if Running <= 13.50:
    if Running <= 9.00:
      if Detective Stories <= 5.50:
        if Running <= 4.00:
          Predict: Cat
        else (Running > 4.00):
          Predict: Dog
      else (Detective Stories > 5.50):
        Predict: Cat
    else (Running > 9.00):
      if Running <= 10.50:
        Predict: Duck
      else (Running > 10.50):
        if Art <= 12.00:
          if Art <= 7.50:
            if Gardening <= 3.00:
              Predict: Cat
            else (Gardening > 3.00):
              if Gardening <= 6.50:
                Predict: Duck
              else (Gardening > 6.50):
                Predict: Dog
          else (Art > 7.50):
            Predict: Cat
        else (Art > 12.00):
          if Gardening <= 3.00:
            Predict: Duck
          else (Gardening > 3.00):
            Predict: Cat
  else (Running > 13.50):
    Predict: Duck
else (Swimming > 4.50):
  if Gardening <= 4.50:
    if Running <= 13.50:
      if Running <= 3.50:
        if Detective Stories <= 11.00:
          Predict: Cat
        else (Detective Stories > 11.00):
          Predict: Duck
      else (Running > 3.50):
        Predict: Duck
    else (Running > 13.50):
      if Art <= 5.50:
        Predict: Dog
      else (Art > 5.50):
        Predict: Duck
  else (Gardening > 4.50):
    if Running <= 9.50:
      if Art <= 6.50:
        if Gardening <= 8.50:
          if Art <= 1.50:
            Predict: Dog
          else (Art > 1.50):
            if Art <= 2.50:
              Predict: Duck
            else (Art > 2.50):
              if Running <= 6.50:
                if Gardening <= 6.50:
                  Predict: Dog
                else (Gardening > 6.50):
                  Predict: Duck
              else (Running > 6.50):
                if Swimming <= 10.00:
                  if Gardening <= 7.00:
                    Predict: Dog
                  else (Gardening > 7.00):
                    Predict: Duck
                else (Swimming > 10.00):
                  Predict: Dog
        else (Gardening > 8.50):
          Predict: Dog
      else (Art > 6.50):
        if Running <= 4.50:
          Predict: Cat
        else (Running > 4.50):
          Predict: Duck
    else (Running > 9.50):
      if Art <= 4.50:
        Predict: Dog
      else (Art > 4.50):
        if Swimming <= 9.50:
          Predict: Dog
        else (Swimming > 9.50):
          if Running <= 13.50:
            if Detective Stories <= 12.00:
              if Swimming <= 12.50:
                Predict: Duck
              else (Swimming > 12.50):
                Predict: Dog
            else (Detective Stories > 12.00):
              Predict: Dog
          else (Running > 13.50):
            if Swimming <= 12.50:
              Predict: Duck
            else (Swimming > 12.50):
              Predict: Cat

Aggregated Rules (Across Forest):
Swimming > 4.50 appeared in 51.2% of paths.
Art > 4.50 appeared in 57.3% of paths.
Running > 4.50 appeared in 35.4% of paths.

### Setting: Medium (n_trees=10, threshold=0.5) ###

Class: Cat

Simple Rules (Single Trees):

Tree 0 Rules:
if Swimming <= 4.50:
  if Detective Stories <= 2.50:
    if Swimming <= 2.50:
      Predict: Dog
    else (Swimming > 2.50):
      Predict: Cat
  else (Detective Stories > 2.50):
    if Running <= 8.50:
      if Swimming <= 0.50:
        if Detective Stories <= 6.00:
          Predict: Dog
        else (Detective Stories > 6.00):
          Predict: Cat
      else (Swimming > 0.50):
        Predict: Cat
    else (Running > 8.50):
      if Detective Stories <= 8.00:
        Predict: Dog
      else (Detective Stories > 8.00):
        if Gardening <= 1.50:
          if Gardening <= 0.50:
            if Art <= 12.00:
              Predict: Duck
            else (Art > 12.00):
              Predict: Cat
          else (Gardening > 0.50):
            Predict: Duck
        else (Gardening > 1.50):
          if Gardening <= 3.50:
            if Swimming <= 2.00:
              Predict: Duck
            else (Swimming > 2.00):
              if Detective Stories <= 11.00:
                if Detective Stories <= 9.50:
                  Predict: Cat
                else (Detective Stories > 9.50):
                  Predict: Duck
              else (Detective Stories > 11.00):
                Predict: Cat
          else (Gardening > 3.50):
            Predict: Cat
else (Swimming > 4.50):
  if Art <= 4.50:
    if Running <= 6.50:
      if Gardening <= 6.50:
        Predict: Duck
      else (Gardening > 6.50):
        if Detective Stories <= 9.00:
          Predict: Dog
        else (Detective Stories > 9.00):
          if Detective Stories <= 13.00:
            if Gardening <= 11.00:
              Predict: Duck
            else (Gardening > 11.00):
              Predict: Dog
          else (Detective Stories > 13.00):
            Predict: Dog
    else (Running > 6.50):
      if Gardening <= 3.50:
        Predict: Duck
      else (Gardening > 3.50):
        Predict: Dog
  else (Art > 4.50):
    if Gardening <= 9.50:
      if Swimming <= 7.50:
        if Detective Stories <= 8.00:
          Predict: Duck
        else (Detective Stories > 8.00):
          if Art <= 6.50:
            Predict: Dog
          else (Art > 6.50):
            if Art <= 13.50:
              if Gardening <= 4.50:
                Predict: Duck
              else (Gardening > 4.50):
                if Swimming <= 6.50:
                  if Art <= 10.00:
                    Predict: Duck
                  else (Art > 10.00):
                    Predict: Dog
                else (Swimming > 6.50):
                  Predict: Dog
            else (Art > 13.50):
              Predict: Duck
      else (Swimming > 7.50):
        if Running <= 13.50:
          if Running <= 4.50:
            if Swimming <= 10.50:
              Predict: Duck
            else (Swimming > 10.50):
              if Detective Stories <= 10.50:
                if Gardening <= 6.50:
                  Predict: Duck
                else (Gardening > 6.50):
                  Predict: Cat
              else (Detective Stories > 10.50):
                Predict: Cat
          else (Running > 4.50):
            if Swimming <= 9.50:
              if Gardening <= 4.50:
                Predict: Duck
              else (Gardening > 4.50):
                if Gardening <= 7.50:
                  Predict: Dog
                else (Gardening > 7.50):
                  Predict: Duck
            else (Swimming > 9.50):
              Predict: Duck
        else (Running > 13.50):
          if Gardening <= 4.00:
            Predict: Duck
          else (Gardening > 4.00):
            if Swimming <= 12.50:
              Predict: Dog
            else (Swimming > 12.50):
              Predict: Cat
    else (Gardening > 9.50):
      if Swimming <= 9.50:
        Predict: Dog
      else (Swimming > 9.50):
        if Detective Stories <= 12.00:
          if Swimming <= 12.50:
            Predict: Duck
          else (Swimming > 12.50):
            if Running <= 9.00:
              Predict: Duck
            else (Running > 9.00):
              Predict: Dog
        else (Detective Stories > 12.00):
          Predict: Dog

Tree 1 Rules:
if Art <= 4.50:
  if Running <= 4.50:
    if Art <= 0.50:
      Predict: Cat
    else (Art > 0.50):
      if Swimming <= 5.00:
        if Detective Stories <= 4.00:
          Predict: Dog
        else (Detective Stories > 4.00):
          Predict: Cat
      else (Swimming > 5.00):
        if Running <= 3.00:
          Predict: Dog
        else (Running > 3.00):
          if Art <= 1.50:
            Predict: Dog
          else (Art > 1.50):
            if Art <= 3.00:
              Predict: Duck
            else (Art > 3.00):
              Predict: Dog
  else (Running > 4.50):
    if Swimming <= 3.50:
      Predict: Cat
    else (Swimming > 3.50):
      if Swimming <= 11.50:
        if Gardening <= 4.50:
          if Detective Stories <= 11.00:
            Predict: Duck
          else (Detective Stories > 11.00):
            Predict: Dog
        else (Gardening > 4.50):
          if Running <= 6.50:
            if Gardening <= 10.50:
              Predict: Duck
            else (Gardening > 10.50):
              Predict: Dog
          else (Running > 6.50):
            Predict: Dog
      else (Swimming > 11.50):
        if Detective Stories <= 5.50:
          Predict: Duck
        else (Detective Stories > 5.50):
          if Running <= 10.50:
            if Gardening <= 4.00:
              Predict: Duck
            else (Gardening > 4.00):
              Predict: Dog
          else (Running > 10.50):
            if Gardening <= 4.00:
              Predict: Duck
            else (Gardening > 4.00):
              Predict: Dog
else (Art > 4.50):
  if Swimming <= 4.50:
    if Running <= 8.50:
      if Swimming <= 0.50:
        if Detective Stories <= 6.00:
          Predict: Dog
        else (Detective Stories > 6.00):
          Predict: Cat
      else (Swimming > 0.50):
        Predict: Cat
    else (Running > 8.50):
      if Running <= 10.50:
        if Detective Stories <= 5.50:
          Predict: Cat
        else (Detective Stories > 5.50):
          Predict: Duck
      else (Running > 10.50):
        if Art <= 8.50:
          Predict: Duck
        else (Art > 8.50):
          Predict: Cat
  else (Swimming > 4.50):
    if Gardening <= 5.50:
      if Running <= 13.50:
        if Swimming <= 12.50:
          Predict: Duck
        else (Swimming > 12.50):
          if Running <= 5.00:
            Predict: Cat
          else (Running > 5.00):
            Predict: Duck
      else (Running > 13.50):
        if Detective Stories <= 10.50:
          Predict: Duck
        else (Detective Stories > 10.50):
          if Art <= 8.50:
            Predict: Dog
          else (Art > 8.50):
            Predict: Cat
    else (Gardening > 5.50):
      if Running <= 4.50:
        Predict: Cat
      else (Running > 4.50):
        if Detective Stories <= 2.00:
          Predict: Dog
        else (Detective Stories > 2.00):
          if Running <= 9.50:
            if Swimming <= 10.50:
              if Art <= 5.50:
                Predict: Dog
              else (Art > 5.50):
                Predict: Duck
            else (Swimming > 10.50):
              Predict: Duck
          else (Running > 9.50):
            if Swimming <= 8.50:
              Predict: Dog
            else (Swimming > 8.50):
              if Running <= 10.50:
                Predict: Dog
              else (Running > 10.50):
                if Gardening <= 13.50:
                  if Detective Stories <= 7.50:
                    if Detective Stories <= 4.50:
                      Predict: Duck
                    else (Detective Stories > 4.50):
                      Predict: Dog
                  else (Detective Stories > 7.50):
                    Predict: Duck
                else (Gardening > 13.50):
                  Predict: Dog

Tree 2 Rules:
if Art <= 4.50:
  if Gardening <= 4.50:
    if Swimming <= 4.50:
      if Running <= 8.00:
        Predict: Cat
      else (Running > 8.00):
        Predict: Duck
    else (Swimming > 4.50):
      if Running <= 13.00:
        if Gardening <= 0.50:
          Predict: Duck
        else (Gardening > 0.50):
          if Running <= 8.50:
            Predict: Duck
          else (Running > 8.50):
            if Detective Stories <= 10.50:
              Predict: Dog
            else (Detective Stories > 10.50):
              Predict: Duck
      else (Running > 13.00):
        Predict: Dog
  else (Gardening > 4.50):
    if Swimming <= 13.50:
      if Running <= 6.50:
        if Running <= 5.50:
          Predict: Dog
        else (Running > 5.50):
          if Art <= 2.50:
            Predict: Dog
          else (Art > 2.50):
            if Swimming <= 10.00:
              if Swimming <= 4.00:
                Predict: Dog
              else (Swimming > 4.00):
                Predict: Duck
            else (Swimming > 10.00):
              Predict: Dog
      else (Running > 6.50):
        Predict: Dog
    else (Swimming > 13.50):
      if Running <= 7.00:
        Predict: Duck
      else (Running > 7.00):
        Predict: Dog
else (Art > 4.50):
  if Art <= 9.50:
    if Swimming <= 4.50:
      if Art <= 8.50:
        if Running <= 10.50:
          Predict: Cat
        else (Running > 10.50):
          Predict: Duck
      else (Art > 8.50):
        if Gardening <= 6.00:
          Predict: Duck
        else (Gardening > 6.00):
          Predict: Cat
    else (Swimming > 4.50):
      if Detective Stories <= 6.50:
        if Swimming <= 13.00:
          if Art <= 5.50:
            Predict: Dog
          else (Art > 5.50):
            if Gardening <= 9.50:
              Predict: Duck
            else (Gardening > 9.50):
              if Swimming <= 10.50:
                Predict: Duck
              else (Swimming > 10.50):
                Predict: Dog
        else (Swimming > 13.00):
          Predict: Dog
      else (Detective Stories > 6.50):
        if Running <= 9.50:
          if Gardening <= 10.50:
            Predict: Duck
          else (Gardening > 10.50):
            if Swimming <= 10.50:
              Predict: Dog
            else (Swimming > 10.50):
              Predict: Duck
        else (Running > 9.50):
          if Art <= 6.50:
            if Gardening <= 4.50:
              Predict: Duck
            else (Gardening > 4.50):
              Predict: Dog
          else (Art > 6.50):
            if Gardening <= 9.50:
              if Running <= 10.50:
                if Swimming <= 8.50:
                  Predict: Duck
                else (Swimming > 8.50):
                  Predict: Dog
              else (Running > 10.50):
                Predict: Duck
            else (Gardening > 9.50):
              Predict: Dog
  else (Art > 9.50):
    if Detective Stories <= 2.50:
      if Swimming <= 7.00:
        Predict: Cat
      else (Swimming > 7.00):
        Predict: Duck
    else (Detective Stories > 2.50):
      if Running <= 4.50:
        Predict: Cat
      else (Running > 4.50):
        if Swimming <= 7.50:
          if Gardening <= 11.50:
            if Gardening <= 2.00:
              if Swimming <= 2.50:
                Predict: Duck
              else (Swimming > 2.50):
                Predict: Cat
            else (Gardening > 2.00):
              Predict: Cat
          else (Gardening > 11.50):
            Predict: Dog
        else (Swimming > 7.50):
          if Swimming <= 9.50:
            if Gardening <= 4.50:
              Predict: Duck
            else (Gardening > 4.50):
              if Running <= 12.00:
                Predict: Duck
              else (Running > 12.00):
                Predict: Dog
          else (Swimming > 9.50):
            Predict: Duck

Tree 3 Rules:
if Running <= 4.50:
  if Gardening <= 10.00:
    if Art <= 5.50:
      if Swimming <= 10.50:
        if Gardening <= 4.00:
          Predict: Cat
        else (Gardening > 4.00):
          if Detective Stories <= 11.50:
            Predict: Cat
          else (Detective Stories > 11.50):
            Predict: Dog
      else (Swimming > 10.50):
        if Detective Stories <= 6.00:
          Predict: Dog
        else (Detective Stories > 6.00):
          Predict: Duck
    else (Art > 5.50):
      if Swimming <= 7.00:
        Predict: Cat
      else (Swimming > 7.00):
        if Running <= 2.50:
          if Gardening <= 2.50:
            Predict: Duck
          else (Gardening > 2.50):
            if Swimming <= 12.50:
              if Art <= 10.00:
                Predict: Duck
              else (Art > 10.00):
                Predict: Cat
            else (Swimming > 12.50):
              Predict: Cat
        else (Running > 2.50):
          Predict: Cat
  else (Gardening > 10.00):
    if Detective Stories <= 9.50:
      Predict: Dog
    else (Detective Stories > 9.50):
      if Art <= 5.50:
        Predict: Dog
      else (Art > 5.50):
        Predict: Cat
else (Running > 4.50):
  if Art <= 4.50:
    if Running <= 6.50:
      if Detective Stories <= 3.50:
        Predict: Duck
      else (Detective Stories > 3.50):
        Predict: Dog
    else (Running > 6.50):
      if Swimming <= 13.50:
        Predict: Dog
      else (Swimming > 13.50):
        if Running <= 12.50:
          if Gardening <= 5.50:
            Predict: Duck
          else (Gardening > 5.50):
            Predict: Dog
        else (Running > 12.50):
          Predict: Dog
  else (Art > 4.50):
    if Running <= 10.50:
      if Gardening <= 4.50:
        if Swimming <= 5.00:
          if Art <= 11.50:
            Predict: Duck
          else (Art > 11.50):
            if Running <= 9.50:
              Predict: Cat
            else (Running > 9.50):
              Predict: Duck
        else (Swimming > 5.00):
          Predict: Duck
      else (Gardening > 4.50):
        if Swimming <= 4.00:
          if Gardening <= 8.00:
            Predict: Cat
          else (Gardening > 8.00):
            if Art <= 7.50:
              Predict: Dog
            else (Art > 7.50):
              Predict: Cat
        else (Swimming > 4.00):
          if Art <= 5.50:
            if Running <= 8.50:
              Predict: Dog
            else (Running > 8.50):
              Predict: Duck
          else (Art > 5.50):
            if Detective Stories <= 3.00:
              Predict: Dog
            else (Detective Stories > 3.00):
              Predict: Duck
    else (Running > 10.50):
      if Gardening <= 4.50:
        if Swimming <= 4.50:
          if Gardening <= 3.50:
            Predict: Cat
          else (Gardening > 3.50):
            if Running <= 13.50:
              Predict: Cat
            else (Running > 13.50):
              Predict: Duck
        else (Swimming > 4.50):
          Predict: Duck
      else (Gardening > 4.50):
        if Art <= 9.50:
          if Gardening <= 6.50:
            if Running <= 13.50:
              Predict: Dog
            else (Running > 13.50):
              Predict: Duck
          else (Gardening > 6.50):
            if Detective Stories <= 4.50:
              if Art <= 7.00:
                Predict: Duck
              else (Art > 7.00):
                Predict: Dog
            else (Detective Stories > 4.50):
              Predict: Dog
        else (Art > 9.50):
          if Swimming <= 5.00:
            Predict: Cat
          else (Swimming > 5.00):
            if Detective Stories <= 12.00:
              if Running <= 12.00:
                Predict: Duck
              else (Running > 12.00):
                if Running <= 13.50:
                  Predict: Dog
                else (Running > 13.50):
                  Predict: Cat
            else (Detective Stories > 12.00):
              Predict: Dog

Tree 4 Rules:
if Swimming <= 4.50:
  if Running <= 13.50:
    if Running <= 9.00:
      if Detective Stories <= 5.50:
        if Running <= 4.00:
          Predict: Cat
        else (Running > 4.00):
          Predict: Dog
      else (Detective Stories > 5.50):
        Predict: Cat
    else (Running > 9.00):
      if Running <= 10.50:
        Predict: Duck
      else (Running > 10.50):
        if Art <= 12.00:
          if Art <= 7.50:
            if Gardening <= 3.00:
              Predict: Cat
            else (Gardening > 3.00):
              if Gardening <= 6.50:
                Predict: Duck
              else (Gardening > 6.50):
                Predict: Dog
          else (Art > 7.50):
            Predict: Cat
        else (Art > 12.00):
          if Gardening <= 3.00:
            Predict: Duck
          else (Gardening > 3.00):
            Predict: Cat
  else (Running > 13.50):
    Predict: Duck
else (Swimming > 4.50):
  if Gardening <= 4.50:
    if Running <= 13.50:
      if Running <= 3.50:
        if Detective Stories <= 11.00:
          Predict: Cat
        else (Detective Stories > 11.00):
          Predict: Duck
      else (Running > 3.50):
        Predict: Duck
    else (Running > 13.50):
      if Art <= 5.50:
        Predict: Dog
      else (Art > 5.50):
        Predict: Duck
  else (Gardening > 4.50):
    if Running <= 9.50:
      if Art <= 6.50:
        if Gardening <= 8.50:
          if Art <= 1.50:
            Predict: Dog
          else (Art > 1.50):
            if Art <= 2.50:
              Predict: Duck
            else (Art > 2.50):
              if Running <= 6.50:
                if Gardening <= 6.50:
                  Predict: Dog
                else (Gardening > 6.50):
                  Predict: Duck
              else (Running > 6.50):
                if Swimming <= 10.00:
                  if Gardening <= 7.00:
                    Predict: Dog
                  else (Gardening > 7.00):
                    Predict: Duck
                else (Swimming > 10.00):
                  Predict: Dog
        else (Gardening > 8.50):
          Predict: Dog
      else (Art > 6.50):
        if Running <= 4.50:
          Predict: Cat
        else (Running > 4.50):
          Predict: Duck
    else (Running > 9.50):
      if Art <= 4.50:
        Predict: Dog
      else (Art > 4.50):
        if Swimming <= 9.50:
          Predict: Dog
        else (Swimming > 9.50):
          if Running <= 13.50:
            if Detective Stories <= 12.00:
              if Swimming <= 12.50:
                Predict: Duck
              else (Swimming > 12.50):
                Predict: Dog
            else (Detective Stories > 12.00):
              Predict: Dog
          else (Running > 13.50):
            if Swimming <= 12.50:
              Predict: Duck
            else (Swimming > 12.50):
              Predict: Cat

Tree 5 Rules:
if Running <= 4.50:
  if Art <= 4.50:
    if Gardening <= 5.50:
      Predict: Duck
    else (Gardening > 5.50):
      Predict: Dog
  else (Art > 4.50):
    if Art <= 7.50:
      if Swimming <= 7.00:
        Predict: Cat
      else (Swimming > 7.00):
        Predict: Duck
    else (Art > 7.50):
      Predict: Cat
else (Running > 4.50):
  if Art <= 4.50:
    if Gardening <= 4.50:
      if Running <= 13.50:
        if Running <= 12.50:
          if Running <= 11.00:
            if Swimming <= 11.50:
              Predict: Dog
            else (Swimming > 11.50):
              Predict: Duck
          else (Running > 11.00):
            Predict: Duck
        else (Running > 12.50):
          Predict: Cat
      else (Running > 13.50):
        Predict: Dog
    else (Gardening > 4.50):
      if Detective Stories <= 2.50:
        if Gardening <= 6.50:
          Predict: Duck
        else (Gardening > 6.50):
          Predict: Dog
      else (Detective Stories > 2.50):
        if Running <= 6.50:
          if Swimming <= 8.50:
            if Gardening <= 10.50:
              Predict: Duck
            else (Gardening > 10.50):
              Predict: Dog
          else (Swimming > 8.50):
            Predict: Dog
        else (Running > 6.50):
          Predict: Dog
  else (Art > 4.50):
    if Swimming <= 4.50:
      if Detective Stories <= 6.50:
        if Art <= 9.50:
          Predict: Duck
        else (Art > 9.50):
          Predict: Cat
      else (Detective Stories > 6.50):
        if Art <= 9.50:
          if Running <= 8.00:
            Predict: Cat
          else (Running > 8.00):
            Predict: Duck
        else (Art > 9.50):
          if Swimming <= 1.50:
            if Gardening <= 2.00:
              if Running <= 9.50:
                Predict: Cat
              else (Running > 9.50):
                Predict: Duck
            else (Gardening > 2.00):
              Predict: Cat
          else (Swimming > 1.50):
            Predict: Cat
    else (Swimming > 4.50):
      if Running <= 10.50:
        if Detective Stories <= 5.50:
          if Swimming <= 10.50:
            Predict: Dog
          else (Swimming > 10.50):
            Predict: Duck
        else (Detective Stories > 5.50):
          Predict: Duck
      else (Running > 10.50):
        if Art <= 6.50:
          if Swimming <= 12.50:
            Predict: Dog
          else (Swimming > 12.50):
            Predict: Duck
        else (Art > 6.50):
          if Gardening <= 4.00:
            Predict: Duck
          else (Gardening > 4.00):
            if Detective Stories <= 12.50:
              if Art <= 9.00:
                Predict: Dog
              else (Art > 9.00):
                if Detective Stories <= 11.50:
                  Predict: Cat
                else (Detective Stories > 11.50):
                  Predict: Duck
            else (Detective Stories > 12.50):
              Predict: Dog

Tree 6 Rules:
if Swimming <= 4.50:
  if Running <= 13.50:
    if Running <= 5.50:
      if Detective Stories <= 2.50:
        if Running <= 1.50:
          Predict: Cat
        else (Running > 1.50):
          Predict: Dog
      else (Detective Stories > 2.50):
        Predict: Cat
    else (Running > 5.50):
      if Art <= 6.50:
        if Gardening <= 6.50:
          if Art <= 2.00:
            Predict: Cat
          else (Art > 2.00):
            Predict: Duck
        else (Gardening > 6.50):
          Predict: Dog
      else (Art > 6.50):
        if Art <= 9.50:
          if Swimming <= 2.00:
            Predict: Duck
          else (Swimming > 2.00):
            Predict: Cat
        else (Art > 9.50):
          if Running <= 9.50:
            Predict: Cat
          else (Running > 9.50):
            if Gardening <= 1.00:
              Predict: Duck
            else (Gardening > 1.00):
              if Running <= 10.50:
                Predict: Duck
              else (Running > 10.50):
                Predict: Cat
  else (Running > 13.50):
    if Swimming <= 1.50:
      Predict: Dog
    else (Swimming > 1.50):
      Predict: Duck
else (Swimming > 4.50):
  if Art <= 4.50:
    if Gardening <= 2.50:
      if Art <= 3.50:
        if Gardening <= 0.50:
          Predict: Duck
        else (Gardening > 0.50):
          if Running <= 8.50:
            Predict: Duck
          else (Running > 8.50):
            Predict: Dog
      else (Art > 3.50):
        if Swimming <= 11.00:
          Predict: Dog
        else (Swimming > 11.00):
          Predict: Duck
    else (Gardening > 2.50):
      if Detective Stories <= 3.50:
        if Gardening <= 6.50:
          Predict: Duck
        else (Gardening > 6.50):
          Predict: Dog
      else (Detective Stories > 3.50):
        if Swimming <= 8.50:
          if Swimming <= 7.50:
            Predict: Dog
          else (Swimming > 7.50):
            if Running <= 8.00:
              Predict: Duck
            else (Running > 8.00):
              Predict: Dog
        else (Swimming > 8.50):
          Predict: Dog
  else (Art > 4.50):
    if Detective Stories <= 1.50:
      if Art <= 9.00:
        Predict: Dog
      else (Art > 9.00):
        Predict: Duck
    else (Detective Stories > 1.50):
      if Running <= 10.50:
        if Gardening <= 10.50:
          if Art <= 9.50:
            if Detective Stories <= 5.50:
              if Running <= 7.50:
                Predict: Duck
              else (Running > 7.50):
                Predict: Dog
            else (Detective Stories > 5.50):
              if Swimming <= 13.50:
                if Running <= 9.50:
                  Predict: Duck
                else (Running > 9.50):
                  if Gardening <= 4.00:
                    Predict: Duck
                  else (Gardening > 4.00):
                    Predict: Dog
              else (Swimming > 13.50):
                if Detective Stories <= 11.50:
                  Predict: Duck
                else (Detective Stories > 11.50):
                  Predict: Dog
          else (Art > 9.50):
            if Running <= 4.00:
              Predict: Cat
            else (Running > 4.00):
              Predict: Duck
        else (Gardening > 10.50):
          Predict: Dog
      else (Running > 10.50):
        if Swimming <= 8.50:
          if Gardening <= 2.00:
            Predict: Duck
          else (Gardening > 2.00):
            Predict: Dog
        else (Swimming > 8.50):
          if Running <= 13.50:
            if Swimming <= 13.50:
              if Detective Stories <= 12.50:
                if Gardening <= 13.50:
                  if Running <= 11.50:
                    if Art <= 8.00:
                      Predict: Dog
                    else (Art > 8.00):
                      Predict: Duck
                  else (Running > 11.50):
                    Predict: Duck
                else (Gardening > 13.50):
                  Predict: Dog
              else (Detective Stories > 12.50):
                Predict: Dog
            else (Swimming > 13.50):
              if Gardening <= 5.50:
                Predict: Duck
              else (Gardening > 5.50):
                Predict: Dog
          else (Running > 13.50):
            Predict: Duck

Tree 7 Rules:
if Running <= 4.50:
  if Art <= 5.50:
    if Swimming <= 10.00:
      if Art <= 0.50:
        Predict: Cat
      else (Art > 0.50):
        Predict: Dog
    else (Swimming > 10.00):
      Predict: Duck
  else (Art > 5.50):
    if Art <= 9.50:
      if Swimming <= 7.00:
        Predict: Cat
      else (Swimming > 7.00):
        Predict: Duck
    else (Art > 9.50):
      Predict: Cat
else (Running > 4.50):
  if Swimming <= 9.50:
    if Art <= 4.50:
      if Swimming <= 7.50:
        if Gardening <= 3.00:
          Predict: Duck
        else (Gardening > 3.00):
          Predict: Dog
      else (Swimming > 7.50):
        Predict: Dog
    else (Art > 4.50):
      if Gardening <= 9.50:
        if Swimming <= 4.50:
          if Detective Stories <= 7.50:
            Predict: Duck
          else (Detective Stories > 7.50):
            if Running <= 13.50:
              if Art <= 9.50:
                Predict: Duck
              else (Art > 9.50):
                Predict: Cat
            else (Running > 13.50):
              Predict: Duck
        else (Swimming > 4.50):
          if Gardening <= 5.00:
            Predict: Duck
          else (Gardening > 5.00):
            if Art <= 10.00:
              if Gardening <= 7.50:
                Predict: Dog
              else (Gardening > 7.50):
                if Gardening <= 8.50:
                  Predict: Duck
                else (Gardening > 8.50):
                  if Art <= 6.50:
                    Predict: Dog
                  else (Art > 6.50):
                    Predict: Duck
            else (Art > 10.00):
              Predict: Dog
      else (Gardening > 9.50):
        Predict: Dog
  else (Swimming > 9.50):
    if Gardening <= 4.50:
      Predict: Duck
    else (Gardening > 4.50):
      if Art <= 4.50:
        Predict: Dog
      else (Art > 4.50):
        if Gardening <= 8.50:
          if Swimming <= 12.50:
            Predict: Duck
          else (Swimming > 12.50):
            if Running <= 13.50:
              if Detective Stories <= 5.50:
                Predict: Dog
              else (Detective Stories > 5.50):
                Predict: Duck
            else (Running > 13.50):
              Predict: Cat
        else (Gardening > 8.50):
          if Swimming <= 11.00:
            if Art <= 8.50:
              Predict: Dog
            else (Art > 8.50):
              Predict: Duck
          else (Swimming > 11.00):
            if Gardening <= 13.50:
              Predict: Dog
            else (Gardening > 13.50):
              if Running <= 11.00:
                Predict: Duck
              else (Running > 11.00):
                Predict: Dog

Tree 8 Rules:
if Swimming <= 4.50:
  if Gardening <= 8.50:
    if Gardening <= 2.50:
      if Detective Stories <= 11.50:
        if Art <= 10.50:
          Predict: Cat
        else (Art > 10.50):
          if Art <= 11.50:
            if Gardening <= 0.50:
              if Running <= 6.00:
                Predict: Cat
              else (Running > 6.00):
                Predict: Duck
            else (Gardening > 0.50):
              Predict: Cat
          else (Art > 11.50):
            Predict: Cat
      else (Detective Stories > 11.50):
        if Running <= 6.50:
          Predict: Cat
        else (Running > 6.50):
          Predict: Duck
    else (Gardening > 2.50):
      if Detective Stories <= 6.50:
        if Running <= 8.00:
          Predict: Cat
        else (Running > 8.00):
          Predict: Duck
      else (Detective Stories > 6.50):
        if Detective Stories <= 10.50:
          if Gardening <= 3.50:
            if Running <= 7.00:
              Predict: Cat
            else (Running > 7.00):
              Predict: Duck
          else (Gardening > 3.50):
            Predict: Cat
        else (Detective Stories > 10.50):
          Predict: Cat
  else (Gardening > 8.50):
    if Running <= 5.00:
      Predict: Cat
    else (Running > 5.00):
      if Art <= 7.50:
        Predict: Dog
      else (Art > 7.50):
        Predict: Cat
else (Swimming > 4.50):
  if Running <= 9.50:
    if Art <= 5.50:
      if Art <= 1.50:
        if Art <= 0.50:
          if Running <= 3.50:
            Predict: Duck
          else (Running > 3.50):
            Predict: Dog
        else (Art > 0.50):
          Predict: Dog
      else (Art > 1.50):
        if Detective Stories <= 4.50:
          Predict: Duck
        else (Detective Stories > 4.50):
          if Gardening <= 4.50:
            Predict: Duck
          else (Gardening > 4.50):
            if Gardening <= 9.50:
              if Detective Stories <= 11.00:
                Predict: Dog
              else (Detective Stories > 11.00):
                if Swimming <= 10.50:
                  Predict: Duck
                else (Swimming > 10.50):
                  Predict: Dog
            else (Gardening > 9.50):
              Predict: Dog
    else (Art > 5.50):
      Predict: Duck
  else (Running > 9.50):
    if Swimming <= 9.50:
      if Detective Stories <= 12.50:
        if Detective Stories <= 10.50:
          if Running <= 13.50:
            if Art <= 9.00:
              Predict: Dog
            else (Art > 9.00):
              if Gardening <= 8.00:
                Predict: Dog
              else (Gardening > 8.00):
                Predict: Duck
          else (Running > 13.50):
            if Art <= 10.50:
              Predict: Dog
            else (Art > 10.50):
              Predict: Duck
        else (Detective Stories > 10.50):
          Predict: Dog
      else (Detective Stories > 12.50):
        if Gardening <= 4.50:
          Predict: Duck
        else (Gardening > 4.50):
          Predict: Dog
    else (Swimming > 9.50):
      if Gardening <= 6.50:
        if Running <= 10.50:
          if Swimming <= 13.00:
            Predict: Duck
          else (Swimming > 13.00):
            Predict: Dog
        else (Running > 10.50):
          if Running <= 12.50:
            Predict: Duck
          else (Running > 12.50):
            if Art <= 4.50:
              Predict: Dog
            else (Art > 4.50):
              if Gardening <= 4.00:
                Predict: Duck
              else (Gardening > 4.00):
                if Detective Stories <= 8.50:
                  Predict: Dog
                else (Detective Stories > 8.50):
                  if Running <= 13.50:
                    Predict: Duck
                  else (Running > 13.50):
                    if Swimming <= 12.50:
                      Predict: Duck
                    else (Swimming > 12.50):
                      Predict: Cat
      else (Gardening > 6.50):
        if Detective Stories <= 4.50:
          if Running <= 11.50:
            Predict: Dog
          else (Running > 11.50):
            Predict: Duck
        else (Detective Stories > 4.50):
          if Gardening <= 13.50:
            if Art <= 9.00:
              Predict: Dog
            else (Art > 9.00):
              if Art <= 12.50:
                Predict: Duck
              else (Art > 12.50):
                Predict: Dog
          else (Gardening > 13.50):
            if Swimming <= 12.50:
              Predict: Duck
            else (Swimming > 12.50):
              Predict: Dog

Tree 9 Rules:
if Running <= 4.50:
  if Running <= 3.50:
    if Art <= 0.50:
      Predict: Duck
    else (Art > 0.50):
      if Swimming <= 5.00:
        Predict: Cat
      else (Swimming > 5.00):
        if Swimming <= 10.50:
          if Detective Stories <= 9.50:
            Predict: Dog
          else (Detective Stories > 9.50):
            Predict: Duck
        else (Swimming > 10.50):
          Predict: Cat
  else (Running > 3.50):
    if Gardening <= 4.50:
      Predict: Cat
    else (Gardening > 4.50):
      if Detective Stories <= 11.00:
        if Art <= 5.50:
          Predict: Duck
        else (Art > 5.50):
          Predict: Cat
      else (Detective Stories > 11.00):
        Predict: Cat
else (Running > 4.50):
  if Art <= 4.50:
    if Running <= 9.00:
      if Art <= 1.50:
        Predict: Dog
      else (Art > 1.50):
        if Gardening <= 9.00:
          if Swimming <= 6.50:
            if Art <= 3.50:
              Predict: Duck
            else (Art > 3.50):
              Predict: Dog
          else (Swimming > 6.50):
            Predict: Duck
        else (Gardening > 9.00):
          Predict: Dog
    else (Running > 9.00):
      if Gardening <= 3.50:
        if Swimming <= 11.50:
          if Running <= 11.50:
            Predict: Dog
          else (Running > 11.50):
            Predict: Cat
        else (Swimming > 11.50):
          Predict: Duck
      else (Gardening > 3.50):
        Predict: Dog
  else (Art > 4.50):
    if Swimming <= 4.50:
      if Art <= 9.50:
        if Art <= 8.50:
          if Detective Stories <= 8.50:
            Predict: Dog
          else (Detective Stories > 8.50):
            Predict: Cat
        else (Art > 8.50):
          Predict: Duck
      else (Art > 9.50):
        if Running <= 9.00:
          Predict: Cat
        else (Running > 9.00):
          if Gardening <= 1.00:
            Predict: Duck
          else (Gardening > 1.00):
            Predict: Cat
    else (Swimming > 4.50):
      if Detective Stories <= 1.50:
        Predict: Dog
      else (Detective Stories > 1.50):
        if Gardening <= 4.50:
          if Art <= 7.50:
            if Gardening <= 3.50:
              Predict: Duck
            else (Gardening > 3.50):
              if Running <= 10.50:
                Predict: Duck
              else (Running > 10.50):
                Predict: Dog
          else (Art > 7.50):
            Predict: Duck
        else (Gardening > 4.50):
          if Running <= 9.50:
            if Detective Stories <= 11.50:
              Predict: Duck
            else (Detective Stories > 11.50):
              if Gardening <= 8.50:
                Predict: Duck
              else (Gardening > 8.50):
                if Detective Stories <= 12.50:
                  Predict: Dog
                else (Detective Stories > 12.50):
                  Predict: Duck
          else (Running > 9.50):
            if Art <= 5.50:
              Predict: Duck
            else (Art > 5.50):
              if Detective Stories <= 4.50:
                Predict: Duck
              else (Detective Stories > 4.50):
                if Swimming <= 9.50:
                  Predict: Dog
                else (Swimming > 9.50):
                  if Detective Stories <= 10.50:
                    Predict: Dog
                  else (Detective Stories > 10.50):
                    if Detective Stories <= 12.50:
                      Predict: Duck
                    else (Detective Stories > 12.50):
                      Predict: Dog

Aggregated Rules (Across Forest):
Swimming <= 4.50 appeared in 51.6% of paths.

Class: Dog

Simple Rules (Single Trees):

Tree 0 Rules:
if Swimming <= 4.50:
  if Detective Stories <= 2.50:
    if Swimming <= 2.50:
      Predict: Dog
    else (Swimming > 2.50):
      Predict: Cat
  else (Detective Stories > 2.50):
    if Running <= 8.50:
      if Swimming <= 0.50:
        if Detective Stories <= 6.00:
          Predict: Dog
        else (Detective Stories > 6.00):
          Predict: Cat
      else (Swimming > 0.50):
        Predict: Cat
    else (Running > 8.50):
      if Detective Stories <= 8.00:
        Predict: Dog
      else (Detective Stories > 8.00):
        if Gardening <= 1.50:
          if Gardening <= 0.50:
            if Art <= 12.00:
              Predict: Duck
            else (Art > 12.00):
              Predict: Cat
          else (Gardening > 0.50):
            Predict: Duck
        else (Gardening > 1.50):
          if Gardening <= 3.50:
            if Swimming <= 2.00:
              Predict: Duck
            else (Swimming > 2.00):
              if Detective Stories <= 11.00:
                if Detective Stories <= 9.50:
                  Predict: Cat
                else (Detective Stories > 9.50):
                  Predict: Duck
              else (Detective Stories > 11.00):
                Predict: Cat
          else (Gardening > 3.50):
            Predict: Cat
else (Swimming > 4.50):
  if Art <= 4.50:
    if Running <= 6.50:
      if Gardening <= 6.50:
        Predict: Duck
      else (Gardening > 6.50):
        if Detective Stories <= 9.00:
          Predict: Dog
        else (Detective Stories > 9.00):
          if Detective Stories <= 13.00:
            if Gardening <= 11.00:
              Predict: Duck
            else (Gardening > 11.00):
              Predict: Dog
          else (Detective Stories > 13.00):
            Predict: Dog
    else (Running > 6.50):
      if Gardening <= 3.50:
        Predict: Duck
      else (Gardening > 3.50):
        Predict: Dog
  else (Art > 4.50):
    if Gardening <= 9.50:
      if Swimming <= 7.50:
        if Detective Stories <= 8.00:
          Predict: Duck
        else (Detective Stories > 8.00):
          if Art <= 6.50:
            Predict: Dog
          else (Art > 6.50):
            if Art <= 13.50:
              if Gardening <= 4.50:
                Predict: Duck
              else (Gardening > 4.50):
                if Swimming <= 6.50:
                  if Art <= 10.00:
                    Predict: Duck
                  else (Art > 10.00):
                    Predict: Dog
                else (Swimming > 6.50):
                  Predict: Dog
            else (Art > 13.50):
              Predict: Duck
      else (Swimming > 7.50):
        if Running <= 13.50:
          if Running <= 4.50:
            if Swimming <= 10.50:
              Predict: Duck
            else (Swimming > 10.50):
              if Detective Stories <= 10.50:
                if Gardening <= 6.50:
                  Predict: Duck
                else (Gardening > 6.50):
                  Predict: Cat
              else (Detective Stories > 10.50):
                Predict: Cat
          else (Running > 4.50):
            if Swimming <= 9.50:
              if Gardening <= 4.50:
                Predict: Duck
              else (Gardening > 4.50):
                if Gardening <= 7.50:
                  Predict: Dog
                else (Gardening > 7.50):
                  Predict: Duck
            else (Swimming > 9.50):
              Predict: Duck
        else (Running > 13.50):
          if Gardening <= 4.00:
            Predict: Duck
          else (Gardening > 4.00):
            if Swimming <= 12.50:
              Predict: Dog
            else (Swimming > 12.50):
              Predict: Cat
    else (Gardening > 9.50):
      if Swimming <= 9.50:
        Predict: Dog
      else (Swimming > 9.50):
        if Detective Stories <= 12.00:
          if Swimming <= 12.50:
            Predict: Duck
          else (Swimming > 12.50):
            if Running <= 9.00:
              Predict: Duck
            else (Running > 9.00):
              Predict: Dog
        else (Detective Stories > 12.00):
          Predict: Dog

Tree 1 Rules:
if Art <= 4.50:
  if Running <= 4.50:
    if Art <= 0.50:
      Predict: Cat
    else (Art > 0.50):
      if Swimming <= 5.00:
        if Detective Stories <= 4.00:
          Predict: Dog
        else (Detective Stories > 4.00):
          Predict: Cat
      else (Swimming > 5.00):
        if Running <= 3.00:
          Predict: Dog
        else (Running > 3.00):
          if Art <= 1.50:
            Predict: Dog
          else (Art > 1.50):
            if Art <= 3.00:
              Predict: Duck
            else (Art > 3.00):
              Predict: Dog
  else (Running > 4.50):
    if Swimming <= 3.50:
      Predict: Cat
    else (Swimming > 3.50):
      if Swimming <= 11.50:
        if Gardening <= 4.50:
          if Detective Stories <= 11.00:
            Predict: Duck
          else (Detective Stories > 11.00):
            Predict: Dog
        else (Gardening > 4.50):
          if Running <= 6.50:
            if Gardening <= 10.50:
              Predict: Duck
            else (Gardening > 10.50):
              Predict: Dog
          else (Running > 6.50):
            Predict: Dog
      else (Swimming > 11.50):
        if Detective Stories <= 5.50:
          Predict: Duck
        else (Detective Stories > 5.50):
          if Running <= 10.50:
            if Gardening <= 4.00:
              Predict: Duck
            else (Gardening > 4.00):
              Predict: Dog
          else (Running > 10.50):
            if Gardening <= 4.00:
              Predict: Duck
            else (Gardening > 4.00):
              Predict: Dog
else (Art > 4.50):
  if Swimming <= 4.50:
    if Running <= 8.50:
      if Swimming <= 0.50:
        if Detective Stories <= 6.00:
          Predict: Dog
        else (Detective Stories > 6.00):
          Predict: Cat
      else (Swimming > 0.50):
        Predict: Cat
    else (Running > 8.50):
      if Running <= 10.50:
        if Detective Stories <= 5.50:
          Predict: Cat
        else (Detective Stories > 5.50):
          Predict: Duck
      else (Running > 10.50):
        if Art <= 8.50:
          Predict: Duck
        else (Art > 8.50):
          Predict: Cat
  else (Swimming > 4.50):
    if Gardening <= 5.50:
      if Running <= 13.50:
        if Swimming <= 12.50:
          Predict: Duck
        else (Swimming > 12.50):
          if Running <= 5.00:
            Predict: Cat
          else (Running > 5.00):
            Predict: Duck
      else (Running > 13.50):
        if Detective Stories <= 10.50:
          Predict: Duck
        else (Detective Stories > 10.50):
          if Art <= 8.50:
            Predict: Dog
          else (Art > 8.50):
            Predict: Cat
    else (Gardening > 5.50):
      if Running <= 4.50:
        Predict: Cat
      else (Running > 4.50):
        if Detective Stories <= 2.00:
          Predict: Dog
        else (Detective Stories > 2.00):
          if Running <= 9.50:
            if Swimming <= 10.50:
              if Art <= 5.50:
                Predict: Dog
              else (Art > 5.50):
                Predict: Duck
            else (Swimming > 10.50):
              Predict: Duck
          else (Running > 9.50):
            if Swimming <= 8.50:
              Predict: Dog
            else (Swimming > 8.50):
              if Running <= 10.50:
                Predict: Dog
              else (Running > 10.50):
                if Gardening <= 13.50:
                  if Detective Stories <= 7.50:
                    if Detective Stories <= 4.50:
                      Predict: Duck
                    else (Detective Stories > 4.50):
                      Predict: Dog
                  else (Detective Stories > 7.50):
                    Predict: Duck
                else (Gardening > 13.50):
                  Predict: Dog

Tree 2 Rules:
if Art <= 4.50:
  if Gardening <= 4.50:
    if Swimming <= 4.50:
      if Running <= 8.00:
        Predict: Cat
      else (Running > 8.00):
        Predict: Duck
    else (Swimming > 4.50):
      if Running <= 13.00:
        if Gardening <= 0.50:
          Predict: Duck
        else (Gardening > 0.50):
          if Running <= 8.50:
            Predict: Duck
          else (Running > 8.50):
            if Detective Stories <= 10.50:
              Predict: Dog
            else (Detective Stories > 10.50):
              Predict: Duck
      else (Running > 13.00):
        Predict: Dog
  else (Gardening > 4.50):
    if Swimming <= 13.50:
      if Running <= 6.50:
        if Running <= 5.50:
          Predict: Dog
        else (Running > 5.50):
          if Art <= 2.50:
            Predict: Dog
          else (Art > 2.50):
            if Swimming <= 10.00:
              if Swimming <= 4.00:
                Predict: Dog
              else (Swimming > 4.00):
                Predict: Duck
            else (Swimming > 10.00):
              Predict: Dog
      else (Running > 6.50):
        Predict: Dog
    else (Swimming > 13.50):
      if Running <= 7.00:
        Predict: Duck
      else (Running > 7.00):
        Predict: Dog
else (Art > 4.50):
  if Art <= 9.50:
    if Swimming <= 4.50:
      if Art <= 8.50:
        if Running <= 10.50:
          Predict: Cat
        else (Running > 10.50):
          Predict: Duck
      else (Art > 8.50):
        if Gardening <= 6.00:
          Predict: Duck
        else (Gardening > 6.00):
          Predict: Cat
    else (Swimming > 4.50):
      if Detective Stories <= 6.50:
        if Swimming <= 13.00:
          if Art <= 5.50:
            Predict: Dog
          else (Art > 5.50):
            if Gardening <= 9.50:
              Predict: Duck
            else (Gardening > 9.50):
              if Swimming <= 10.50:
                Predict: Duck
              else (Swimming > 10.50):
                Predict: Dog
        else (Swimming > 13.00):
          Predict: Dog
      else (Detective Stories > 6.50):
        if Running <= 9.50:
          if Gardening <= 10.50:
            Predict: Duck
          else (Gardening > 10.50):
            if Swimming <= 10.50:
              Predict: Dog
            else (Swimming > 10.50):
              Predict: Duck
        else (Running > 9.50):
          if Art <= 6.50:
            if Gardening <= 4.50:
              Predict: Duck
            else (Gardening > 4.50):
              Predict: Dog
          else (Art > 6.50):
            if Gardening <= 9.50:
              if Running <= 10.50:
                if Swimming <= 8.50:
                  Predict: Duck
                else (Swimming > 8.50):
                  Predict: Dog
              else (Running > 10.50):
                Predict: Duck
            else (Gardening > 9.50):
              Predict: Dog
  else (Art > 9.50):
    if Detective Stories <= 2.50:
      if Swimming <= 7.00:
        Predict: Cat
      else (Swimming > 7.00):
        Predict: Duck
    else (Detective Stories > 2.50):
      if Running <= 4.50:
        Predict: Cat
      else (Running > 4.50):
        if Swimming <= 7.50:
          if Gardening <= 11.50:
            if Gardening <= 2.00:
              if Swimming <= 2.50:
                Predict: Duck
              else (Swimming > 2.50):
                Predict: Cat
            else (Gardening > 2.00):
              Predict: Cat
          else (Gardening > 11.50):
            Predict: Dog
        else (Swimming > 7.50):
          if Swimming <= 9.50:
            if Gardening <= 4.50:
              Predict: Duck
            else (Gardening > 4.50):
              if Running <= 12.00:
                Predict: Duck
              else (Running > 12.00):
                Predict: Dog
          else (Swimming > 9.50):
            Predict: Duck

Tree 3 Rules:
if Running <= 4.50:
  if Gardening <= 10.00:
    if Art <= 5.50:
      if Swimming <= 10.50:
        if Gardening <= 4.00:
          Predict: Cat
        else (Gardening > 4.00):
          if Detective Stories <= 11.50:
            Predict: Cat
          else (Detective Stories > 11.50):
            Predict: Dog
      else (Swimming > 10.50):
        if Detective Stories <= 6.00:
          Predict: Dog
        else (Detective Stories > 6.00):
          Predict: Duck
    else (Art > 5.50):
      if Swimming <= 7.00:
        Predict: Cat
      else (Swimming > 7.00):
        if Running <= 2.50:
          if Gardening <= 2.50:
            Predict: Duck
          else (Gardening > 2.50):
            if Swimming <= 12.50:
              if Art <= 10.00:
                Predict: Duck
              else (Art > 10.00):
                Predict: Cat
            else (Swimming > 12.50):
              Predict: Cat
        else (Running > 2.50):
          Predict: Cat
  else (Gardening > 10.00):
    if Detective Stories <= 9.50:
      Predict: Dog
    else (Detective Stories > 9.50):
      if Art <= 5.50:
        Predict: Dog
      else (Art > 5.50):
        Predict: Cat
else (Running > 4.50):
  if Art <= 4.50:
    if Running <= 6.50:
      if Detective Stories <= 3.50:
        Predict: Duck
      else (Detective Stories > 3.50):
        Predict: Dog
    else (Running > 6.50):
      if Swimming <= 13.50:
        Predict: Dog
      else (Swimming > 13.50):
        if Running <= 12.50:
          if Gardening <= 5.50:
            Predict: Duck
          else (Gardening > 5.50):
            Predict: Dog
        else (Running > 12.50):
          Predict: Dog
  else (Art > 4.50):
    if Running <= 10.50:
      if Gardening <= 4.50:
        if Swimming <= 5.00:
          if Art <= 11.50:
            Predict: Duck
          else (Art > 11.50):
            if Running <= 9.50:
              Predict: Cat
            else (Running > 9.50):
              Predict: Duck
        else (Swimming > 5.00):
          Predict: Duck
      else (Gardening > 4.50):
        if Swimming <= 4.00:
          if Gardening <= 8.00:
            Predict: Cat
          else (Gardening > 8.00):
            if Art <= 7.50:
              Predict: Dog
            else (Art > 7.50):
              Predict: Cat
        else (Swimming > 4.00):
          if Art <= 5.50:
            if Running <= 8.50:
              Predict: Dog
            else (Running > 8.50):
              Predict: Duck
          else (Art > 5.50):
            if Detective Stories <= 3.00:
              Predict: Dog
            else (Detective Stories > 3.00):
              Predict: Duck
    else (Running > 10.50):
      if Gardening <= 4.50:
        if Swimming <= 4.50:
          if Gardening <= 3.50:
            Predict: Cat
          else (Gardening > 3.50):
            if Running <= 13.50:
              Predict: Cat
            else (Running > 13.50):
              Predict: Duck
        else (Swimming > 4.50):
          Predict: Duck
      else (Gardening > 4.50):
        if Art <= 9.50:
          if Gardening <= 6.50:
            if Running <= 13.50:
              Predict: Dog
            else (Running > 13.50):
              Predict: Duck
          else (Gardening > 6.50):
            if Detective Stories <= 4.50:
              if Art <= 7.00:
                Predict: Duck
              else (Art > 7.00):
                Predict: Dog
            else (Detective Stories > 4.50):
              Predict: Dog
        else (Art > 9.50):
          if Swimming <= 5.00:
            Predict: Cat
          else (Swimming > 5.00):
            if Detective Stories <= 12.00:
              if Running <= 12.00:
                Predict: Duck
              else (Running > 12.00):
                if Running <= 13.50:
                  Predict: Dog
                else (Running > 13.50):
                  Predict: Cat
            else (Detective Stories > 12.00):
              Predict: Dog

Tree 4 Rules:
if Swimming <= 4.50:
  if Running <= 13.50:
    if Running <= 9.00:
      if Detective Stories <= 5.50:
        if Running <= 4.00:
          Predict: Cat
        else (Running > 4.00):
          Predict: Dog
      else (Detective Stories > 5.50):
        Predict: Cat
    else (Running > 9.00):
      if Running <= 10.50:
        Predict: Duck
      else (Running > 10.50):
        if Art <= 12.00:
          if Art <= 7.50:
            if Gardening <= 3.00:
              Predict: Cat
            else (Gardening > 3.00):
              if Gardening <= 6.50:
                Predict: Duck
              else (Gardening > 6.50):
                Predict: Dog
          else (Art > 7.50):
            Predict: Cat
        else (Art > 12.00):
          if Gardening <= 3.00:
            Predict: Duck
          else (Gardening > 3.00):
            Predict: Cat
  else (Running > 13.50):
    Predict: Duck
else (Swimming > 4.50):
  if Gardening <= 4.50:
    if Running <= 13.50:
      if Running <= 3.50:
        if Detective Stories <= 11.00:
          Predict: Cat
        else (Detective Stories > 11.00):
          Predict: Duck
      else (Running > 3.50):
        Predict: Duck
    else (Running > 13.50):
      if Art <= 5.50:
        Predict: Dog
      else (Art > 5.50):
        Predict: Duck
  else (Gardening > 4.50):
    if Running <= 9.50:
      if Art <= 6.50:
        if Gardening <= 8.50:
          if Art <= 1.50:
            Predict: Dog
          else (Art > 1.50):
            if Art <= 2.50:
              Predict: Duck
            else (Art > 2.50):
              if Running <= 6.50:
                if Gardening <= 6.50:
                  Predict: Dog
                else (Gardening > 6.50):
                  Predict: Duck
              else (Running > 6.50):
                if Swimming <= 10.00:
                  if Gardening <= 7.00:
                    Predict: Dog
                  else (Gardening > 7.00):
                    Predict: Duck
                else (Swimming > 10.00):
                  Predict: Dog
        else (Gardening > 8.50):
          Predict: Dog
      else (Art > 6.50):
        if Running <= 4.50:
          Predict: Cat
        else (Running > 4.50):
          Predict: Duck
    else (Running > 9.50):
      if Art <= 4.50:
        Predict: Dog
      else (Art > 4.50):
        if Swimming <= 9.50:
          Predict: Dog
        else (Swimming > 9.50):
          if Running <= 13.50:
            if Detective Stories <= 12.00:
              if Swimming <= 12.50:
                Predict: Duck
              else (Swimming > 12.50):
                Predict: Dog
            else (Detective Stories > 12.00):
              Predict: Dog
          else (Running > 13.50):
            if Swimming <= 12.50:
              Predict: Duck
            else (Swimming > 12.50):
              Predict: Cat

Tree 5 Rules:
if Running <= 4.50:
  if Art <= 4.50:
    if Gardening <= 5.50:
      Predict: Duck
    else (Gardening > 5.50):
      Predict: Dog
  else (Art > 4.50):
    if Art <= 7.50:
      if Swimming <= 7.00:
        Predict: Cat
      else (Swimming > 7.00):
        Predict: Duck
    else (Art > 7.50):
      Predict: Cat
else (Running > 4.50):
  if Art <= 4.50:
    if Gardening <= 4.50:
      if Running <= 13.50:
        if Running <= 12.50:
          if Running <= 11.00:
            if Swimming <= 11.50:
              Predict: Dog
            else (Swimming > 11.50):
              Predict: Duck
          else (Running > 11.00):
            Predict: Duck
        else (Running > 12.50):
          Predict: Cat
      else (Running > 13.50):
        Predict: Dog
    else (Gardening > 4.50):
      if Detective Stories <= 2.50:
        if Gardening <= 6.50:
          Predict: Duck
        else (Gardening > 6.50):
          Predict: Dog
      else (Detective Stories > 2.50):
        if Running <= 6.50:
          if Swimming <= 8.50:
            if Gardening <= 10.50:
              Predict: Duck
            else (Gardening > 10.50):
              Predict: Dog
          else (Swimming > 8.50):
            Predict: Dog
        else (Running > 6.50):
          Predict: Dog
  else (Art > 4.50):
    if Swimming <= 4.50:
      if Detective Stories <= 6.50:
        if Art <= 9.50:
          Predict: Duck
        else (Art > 9.50):
          Predict: Cat
      else (Detective Stories > 6.50):
        if Art <= 9.50:
          if Running <= 8.00:
            Predict: Cat
          else (Running > 8.00):
            Predict: Duck
        else (Art > 9.50):
          if Swimming <= 1.50:
            if Gardening <= 2.00:
              if Running <= 9.50:
                Predict: Cat
              else (Running > 9.50):
                Predict: Duck
            else (Gardening > 2.00):
              Predict: Cat
          else (Swimming > 1.50):
            Predict: Cat
    else (Swimming > 4.50):
      if Running <= 10.50:
        if Detective Stories <= 5.50:
          if Swimming <= 10.50:
            Predict: Dog
          else (Swimming > 10.50):
            Predict: Duck
        else (Detective Stories > 5.50):
          Predict: Duck
      else (Running > 10.50):
        if Art <= 6.50:
          if Swimming <= 12.50:
            Predict: Dog
          else (Swimming > 12.50):
            Predict: Duck
        else (Art > 6.50):
          if Gardening <= 4.00:
            Predict: Duck
          else (Gardening > 4.00):
            if Detective Stories <= 12.50:
              if Art <= 9.00:
                Predict: Dog
              else (Art > 9.00):
                if Detective Stories <= 11.50:
                  Predict: Cat
                else (Detective Stories > 11.50):
                  Predict: Duck
            else (Detective Stories > 12.50):
              Predict: Dog

Tree 6 Rules:
if Swimming <= 4.50:
  if Running <= 13.50:
    if Running <= 5.50:
      if Detective Stories <= 2.50:
        if Running <= 1.50:
          Predict: Cat
        else (Running > 1.50):
          Predict: Dog
      else (Detective Stories > 2.50):
        Predict: Cat
    else (Running > 5.50):
      if Art <= 6.50:
        if Gardening <= 6.50:
          if Art <= 2.00:
            Predict: Cat
          else (Art > 2.00):
            Predict: Duck
        else (Gardening > 6.50):
          Predict: Dog
      else (Art > 6.50):
        if Art <= 9.50:
          if Swimming <= 2.00:
            Predict: Duck
          else (Swimming > 2.00):
            Predict: Cat
        else (Art > 9.50):
          if Running <= 9.50:
            Predict: Cat
          else (Running > 9.50):
            if Gardening <= 1.00:
              Predict: Duck
            else (Gardening > 1.00):
              if Running <= 10.50:
                Predict: Duck
              else (Running > 10.50):
                Predict: Cat
  else (Running > 13.50):
    if Swimming <= 1.50:
      Predict: Dog
    else (Swimming > 1.50):
      Predict: Duck
else (Swimming > 4.50):
  if Art <= 4.50:
    if Gardening <= 2.50:
      if Art <= 3.50:
        if Gardening <= 0.50:
          Predict: Duck
        else (Gardening > 0.50):
          if Running <= 8.50:
            Predict: Duck
          else (Running > 8.50):
            Predict: Dog
      else (Art > 3.50):
        if Swimming <= 11.00:
          Predict: Dog
        else (Swimming > 11.00):
          Predict: Duck
    else (Gardening > 2.50):
      if Detective Stories <= 3.50:
        if Gardening <= 6.50:
          Predict: Duck
        else (Gardening > 6.50):
          Predict: Dog
      else (Detective Stories > 3.50):
        if Swimming <= 8.50:
          if Swimming <= 7.50:
            Predict: Dog
          else (Swimming > 7.50):
            if Running <= 8.00:
              Predict: Duck
            else (Running > 8.00):
              Predict: Dog
        else (Swimming > 8.50):
          Predict: Dog
  else (Art > 4.50):
    if Detective Stories <= 1.50:
      if Art <= 9.00:
        Predict: Dog
      else (Art > 9.00):
        Predict: Duck
    else (Detective Stories > 1.50):
      if Running <= 10.50:
        if Gardening <= 10.50:
          if Art <= 9.50:
            if Detective Stories <= 5.50:
              if Running <= 7.50:
                Predict: Duck
              else (Running > 7.50):
                Predict: Dog
            else (Detective Stories > 5.50):
              if Swimming <= 13.50:
                if Running <= 9.50:
                  Predict: Duck
                else (Running > 9.50):
                  if Gardening <= 4.00:
                    Predict: Duck
                  else (Gardening > 4.00):
                    Predict: Dog
              else (Swimming > 13.50):
                if Detective Stories <= 11.50:
                  Predict: Duck
                else (Detective Stories > 11.50):
                  Predict: Dog
          else (Art > 9.50):
            if Running <= 4.00:
              Predict: Cat
            else (Running > 4.00):
              Predict: Duck
        else (Gardening > 10.50):
          Predict: Dog
      else (Running > 10.50):
        if Swimming <= 8.50:
          if Gardening <= 2.00:
            Predict: Duck
          else (Gardening > 2.00):
            Predict: Dog
        else (Swimming > 8.50):
          if Running <= 13.50:
            if Swimming <= 13.50:
              if Detective Stories <= 12.50:
                if Gardening <= 13.50:
                  if Running <= 11.50:
                    if Art <= 8.00:
                      Predict: Dog
                    else (Art > 8.00):
                      Predict: Duck
                  else (Running > 11.50):
                    Predict: Duck
                else (Gardening > 13.50):
                  Predict: Dog
              else (Detective Stories > 12.50):
                Predict: Dog
            else (Swimming > 13.50):
              if Gardening <= 5.50:
                Predict: Duck
              else (Gardening > 5.50):
                Predict: Dog
          else (Running > 13.50):
            Predict: Duck

Tree 7 Rules:
if Running <= 4.50:
  if Art <= 5.50:
    if Swimming <= 10.00:
      if Art <= 0.50:
        Predict: Cat
      else (Art > 0.50):
        Predict: Dog
    else (Swimming > 10.00):
      Predict: Duck
  else (Art > 5.50):
    if Art <= 9.50:
      if Swimming <= 7.00:
        Predict: Cat
      else (Swimming > 7.00):
        Predict: Duck
    else (Art > 9.50):
      Predict: Cat
else (Running > 4.50):
  if Swimming <= 9.50:
    if Art <= 4.50:
      if Swimming <= 7.50:
        if Gardening <= 3.00:
          Predict: Duck
        else (Gardening > 3.00):
          Predict: Dog
      else (Swimming > 7.50):
        Predict: Dog
    else (Art > 4.50):
      if Gardening <= 9.50:
        if Swimming <= 4.50:
          if Detective Stories <= 7.50:
            Predict: Duck
          else (Detective Stories > 7.50):
            if Running <= 13.50:
              if Art <= 9.50:
                Predict: Duck
              else (Art > 9.50):
                Predict: Cat
            else (Running > 13.50):
              Predict: Duck
        else (Swimming > 4.50):
          if Gardening <= 5.00:
            Predict: Duck
          else (Gardening > 5.00):
            if Art <= 10.00:
              if Gardening <= 7.50:
                Predict: Dog
              else (Gardening > 7.50):
                if Gardening <= 8.50:
                  Predict: Duck
                else (Gardening > 8.50):
                  if Art <= 6.50:
                    Predict: Dog
                  else (Art > 6.50):
                    Predict: Duck
            else (Art > 10.00):
              Predict: Dog
      else (Gardening > 9.50):
        Predict: Dog
  else (Swimming > 9.50):
    if Gardening <= 4.50:
      Predict: Duck
    else (Gardening > 4.50):
      if Art <= 4.50:
        Predict: Dog
      else (Art > 4.50):
        if Gardening <= 8.50:
          if Swimming <= 12.50:
            Predict: Duck
          else (Swimming > 12.50):
            if Running <= 13.50:
              if Detective Stories <= 5.50:
                Predict: Dog
              else (Detective Stories > 5.50):
                Predict: Duck
            else (Running > 13.50):
              Predict: Cat
        else (Gardening > 8.50):
          if Swimming <= 11.00:
            if Art <= 8.50:
              Predict: Dog
            else (Art > 8.50):
              Predict: Duck
          else (Swimming > 11.00):
            if Gardening <= 13.50:
              Predict: Dog
            else (Gardening > 13.50):
              if Running <= 11.00:
                Predict: Duck
              else (Running > 11.00):
                Predict: Dog

Tree 8 Rules:
if Swimming <= 4.50:
  if Gardening <= 8.50:
    if Gardening <= 2.50:
      if Detective Stories <= 11.50:
        if Art <= 10.50:
          Predict: Cat
        else (Art > 10.50):
          if Art <= 11.50:
            if Gardening <= 0.50:
              if Running <= 6.00:
                Predict: Cat
              else (Running > 6.00):
                Predict: Duck
            else (Gardening > 0.50):
              Predict: Cat
          else (Art > 11.50):
            Predict: Cat
      else (Detective Stories > 11.50):
        if Running <= 6.50:
          Predict: Cat
        else (Running > 6.50):
          Predict: Duck
    else (Gardening > 2.50):
      if Detective Stories <= 6.50:
        if Running <= 8.00:
          Predict: Cat
        else (Running > 8.00):
          Predict: Duck
      else (Detective Stories > 6.50):
        if Detective Stories <= 10.50:
          if Gardening <= 3.50:
            if Running <= 7.00:
              Predict: Cat
            else (Running > 7.00):
              Predict: Duck
          else (Gardening > 3.50):
            Predict: Cat
        else (Detective Stories > 10.50):
          Predict: Cat
  else (Gardening > 8.50):
    if Running <= 5.00:
      Predict: Cat
    else (Running > 5.00):
      if Art <= 7.50:
        Predict: Dog
      else (Art > 7.50):
        Predict: Cat
else (Swimming > 4.50):
  if Running <= 9.50:
    if Art <= 5.50:
      if Art <= 1.50:
        if Art <= 0.50:
          if Running <= 3.50:
            Predict: Duck
          else (Running > 3.50):
            Predict: Dog
        else (Art > 0.50):
          Predict: Dog
      else (Art > 1.50):
        if Detective Stories <= 4.50:
          Predict: Duck
        else (Detective Stories > 4.50):
          if Gardening <= 4.50:
            Predict: Duck
          else (Gardening > 4.50):
            if Gardening <= 9.50:
              if Detective Stories <= 11.00:
                Predict: Dog
              else (Detective Stories > 11.00):
                if Swimming <= 10.50:
                  Predict: Duck
                else (Swimming > 10.50):
                  Predict: Dog
            else (Gardening > 9.50):
              Predict: Dog
    else (Art > 5.50):
      Predict: Duck
  else (Running > 9.50):
    if Swimming <= 9.50:
      if Detective Stories <= 12.50:
        if Detective Stories <= 10.50:
          if Running <= 13.50:
            if Art <= 9.00:
              Predict: Dog
            else (Art > 9.00):
              if Gardening <= 8.00:
                Predict: Dog
              else (Gardening > 8.00):
                Predict: Duck
          else (Running > 13.50):
            if Art <= 10.50:
              Predict: Dog
            else (Art > 10.50):
              Predict: Duck
        else (Detective Stories > 10.50):
          Predict: Dog
      else (Detective Stories > 12.50):
        if Gardening <= 4.50:
          Predict: Duck
        else (Gardening > 4.50):
          Predict: Dog
    else (Swimming > 9.50):
      if Gardening <= 6.50:
        if Running <= 10.50:
          if Swimming <= 13.00:
            Predict: Duck
          else (Swimming > 13.00):
            Predict: Dog
        else (Running > 10.50):
          if Running <= 12.50:
            Predict: Duck
          else (Running > 12.50):
            if Art <= 4.50:
              Predict: Dog
            else (Art > 4.50):
              if Gardening <= 4.00:
                Predict: Duck
              else (Gardening > 4.00):
                if Detective Stories <= 8.50:
                  Predict: Dog
                else (Detective Stories > 8.50):
                  if Running <= 13.50:
                    Predict: Duck
                  else (Running > 13.50):
                    if Swimming <= 12.50:
                      Predict: Duck
                    else (Swimming > 12.50):
                      Predict: Cat
      else (Gardening > 6.50):
        if Detective Stories <= 4.50:
          if Running <= 11.50:
            Predict: Dog
          else (Running > 11.50):
            Predict: Duck
        else (Detective Stories > 4.50):
          if Gardening <= 13.50:
            if Art <= 9.00:
              Predict: Dog
            else (Art > 9.00):
              if Art <= 12.50:
                Predict: Duck
              else (Art > 12.50):
                Predict: Dog
          else (Gardening > 13.50):
            if Swimming <= 12.50:
              Predict: Duck
            else (Swimming > 12.50):
              Predict: Dog

Tree 9 Rules:
if Running <= 4.50:
  if Running <= 3.50:
    if Art <= 0.50:
      Predict: Duck
    else (Art > 0.50):
      if Swimming <= 5.00:
        Predict: Cat
      else (Swimming > 5.00):
        if Swimming <= 10.50:
          if Detective Stories <= 9.50:
            Predict: Dog
          else (Detective Stories > 9.50):
            Predict: Duck
        else (Swimming > 10.50):
          Predict: Cat
  else (Running > 3.50):
    if Gardening <= 4.50:
      Predict: Cat
    else (Gardening > 4.50):
      if Detective Stories <= 11.00:
        if Art <= 5.50:
          Predict: Duck
        else (Art > 5.50):
          Predict: Cat
      else (Detective Stories > 11.00):
        Predict: Cat
else (Running > 4.50):
  if Art <= 4.50:
    if Running <= 9.00:
      if Art <= 1.50:
        Predict: Dog
      else (Art > 1.50):
        if Gardening <= 9.00:
          if Swimming <= 6.50:
            if Art <= 3.50:
              Predict: Duck
            else (Art > 3.50):
              Predict: Dog
          else (Swimming > 6.50):
            Predict: Duck
        else (Gardening > 9.00):
          Predict: Dog
    else (Running > 9.00):
      if Gardening <= 3.50:
        if Swimming <= 11.50:
          if Running <= 11.50:
            Predict: Dog
          else (Running > 11.50):
            Predict: Cat
        else (Swimming > 11.50):
          Predict: Duck
      else (Gardening > 3.50):
        Predict: Dog
  else (Art > 4.50):
    if Swimming <= 4.50:
      if Art <= 9.50:
        if Art <= 8.50:
          if Detective Stories <= 8.50:
            Predict: Dog
          else (Detective Stories > 8.50):
            Predict: Cat
        else (Art > 8.50):
          Predict: Duck
      else (Art > 9.50):
        if Running <= 9.00:
          Predict: Cat
        else (Running > 9.00):
          if Gardening <= 1.00:
            Predict: Duck
          else (Gardening > 1.00):
            Predict: Cat
    else (Swimming > 4.50):
      if Detective Stories <= 1.50:
        Predict: Dog
      else (Detective Stories > 1.50):
        if Gardening <= 4.50:
          if Art <= 7.50:
            if Gardening <= 3.50:
              Predict: Duck
            else (Gardening > 3.50):
              if Running <= 10.50:
                Predict: Duck
              else (Running > 10.50):
                Predict: Dog
          else (Art > 7.50):
            Predict: Duck
        else (Gardening > 4.50):
          if Running <= 9.50:
            if Detective Stories <= 11.50:
              Predict: Duck
            else (Detective Stories > 11.50):
              if Gardening <= 8.50:
                Predict: Duck
              else (Gardening > 8.50):
                if Detective Stories <= 12.50:
                  Predict: Dog
                else (Detective Stories > 12.50):
                  Predict: Duck
          else (Running > 9.50):
            if Art <= 5.50:
              Predict: Duck
            else (Art > 5.50):
              if Detective Stories <= 4.50:
                Predict: Duck
              else (Detective Stories > 4.50):
                if Swimming <= 9.50:
                  Predict: Dog
                else (Swimming > 9.50):
                  if Detective Stories <= 10.50:
                    Predict: Dog
                  else (Detective Stories > 10.50):
                    if Detective Stories <= 12.50:
                      Predict: Duck
                    else (Detective Stories > 12.50):
                      Predict: Dog

Aggregated Rules (Across Forest):
Swimming > 4.50 appeared in 56.0% of paths.

Class: Duck

Simple Rules (Single Trees):

Tree 0 Rules:
if Swimming <= 4.50:
  if Detective Stories <= 2.50:
    if Swimming <= 2.50:
      Predict: Dog
    else (Swimming > 2.50):
      Predict: Cat
  else (Detective Stories > 2.50):
    if Running <= 8.50:
      if Swimming <= 0.50:
        if Detective Stories <= 6.00:
          Predict: Dog
        else (Detective Stories > 6.00):
          Predict: Cat
      else (Swimming > 0.50):
        Predict: Cat
    else (Running > 8.50):
      if Detective Stories <= 8.00:
        Predict: Dog
      else (Detective Stories > 8.00):
        if Gardening <= 1.50:
          if Gardening <= 0.50:
            if Art <= 12.00:
              Predict: Duck
            else (Art > 12.00):
              Predict: Cat
          else (Gardening > 0.50):
            Predict: Duck
        else (Gardening > 1.50):
          if Gardening <= 3.50:
            if Swimming <= 2.00:
              Predict: Duck
            else (Swimming > 2.00):
              if Detective Stories <= 11.00:
                if Detective Stories <= 9.50:
                  Predict: Cat
                else (Detective Stories > 9.50):
                  Predict: Duck
              else (Detective Stories > 11.00):
                Predict: Cat
          else (Gardening > 3.50):
            Predict: Cat
else (Swimming > 4.50):
  if Art <= 4.50:
    if Running <= 6.50:
      if Gardening <= 6.50:
        Predict: Duck
      else (Gardening > 6.50):
        if Detective Stories <= 9.00:
          Predict: Dog
        else (Detective Stories > 9.00):
          if Detective Stories <= 13.00:
            if Gardening <= 11.00:
              Predict: Duck
            else (Gardening > 11.00):
              Predict: Dog
          else (Detective Stories > 13.00):
            Predict: Dog
    else (Running > 6.50):
      if Gardening <= 3.50:
        Predict: Duck
      else (Gardening > 3.50):
        Predict: Dog
  else (Art > 4.50):
    if Gardening <= 9.50:
      if Swimming <= 7.50:
        if Detective Stories <= 8.00:
          Predict: Duck
        else (Detective Stories > 8.00):
          if Art <= 6.50:
            Predict: Dog
          else (Art > 6.50):
            if Art <= 13.50:
              if Gardening <= 4.50:
                Predict: Duck
              else (Gardening > 4.50):
                if Swimming <= 6.50:
                  if Art <= 10.00:
                    Predict: Duck
                  else (Art > 10.00):
                    Predict: Dog
                else (Swimming > 6.50):
                  Predict: Dog
            else (Art > 13.50):
              Predict: Duck
      else (Swimming > 7.50):
        if Running <= 13.50:
          if Running <= 4.50:
            if Swimming <= 10.50:
              Predict: Duck
            else (Swimming > 10.50):
              if Detective Stories <= 10.50:
                if Gardening <= 6.50:
                  Predict: Duck
                else (Gardening > 6.50):
                  Predict: Cat
              else (Detective Stories > 10.50):
                Predict: Cat
          else (Running > 4.50):
            if Swimming <= 9.50:
              if Gardening <= 4.50:
                Predict: Duck
              else (Gardening > 4.50):
                if Gardening <= 7.50:
                  Predict: Dog
                else (Gardening > 7.50):
                  Predict: Duck
            else (Swimming > 9.50):
              Predict: Duck
        else (Running > 13.50):
          if Gardening <= 4.00:
            Predict: Duck
          else (Gardening > 4.00):
            if Swimming <= 12.50:
              Predict: Dog
            else (Swimming > 12.50):
              Predict: Cat
    else (Gardening > 9.50):
      if Swimming <= 9.50:
        Predict: Dog
      else (Swimming > 9.50):
        if Detective Stories <= 12.00:
          if Swimming <= 12.50:
            Predict: Duck
          else (Swimming > 12.50):
            if Running <= 9.00:
              Predict: Duck
            else (Running > 9.00):
              Predict: Dog
        else (Detective Stories > 12.00):
          Predict: Dog

Tree 1 Rules:
if Art <= 4.50:
  if Running <= 4.50:
    if Art <= 0.50:
      Predict: Cat
    else (Art > 0.50):
      if Swimming <= 5.00:
        if Detective Stories <= 4.00:
          Predict: Dog
        else (Detective Stories > 4.00):
          Predict: Cat
      else (Swimming > 5.00):
        if Running <= 3.00:
          Predict: Dog
        else (Running > 3.00):
          if Art <= 1.50:
            Predict: Dog
          else (Art > 1.50):
            if Art <= 3.00:
              Predict: Duck
            else (Art > 3.00):
              Predict: Dog
  else (Running > 4.50):
    if Swimming <= 3.50:
      Predict: Cat
    else (Swimming > 3.50):
      if Swimming <= 11.50:
        if Gardening <= 4.50:
          if Detective Stories <= 11.00:
            Predict: Duck
          else (Detective Stories > 11.00):
            Predict: Dog
        else (Gardening > 4.50):
          if Running <= 6.50:
            if Gardening <= 10.50:
              Predict: Duck
            else (Gardening > 10.50):
              Predict: Dog
          else (Running > 6.50):
            Predict: Dog
      else (Swimming > 11.50):
        if Detective Stories <= 5.50:
          Predict: Duck
        else (Detective Stories > 5.50):
          if Running <= 10.50:
            if Gardening <= 4.00:
              Predict: Duck
            else (Gardening > 4.00):
              Predict: Dog
          else (Running > 10.50):
            if Gardening <= 4.00:
              Predict: Duck
            else (Gardening > 4.00):
              Predict: Dog
else (Art > 4.50):
  if Swimming <= 4.50:
    if Running <= 8.50:
      if Swimming <= 0.50:
        if Detective Stories <= 6.00:
          Predict: Dog
        else (Detective Stories > 6.00):
          Predict: Cat
      else (Swimming > 0.50):
        Predict: Cat
    else (Running > 8.50):
      if Running <= 10.50:
        if Detective Stories <= 5.50:
          Predict: Cat
        else (Detective Stories > 5.50):
          Predict: Duck
      else (Running > 10.50):
        if Art <= 8.50:
          Predict: Duck
        else (Art > 8.50):
          Predict: Cat
  else (Swimming > 4.50):
    if Gardening <= 5.50:
      if Running <= 13.50:
        if Swimming <= 12.50:
          Predict: Duck
        else (Swimming > 12.50):
          if Running <= 5.00:
            Predict: Cat
          else (Running > 5.00):
            Predict: Duck
      else (Running > 13.50):
        if Detective Stories <= 10.50:
          Predict: Duck
        else (Detective Stories > 10.50):
          if Art <= 8.50:
            Predict: Dog
          else (Art > 8.50):
            Predict: Cat
    else (Gardening > 5.50):
      if Running <= 4.50:
        Predict: Cat
      else (Running > 4.50):
        if Detective Stories <= 2.00:
          Predict: Dog
        else (Detective Stories > 2.00):
          if Running <= 9.50:
            if Swimming <= 10.50:
              if Art <= 5.50:
                Predict: Dog
              else (Art > 5.50):
                Predict: Duck
            else (Swimming > 10.50):
              Predict: Duck
          else (Running > 9.50):
            if Swimming <= 8.50:
              Predict: Dog
            else (Swimming > 8.50):
              if Running <= 10.50:
                Predict: Dog
              else (Running > 10.50):
                if Gardening <= 13.50:
                  if Detective Stories <= 7.50:
                    if Detective Stories <= 4.50:
                      Predict: Duck
                    else (Detective Stories > 4.50):
                      Predict: Dog
                  else (Detective Stories > 7.50):
                    Predict: Duck
                else (Gardening > 13.50):
                  Predict: Dog

Tree 2 Rules:
if Art <= 4.50:
  if Gardening <= 4.50:
    if Swimming <= 4.50:
      if Running <= 8.00:
        Predict: Cat
      else (Running > 8.00):
        Predict: Duck
    else (Swimming > 4.50):
      if Running <= 13.00:
        if Gardening <= 0.50:
          Predict: Duck
        else (Gardening > 0.50):
          if Running <= 8.50:
            Predict: Duck
          else (Running > 8.50):
            if Detective Stories <= 10.50:
              Predict: Dog
            else (Detective Stories > 10.50):
              Predict: Duck
      else (Running > 13.00):
        Predict: Dog
  else (Gardening > 4.50):
    if Swimming <= 13.50:
      if Running <= 6.50:
        if Running <= 5.50:
          Predict: Dog
        else (Running > 5.50):
          if Art <= 2.50:
            Predict: Dog
          else (Art > 2.50):
            if Swimming <= 10.00:
              if Swimming <= 4.00:
                Predict: Dog
              else (Swimming > 4.00):
                Predict: Duck
            else (Swimming > 10.00):
              Predict: Dog
      else (Running > 6.50):
        Predict: Dog
    else (Swimming > 13.50):
      if Running <= 7.00:
        Predict: Duck
      else (Running > 7.00):
        Predict: Dog
else (Art > 4.50):
  if Art <= 9.50:
    if Swimming <= 4.50:
      if Art <= 8.50:
        if Running <= 10.50:
          Predict: Cat
        else (Running > 10.50):
          Predict: Duck
      else (Art > 8.50):
        if Gardening <= 6.00:
          Predict: Duck
        else (Gardening > 6.00):
          Predict: Cat
    else (Swimming > 4.50):
      if Detective Stories <= 6.50:
        if Swimming <= 13.00:
          if Art <= 5.50:
            Predict: Dog
          else (Art > 5.50):
            if Gardening <= 9.50:
              Predict: Duck
            else (Gardening > 9.50):
              if Swimming <= 10.50:
                Predict: Duck
              else (Swimming > 10.50):
                Predict: Dog
        else (Swimming > 13.00):
          Predict: Dog
      else (Detective Stories > 6.50):
        if Running <= 9.50:
          if Gardening <= 10.50:
            Predict: Duck
          else (Gardening > 10.50):
            if Swimming <= 10.50:
              Predict: Dog
            else (Swimming > 10.50):
              Predict: Duck
        else (Running > 9.50):
          if Art <= 6.50:
            if Gardening <= 4.50:
              Predict: Duck
            else (Gardening > 4.50):
              Predict: Dog
          else (Art > 6.50):
            if Gardening <= 9.50:
              if Running <= 10.50:
                if Swimming <= 8.50:
                  Predict: Duck
                else (Swimming > 8.50):
                  Predict: Dog
              else (Running > 10.50):
                Predict: Duck
            else (Gardening > 9.50):
              Predict: Dog
  else (Art > 9.50):
    if Detective Stories <= 2.50:
      if Swimming <= 7.00:
        Predict: Cat
      else (Swimming > 7.00):
        Predict: Duck
    else (Detective Stories > 2.50):
      if Running <= 4.50:
        Predict: Cat
      else (Running > 4.50):
        if Swimming <= 7.50:
          if Gardening <= 11.50:
            if Gardening <= 2.00:
              if Swimming <= 2.50:
                Predict: Duck
              else (Swimming > 2.50):
                Predict: Cat
            else (Gardening > 2.00):
              Predict: Cat
          else (Gardening > 11.50):
            Predict: Dog
        else (Swimming > 7.50):
          if Swimming <= 9.50:
            if Gardening <= 4.50:
              Predict: Duck
            else (Gardening > 4.50):
              if Running <= 12.00:
                Predict: Duck
              else (Running > 12.00):
                Predict: Dog
          else (Swimming > 9.50):
            Predict: Duck

Tree 3 Rules:
if Running <= 4.50:
  if Gardening <= 10.00:
    if Art <= 5.50:
      if Swimming <= 10.50:
        if Gardening <= 4.00:
          Predict: Cat
        else (Gardening > 4.00):
          if Detective Stories <= 11.50:
            Predict: Cat
          else (Detective Stories > 11.50):
            Predict: Dog
      else (Swimming > 10.50):
        if Detective Stories <= 6.00:
          Predict: Dog
        else (Detective Stories > 6.00):
          Predict: Duck
    else (Art > 5.50):
      if Swimming <= 7.00:
        Predict: Cat
      else (Swimming > 7.00):
        if Running <= 2.50:
          if Gardening <= 2.50:
            Predict: Duck
          else (Gardening > 2.50):
            if Swimming <= 12.50:
              if Art <= 10.00:
                Predict: Duck
              else (Art > 10.00):
                Predict: Cat
            else (Swimming > 12.50):
              Predict: Cat
        else (Running > 2.50):
          Predict: Cat
  else (Gardening > 10.00):
    if Detective Stories <= 9.50:
      Predict: Dog
    else (Detective Stories > 9.50):
      if Art <= 5.50:
        Predict: Dog
      else (Art > 5.50):
        Predict: Cat
else (Running > 4.50):
  if Art <= 4.50:
    if Running <= 6.50:
      if Detective Stories <= 3.50:
        Predict: Duck
      else (Detective Stories > 3.50):
        Predict: Dog
    else (Running > 6.50):
      if Swimming <= 13.50:
        Predict: Dog
      else (Swimming > 13.50):
        if Running <= 12.50:
          if Gardening <= 5.50:
            Predict: Duck
          else (Gardening > 5.50):
            Predict: Dog
        else (Running > 12.50):
          Predict: Dog
  else (Art > 4.50):
    if Running <= 10.50:
      if Gardening <= 4.50:
        if Swimming <= 5.00:
          if Art <= 11.50:
            Predict: Duck
          else (Art > 11.50):
            if Running <= 9.50:
              Predict: Cat
            else (Running > 9.50):
              Predict: Duck
        else (Swimming > 5.00):
          Predict: Duck
      else (Gardening > 4.50):
        if Swimming <= 4.00:
          if Gardening <= 8.00:
            Predict: Cat
          else (Gardening > 8.00):
            if Art <= 7.50:
              Predict: Dog
            else (Art > 7.50):
              Predict: Cat
        else (Swimming > 4.00):
          if Art <= 5.50:
            if Running <= 8.50:
              Predict: Dog
            else (Running > 8.50):
              Predict: Duck
          else (Art > 5.50):
            if Detective Stories <= 3.00:
              Predict: Dog
            else (Detective Stories > 3.00):
              Predict: Duck
    else (Running > 10.50):
      if Gardening <= 4.50:
        if Swimming <= 4.50:
          if Gardening <= 3.50:
            Predict: Cat
          else (Gardening > 3.50):
            if Running <= 13.50:
              Predict: Cat
            else (Running > 13.50):
              Predict: Duck
        else (Swimming > 4.50):
          Predict: Duck
      else (Gardening > 4.50):
        if Art <= 9.50:
          if Gardening <= 6.50:
            if Running <= 13.50:
              Predict: Dog
            else (Running > 13.50):
              Predict: Duck
          else (Gardening > 6.50):
            if Detective Stories <= 4.50:
              if Art <= 7.00:
                Predict: Duck
              else (Art > 7.00):
                Predict: Dog
            else (Detective Stories > 4.50):
              Predict: Dog
        else (Art > 9.50):
          if Swimming <= 5.00:
            Predict: Cat
          else (Swimming > 5.00):
            if Detective Stories <= 12.00:
              if Running <= 12.00:
                Predict: Duck
              else (Running > 12.00):
                if Running <= 13.50:
                  Predict: Dog
                else (Running > 13.50):
                  Predict: Cat
            else (Detective Stories > 12.00):
              Predict: Dog

Tree 4 Rules:
if Swimming <= 4.50:
  if Running <= 13.50:
    if Running <= 9.00:
      if Detective Stories <= 5.50:
        if Running <= 4.00:
          Predict: Cat
        else (Running > 4.00):
          Predict: Dog
      else (Detective Stories > 5.50):
        Predict: Cat
    else (Running > 9.00):
      if Running <= 10.50:
        Predict: Duck
      else (Running > 10.50):
        if Art <= 12.00:
          if Art <= 7.50:
            if Gardening <= 3.00:
              Predict: Cat
            else (Gardening > 3.00):
              if Gardening <= 6.50:
                Predict: Duck
              else (Gardening > 6.50):
                Predict: Dog
          else (Art > 7.50):
            Predict: Cat
        else (Art > 12.00):
          if Gardening <= 3.00:
            Predict: Duck
          else (Gardening > 3.00):
            Predict: Cat
  else (Running > 13.50):
    Predict: Duck
else (Swimming > 4.50):
  if Gardening <= 4.50:
    if Running <= 13.50:
      if Running <= 3.50:
        if Detective Stories <= 11.00:
          Predict: Cat
        else (Detective Stories > 11.00):
          Predict: Duck
      else (Running > 3.50):
        Predict: Duck
    else (Running > 13.50):
      if Art <= 5.50:
        Predict: Dog
      else (Art > 5.50):
        Predict: Duck
  else (Gardening > 4.50):
    if Running <= 9.50:
      if Art <= 6.50:
        if Gardening <= 8.50:
          if Art <= 1.50:
            Predict: Dog
          else (Art > 1.50):
            if Art <= 2.50:
              Predict: Duck
            else (Art > 2.50):
              if Running <= 6.50:
                if Gardening <= 6.50:
                  Predict: Dog
                else (Gardening > 6.50):
                  Predict: Duck
              else (Running > 6.50):
                if Swimming <= 10.00:
                  if Gardening <= 7.00:
                    Predict: Dog
                  else (Gardening > 7.00):
                    Predict: Duck
                else (Swimming > 10.00):
                  Predict: Dog
        else (Gardening > 8.50):
          Predict: Dog
      else (Art > 6.50):
        if Running <= 4.50:
          Predict: Cat
        else (Running > 4.50):
          Predict: Duck
    else (Running > 9.50):
      if Art <= 4.50:
        Predict: Dog
      else (Art > 4.50):
        if Swimming <= 9.50:
          Predict: Dog
        else (Swimming > 9.50):
          if Running <= 13.50:
            if Detective Stories <= 12.00:
              if Swimming <= 12.50:
                Predict: Duck
              else (Swimming > 12.50):
                Predict: Dog
            else (Detective Stories > 12.00):
              Predict: Dog
          else (Running > 13.50):
            if Swimming <= 12.50:
              Predict: Duck
            else (Swimming > 12.50):
              Predict: Cat

Tree 5 Rules:
if Running <= 4.50:
  if Art <= 4.50:
    if Gardening <= 5.50:
      Predict: Duck
    else (Gardening > 5.50):
      Predict: Dog
  else (Art > 4.50):
    if Art <= 7.50:
      if Swimming <= 7.00:
        Predict: Cat
      else (Swimming > 7.00):
        Predict: Duck
    else (Art > 7.50):
      Predict: Cat
else (Running > 4.50):
  if Art <= 4.50:
    if Gardening <= 4.50:
      if Running <= 13.50:
        if Running <= 12.50:
          if Running <= 11.00:
            if Swimming <= 11.50:
              Predict: Dog
            else (Swimming > 11.50):
              Predict: Duck
          else (Running > 11.00):
            Predict: Duck
        else (Running > 12.50):
          Predict: Cat
      else (Running > 13.50):
        Predict: Dog
    else (Gardening > 4.50):
      if Detective Stories <= 2.50:
        if Gardening <= 6.50:
          Predict: Duck
        else (Gardening > 6.50):
          Predict: Dog
      else (Detective Stories > 2.50):
        if Running <= 6.50:
          if Swimming <= 8.50:
            if Gardening <= 10.50:
              Predict: Duck
            else (Gardening > 10.50):
              Predict: Dog
          else (Swimming > 8.50):
            Predict: Dog
        else (Running > 6.50):
          Predict: Dog
  else (Art > 4.50):
    if Swimming <= 4.50:
      if Detective Stories <= 6.50:
        if Art <= 9.50:
          Predict: Duck
        else (Art > 9.50):
          Predict: Cat
      else (Detective Stories > 6.50):
        if Art <= 9.50:
          if Running <= 8.00:
            Predict: Cat
          else (Running > 8.00):
            Predict: Duck
        else (Art > 9.50):
          if Swimming <= 1.50:
            if Gardening <= 2.00:
              if Running <= 9.50:
                Predict: Cat
              else (Running > 9.50):
                Predict: Duck
            else (Gardening > 2.00):
              Predict: Cat
          else (Swimming > 1.50):
            Predict: Cat
    else (Swimming > 4.50):
      if Running <= 10.50:
        if Detective Stories <= 5.50:
          if Swimming <= 10.50:
            Predict: Dog
          else (Swimming > 10.50):
            Predict: Duck
        else (Detective Stories > 5.50):
          Predict: Duck
      else (Running > 10.50):
        if Art <= 6.50:
          if Swimming <= 12.50:
            Predict: Dog
          else (Swimming > 12.50):
            Predict: Duck
        else (Art > 6.50):
          if Gardening <= 4.00:
            Predict: Duck
          else (Gardening > 4.00):
            if Detective Stories <= 12.50:
              if Art <= 9.00:
                Predict: Dog
              else (Art > 9.00):
                if Detective Stories <= 11.50:
                  Predict: Cat
                else (Detective Stories > 11.50):
                  Predict: Duck
            else (Detective Stories > 12.50):
              Predict: Dog

Tree 6 Rules:
if Swimming <= 4.50:
  if Running <= 13.50:
    if Running <= 5.50:
      if Detective Stories <= 2.50:
        if Running <= 1.50:
          Predict: Cat
        else (Running > 1.50):
          Predict: Dog
      else (Detective Stories > 2.50):
        Predict: Cat
    else (Running > 5.50):
      if Art <= 6.50:
        if Gardening <= 6.50:
          if Art <= 2.00:
            Predict: Cat
          else (Art > 2.00):
            Predict: Duck
        else (Gardening > 6.50):
          Predict: Dog
      else (Art > 6.50):
        if Art <= 9.50:
          if Swimming <= 2.00:
            Predict: Duck
          else (Swimming > 2.00):
            Predict: Cat
        else (Art > 9.50):
          if Running <= 9.50:
            Predict: Cat
          else (Running > 9.50):
            if Gardening <= 1.00:
              Predict: Duck
            else (Gardening > 1.00):
              if Running <= 10.50:
                Predict: Duck
              else (Running > 10.50):
                Predict: Cat
  else (Running > 13.50):
    if Swimming <= 1.50:
      Predict: Dog
    else (Swimming > 1.50):
      Predict: Duck
else (Swimming > 4.50):
  if Art <= 4.50:
    if Gardening <= 2.50:
      if Art <= 3.50:
        if Gardening <= 0.50:
          Predict: Duck
        else (Gardening > 0.50):
          if Running <= 8.50:
            Predict: Duck
          else (Running > 8.50):
            Predict: Dog
      else (Art > 3.50):
        if Swimming <= 11.00:
          Predict: Dog
        else (Swimming > 11.00):
          Predict: Duck
    else (Gardening > 2.50):
      if Detective Stories <= 3.50:
        if Gardening <= 6.50:
          Predict: Duck
        else (Gardening > 6.50):
          Predict: Dog
      else (Detective Stories > 3.50):
        if Swimming <= 8.50:
          if Swimming <= 7.50:
            Predict: Dog
          else (Swimming > 7.50):
            if Running <= 8.00:
              Predict: Duck
            else (Running > 8.00):
              Predict: Dog
        else (Swimming > 8.50):
          Predict: Dog
  else (Art > 4.50):
    if Detective Stories <= 1.50:
      if Art <= 9.00:
        Predict: Dog
      else (Art > 9.00):
        Predict: Duck
    else (Detective Stories > 1.50):
      if Running <= 10.50:
        if Gardening <= 10.50:
          if Art <= 9.50:
            if Detective Stories <= 5.50:
              if Running <= 7.50:
                Predict: Duck
              else (Running > 7.50):
                Predict: Dog
            else (Detective Stories > 5.50):
              if Swimming <= 13.50:
                if Running <= 9.50:
                  Predict: Duck
                else (Running > 9.50):
                  if Gardening <= 4.00:
                    Predict: Duck
                  else (Gardening > 4.00):
                    Predict: Dog
              else (Swimming > 13.50):
                if Detective Stories <= 11.50:
                  Predict: Duck
                else (Detective Stories > 11.50):
                  Predict: Dog
          else (Art > 9.50):
            if Running <= 4.00:
              Predict: Cat
            else (Running > 4.00):
              Predict: Duck
        else (Gardening > 10.50):
          Predict: Dog
      else (Running > 10.50):
        if Swimming <= 8.50:
          if Gardening <= 2.00:
            Predict: Duck
          else (Gardening > 2.00):
            Predict: Dog
        else (Swimming > 8.50):
          if Running <= 13.50:
            if Swimming <= 13.50:
              if Detective Stories <= 12.50:
                if Gardening <= 13.50:
                  if Running <= 11.50:
                    if Art <= 8.00:
                      Predict: Dog
                    else (Art > 8.00):
                      Predict: Duck
                  else (Running > 11.50):
                    Predict: Duck
                else (Gardening > 13.50):
                  Predict: Dog
              else (Detective Stories > 12.50):
                Predict: Dog
            else (Swimming > 13.50):
              if Gardening <= 5.50:
                Predict: Duck
              else (Gardening > 5.50):
                Predict: Dog
          else (Running > 13.50):
            Predict: Duck

Tree 7 Rules:
if Running <= 4.50:
  if Art <= 5.50:
    if Swimming <= 10.00:
      if Art <= 0.50:
        Predict: Cat
      else (Art > 0.50):
        Predict: Dog
    else (Swimming > 10.00):
      Predict: Duck
  else (Art > 5.50):
    if Art <= 9.50:
      if Swimming <= 7.00:
        Predict: Cat
      else (Swimming > 7.00):
        Predict: Duck
    else (Art > 9.50):
      Predict: Cat
else (Running > 4.50):
  if Swimming <= 9.50:
    if Art <= 4.50:
      if Swimming <= 7.50:
        if Gardening <= 3.00:
          Predict: Duck
        else (Gardening > 3.00):
          Predict: Dog
      else (Swimming > 7.50):
        Predict: Dog
    else (Art > 4.50):
      if Gardening <= 9.50:
        if Swimming <= 4.50:
          if Detective Stories <= 7.50:
            Predict: Duck
          else (Detective Stories > 7.50):
            if Running <= 13.50:
              if Art <= 9.50:
                Predict: Duck
              else (Art > 9.50):
                Predict: Cat
            else (Running > 13.50):
              Predict: Duck
        else (Swimming > 4.50):
          if Gardening <= 5.00:
            Predict: Duck
          else (Gardening > 5.00):
            if Art <= 10.00:
              if Gardening <= 7.50:
                Predict: Dog
              else (Gardening > 7.50):
                if Gardening <= 8.50:
                  Predict: Duck
                else (Gardening > 8.50):
                  if Art <= 6.50:
                    Predict: Dog
                  else (Art > 6.50):
                    Predict: Duck
            else (Art > 10.00):
              Predict: Dog
      else (Gardening > 9.50):
        Predict: Dog
  else (Swimming > 9.50):
    if Gardening <= 4.50:
      Predict: Duck
    else (Gardening > 4.50):
      if Art <= 4.50:
        Predict: Dog
      else (Art > 4.50):
        if Gardening <= 8.50:
          if Swimming <= 12.50:
            Predict: Duck
          else (Swimming > 12.50):
            if Running <= 13.50:
              if Detective Stories <= 5.50:
                Predict: Dog
              else (Detective Stories > 5.50):
                Predict: Duck
            else (Running > 13.50):
              Predict: Cat
        else (Gardening > 8.50):
          if Swimming <= 11.00:
            if Art <= 8.50:
              Predict: Dog
            else (Art > 8.50):
              Predict: Duck
          else (Swimming > 11.00):
            if Gardening <= 13.50:
              Predict: Dog
            else (Gardening > 13.50):
              if Running <= 11.00:
                Predict: Duck
              else (Running > 11.00):
                Predict: Dog

Tree 8 Rules:
if Swimming <= 4.50:
  if Gardening <= 8.50:
    if Gardening <= 2.50:
      if Detective Stories <= 11.50:
        if Art <= 10.50:
          Predict: Cat
        else (Art > 10.50):
          if Art <= 11.50:
            if Gardening <= 0.50:
              if Running <= 6.00:
                Predict: Cat
              else (Running > 6.00):
                Predict: Duck
            else (Gardening > 0.50):
              Predict: Cat
          else (Art > 11.50):
            Predict: Cat
      else (Detective Stories > 11.50):
        if Running <= 6.50:
          Predict: Cat
        else (Running > 6.50):
          Predict: Duck
    else (Gardening > 2.50):
      if Detective Stories <= 6.50:
        if Running <= 8.00:
          Predict: Cat
        else (Running > 8.00):
          Predict: Duck
      else (Detective Stories > 6.50):
        if Detective Stories <= 10.50:
          if Gardening <= 3.50:
            if Running <= 7.00:
              Predict: Cat
            else (Running > 7.00):
              Predict: Duck
          else (Gardening > 3.50):
            Predict: Cat
        else (Detective Stories > 10.50):
          Predict: Cat
  else (Gardening > 8.50):
    if Running <= 5.00:
      Predict: Cat
    else (Running > 5.00):
      if Art <= 7.50:
        Predict: Dog
      else (Art > 7.50):
        Predict: Cat
else (Swimming > 4.50):
  if Running <= 9.50:
    if Art <= 5.50:
      if Art <= 1.50:
        if Art <= 0.50:
          if Running <= 3.50:
            Predict: Duck
          else (Running > 3.50):
            Predict: Dog
        else (Art > 0.50):
          Predict: Dog
      else (Art > 1.50):
        if Detective Stories <= 4.50:
          Predict: Duck
        else (Detective Stories > 4.50):
          if Gardening <= 4.50:
            Predict: Duck
          else (Gardening > 4.50):
            if Gardening <= 9.50:
              if Detective Stories <= 11.00:
                Predict: Dog
              else (Detective Stories > 11.00):
                if Swimming <= 10.50:
                  Predict: Duck
                else (Swimming > 10.50):
                  Predict: Dog
            else (Gardening > 9.50):
              Predict: Dog
    else (Art > 5.50):
      Predict: Duck
  else (Running > 9.50):
    if Swimming <= 9.50:
      if Detective Stories <= 12.50:
        if Detective Stories <= 10.50:
          if Running <= 13.50:
            if Art <= 9.00:
              Predict: Dog
            else (Art > 9.00):
              if Gardening <= 8.00:
                Predict: Dog
              else (Gardening > 8.00):
                Predict: Duck
          else (Running > 13.50):
            if Art <= 10.50:
              Predict: Dog
            else (Art > 10.50):
              Predict: Duck
        else (Detective Stories > 10.50):
          Predict: Dog
      else (Detective Stories > 12.50):
        if Gardening <= 4.50:
          Predict: Duck
        else (Gardening > 4.50):
          Predict: Dog
    else (Swimming > 9.50):
      if Gardening <= 6.50:
        if Running <= 10.50:
          if Swimming <= 13.00:
            Predict: Duck
          else (Swimming > 13.00):
            Predict: Dog
        else (Running > 10.50):
          if Running <= 12.50:
            Predict: Duck
          else (Running > 12.50):
            if Art <= 4.50:
              Predict: Dog
            else (Art > 4.50):
              if Gardening <= 4.00:
                Predict: Duck
              else (Gardening > 4.00):
                if Detective Stories <= 8.50:
                  Predict: Dog
                else (Detective Stories > 8.50):
                  if Running <= 13.50:
                    Predict: Duck
                  else (Running > 13.50):
                    if Swimming <= 12.50:
                      Predict: Duck
                    else (Swimming > 12.50):
                      Predict: Cat
      else (Gardening > 6.50):
        if Detective Stories <= 4.50:
          if Running <= 11.50:
            Predict: Dog
          else (Running > 11.50):
            Predict: Duck
        else (Detective Stories > 4.50):
          if Gardening <= 13.50:
            if Art <= 9.00:
              Predict: Dog
            else (Art > 9.00):
              if Art <= 12.50:
                Predict: Duck
              else (Art > 12.50):
                Predict: Dog
          else (Gardening > 13.50):
            if Swimming <= 12.50:
              Predict: Duck
            else (Swimming > 12.50):
              Predict: Dog

Tree 9 Rules:
if Running <= 4.50:
  if Running <= 3.50:
    if Art <= 0.50:
      Predict: Duck
    else (Art > 0.50):
      if Swimming <= 5.00:
        Predict: Cat
      else (Swimming > 5.00):
        if Swimming <= 10.50:
          if Detective Stories <= 9.50:
            Predict: Dog
          else (Detective Stories > 9.50):
            Predict: Duck
        else (Swimming > 10.50):
          Predict: Cat
  else (Running > 3.50):
    if Gardening <= 4.50:
      Predict: Cat
    else (Gardening > 4.50):
      if Detective Stories <= 11.00:
        if Art <= 5.50:
          Predict: Duck
        else (Art > 5.50):
          Predict: Cat
      else (Detective Stories > 11.00):
        Predict: Cat
else (Running > 4.50):
  if Art <= 4.50:
    if Running <= 9.00:
      if Art <= 1.50:
        Predict: Dog
      else (Art > 1.50):
        if Gardening <= 9.00:
          if Swimming <= 6.50:
            if Art <= 3.50:
              Predict: Duck
            else (Art > 3.50):
              Predict: Dog
          else (Swimming > 6.50):
            Predict: Duck
        else (Gardening > 9.00):
          Predict: Dog
    else (Running > 9.00):
      if Gardening <= 3.50:
        if Swimming <= 11.50:
          if Running <= 11.50:
            Predict: Dog
          else (Running > 11.50):
            Predict: Cat
        else (Swimming > 11.50):
          Predict: Duck
      else (Gardening > 3.50):
        Predict: Dog
  else (Art > 4.50):
    if Swimming <= 4.50:
      if Art <= 9.50:
        if Art <= 8.50:
          if Detective Stories <= 8.50:
            Predict: Dog
          else (Detective Stories > 8.50):
            Predict: Cat
        else (Art > 8.50):
          Predict: Duck
      else (Art > 9.50):
        if Running <= 9.00:
          Predict: Cat
        else (Running > 9.00):
          if Gardening <= 1.00:
            Predict: Duck
          else (Gardening > 1.00):
            Predict: Cat
    else (Swimming > 4.50):
      if Detective Stories <= 1.50:
        Predict: Dog
      else (Detective Stories > 1.50):
        if Gardening <= 4.50:
          if Art <= 7.50:
            if Gardening <= 3.50:
              Predict: Duck
            else (Gardening > 3.50):
              if Running <= 10.50:
                Predict: Duck
              else (Running > 10.50):
                Predict: Dog
          else (Art > 7.50):
            Predict: Duck
        else (Gardening > 4.50):
          if Running <= 9.50:
            if Detective Stories <= 11.50:
              Predict: Duck
            else (Detective Stories > 11.50):
              if Gardening <= 8.50:
                Predict: Duck
              else (Gardening > 8.50):
                if Detective Stories <= 12.50:
                  Predict: Dog
                else (Detective Stories > 12.50):
                  Predict: Duck
          else (Running > 9.50):
            if Art <= 5.50:
              Predict: Duck
            else (Art > 5.50):
              if Detective Stories <= 4.50:
                Predict: Duck
              else (Detective Stories > 4.50):
                if Swimming <= 9.50:
                  Predict: Dog
                else (Swimming > 9.50):
                  if Detective Stories <= 10.50:
                    Predict: Dog
                  else (Detective Stories > 10.50):
                    if Detective Stories <= 12.50:
                      Predict: Duck
                    else (Detective Stories > 12.50):
                      Predict: Dog

Aggregated Rules (Across Forest):
Swimming > 4.50 appeared in 54.2% of paths.
Art > 4.50 appeared in 54.2% of paths.

### Setting: Strict (n_trees=20, threshold=0.7) ###

Class: Cat

Simple Rules (Single Trees):

Tree 0 Rules:
if Swimming <= 4.50:
  if Detective Stories <= 2.50:
    if Swimming <= 2.50:
      Predict: Dog
    else (Swimming > 2.50):
      Predict: Cat
  else (Detective Stories > 2.50):
    if Running <= 8.50:
      if Swimming <= 0.50:
        if Detective Stories <= 6.00:
          Predict: Dog
        else (Detective Stories > 6.00):
          Predict: Cat
      else (Swimming > 0.50):
        Predict: Cat
    else (Running > 8.50):
      if Detective Stories <= 8.00:
        Predict: Dog
      else (Detective Stories > 8.00):
        if Gardening <= 1.50:
          if Gardening <= 0.50:
            if Art <= 12.00:
              Predict: Duck
            else (Art > 12.00):
              Predict: Cat
          else (Gardening > 0.50):
            Predict: Duck
        else (Gardening > 1.50):
          if Gardening <= 3.50:
            if Swimming <= 2.00:
              Predict: Duck
            else (Swimming > 2.00):
              if Detective Stories <= 11.00:
                if Detective Stories <= 9.50:
                  Predict: Cat
                else (Detective Stories > 9.50):
                  Predict: Duck
              else (Detective Stories > 11.00):
                Predict: Cat
          else (Gardening > 3.50):
            Predict: Cat
else (Swimming > 4.50):
  if Art <= 4.50:
    if Running <= 6.50:
      if Gardening <= 6.50:
        Predict: Duck
      else (Gardening > 6.50):
        if Detective Stories <= 9.00:
          Predict: Dog
        else (Detective Stories > 9.00):
          if Detective Stories <= 13.00:
            if Gardening <= 11.00:
              Predict: Duck
            else (Gardening > 11.00):
              Predict: Dog
          else (Detective Stories > 13.00):
            Predict: Dog
    else (Running > 6.50):
      if Gardening <= 3.50:
        Predict: Duck
      else (Gardening > 3.50):
        Predict: Dog
  else (Art > 4.50):
    if Gardening <= 9.50:
      if Swimming <= 7.50:
        if Detective Stories <= 8.00:
          Predict: Duck
        else (Detective Stories > 8.00):
          if Art <= 6.50:
            Predict: Dog
          else (Art > 6.50):
            if Art <= 13.50:
              if Gardening <= 4.50:
                Predict: Duck
              else (Gardening > 4.50):
                if Swimming <= 6.50:
                  if Art <= 10.00:
                    Predict: Duck
                  else (Art > 10.00):
                    Predict: Dog
                else (Swimming > 6.50):
                  Predict: Dog
            else (Art > 13.50):
              Predict: Duck
      else (Swimming > 7.50):
        if Running <= 13.50:
          if Running <= 4.50:
            if Swimming <= 10.50:
              Predict: Duck
            else (Swimming > 10.50):
              if Detective Stories <= 10.50:
                if Gardening <= 6.50:
                  Predict: Duck
                else (Gardening > 6.50):
                  Predict: Cat
              else (Detective Stories > 10.50):
                Predict: Cat
          else (Running > 4.50):
            if Swimming <= 9.50:
              if Gardening <= 4.50:
                Predict: Duck
              else (Gardening > 4.50):
                if Gardening <= 7.50:
                  Predict: Dog
                else (Gardening > 7.50):
                  Predict: Duck
            else (Swimming > 9.50):
              Predict: Duck
        else (Running > 13.50):
          if Gardening <= 4.00:
            Predict: Duck
          else (Gardening > 4.00):
            if Swimming <= 12.50:
              Predict: Dog
            else (Swimming > 12.50):
              Predict: Cat
    else (Gardening > 9.50):
      if Swimming <= 9.50:
        Predict: Dog
      else (Swimming > 9.50):
        if Detective Stories <= 12.00:
          if Swimming <= 12.50:
            Predict: Duck
          else (Swimming > 12.50):
            if Running <= 9.00:
              Predict: Duck
            else (Running > 9.00):
              Predict: Dog
        else (Detective Stories > 12.00):
          Predict: Dog

Tree 1 Rules:
if Art <= 4.50:
  if Running <= 4.50:
    if Art <= 0.50:
      Predict: Cat
    else (Art > 0.50):
      if Swimming <= 5.00:
        if Detective Stories <= 4.00:
          Predict: Dog
        else (Detective Stories > 4.00):
          Predict: Cat
      else (Swimming > 5.00):
        if Running <= 3.00:
          Predict: Dog
        else (Running > 3.00):
          if Art <= 1.50:
            Predict: Dog
          else (Art > 1.50):
            if Art <= 3.00:
              Predict: Duck
            else (Art > 3.00):
              Predict: Dog
  else (Running > 4.50):
    if Swimming <= 3.50:
      Predict: Cat
    else (Swimming > 3.50):
      if Swimming <= 11.50:
        if Gardening <= 4.50:
          if Detective Stories <= 11.00:
            Predict: Duck
          else (Detective Stories > 11.00):
            Predict: Dog
        else (Gardening > 4.50):
          if Running <= 6.50:
            if Gardening <= 10.50:
              Predict: Duck
            else (Gardening > 10.50):
              Predict: Dog
          else (Running > 6.50):
            Predict: Dog
      else (Swimming > 11.50):
        if Detective Stories <= 5.50:
          Predict: Duck
        else (Detective Stories > 5.50):
          if Running <= 10.50:
            if Gardening <= 4.00:
              Predict: Duck
            else (Gardening > 4.00):
              Predict: Dog
          else (Running > 10.50):
            if Gardening <= 4.00:
              Predict: Duck
            else (Gardening > 4.00):
              Predict: Dog
else (Art > 4.50):
  if Swimming <= 4.50:
    if Running <= 8.50:
      if Swimming <= 0.50:
        if Detective Stories <= 6.00:
          Predict: Dog
        else (Detective Stories > 6.00):
          Predict: Cat
      else (Swimming > 0.50):
        Predict: Cat
    else (Running > 8.50):
      if Running <= 10.50:
        if Detective Stories <= 5.50:
          Predict: Cat
        else (Detective Stories > 5.50):
          Predict: Duck
      else (Running > 10.50):
        if Art <= 8.50:
          Predict: Duck
        else (Art > 8.50):
          Predict: Cat
  else (Swimming > 4.50):
    if Gardening <= 5.50:
      if Running <= 13.50:
        if Swimming <= 12.50:
          Predict: Duck
        else (Swimming > 12.50):
          if Running <= 5.00:
            Predict: Cat
          else (Running > 5.00):
            Predict: Duck
      else (Running > 13.50):
        if Detective Stories <= 10.50:
          Predict: Duck
        else (Detective Stories > 10.50):
          if Art <= 8.50:
            Predict: Dog
          else (Art > 8.50):
            Predict: Cat
    else (Gardening > 5.50):
      if Running <= 4.50:
        Predict: Cat
      else (Running > 4.50):
        if Detective Stories <= 2.00:
          Predict: Dog
        else (Detective Stories > 2.00):
          if Running <= 9.50:
            if Swimming <= 10.50:
              if Art <= 5.50:
                Predict: Dog
              else (Art > 5.50):
                Predict: Duck
            else (Swimming > 10.50):
              Predict: Duck
          else (Running > 9.50):
            if Swimming <= 8.50:
              Predict: Dog
            else (Swimming > 8.50):
              if Running <= 10.50:
                Predict: Dog
              else (Running > 10.50):
                if Gardening <= 13.50:
                  if Detective Stories <= 7.50:
                    if Detective Stories <= 4.50:
                      Predict: Duck
                    else (Detective Stories > 4.50):
                      Predict: Dog
                  else (Detective Stories > 7.50):
                    Predict: Duck
                else (Gardening > 13.50):
                  Predict: Dog

Tree 2 Rules:
if Art <= 4.50:
  if Gardening <= 4.50:
    if Swimming <= 4.50:
      if Running <= 8.00:
        Predict: Cat
      else (Running > 8.00):
        Predict: Duck
    else (Swimming > 4.50):
      if Running <= 13.00:
        if Gardening <= 0.50:
          Predict: Duck
        else (Gardening > 0.50):
          if Running <= 8.50:
            Predict: Duck
          else (Running > 8.50):
            if Detective Stories <= 10.50:
              Predict: Dog
            else (Detective Stories > 10.50):
              Predict: Duck
      else (Running > 13.00):
        Predict: Dog
  else (Gardening > 4.50):
    if Swimming <= 13.50:
      if Running <= 6.50:
        if Running <= 5.50:
          Predict: Dog
        else (Running > 5.50):
          if Art <= 2.50:
            Predict: Dog
          else (Art > 2.50):
            if Swimming <= 10.00:
              if Swimming <= 4.00:
                Predict: Dog
              else (Swimming > 4.00):
                Predict: Duck
            else (Swimming > 10.00):
              Predict: Dog
      else (Running > 6.50):
        Predict: Dog
    else (Swimming > 13.50):
      if Running <= 7.00:
        Predict: Duck
      else (Running > 7.00):
        Predict: Dog
else (Art > 4.50):
  if Art <= 9.50:
    if Swimming <= 4.50:
      if Art <= 8.50:
        if Running <= 10.50:
          Predict: Cat
        else (Running > 10.50):
          Predict: Duck
      else (Art > 8.50):
        if Gardening <= 6.00:
          Predict: Duck
        else (Gardening > 6.00):
          Predict: Cat
    else (Swimming > 4.50):
      if Detective Stories <= 6.50:
        if Swimming <= 13.00:
          if Art <= 5.50:
            Predict: Dog
          else (Art > 5.50):
            if Gardening <= 9.50:
              Predict: Duck
            else (Gardening > 9.50):
              if Swimming <= 10.50:
                Predict: Duck
              else (Swimming > 10.50):
                Predict: Dog
        else (Swimming > 13.00):
          Predict: Dog
      else (Detective Stories > 6.50):
        if Running <= 9.50:
          if Gardening <= 10.50:
            Predict: Duck
          else (Gardening > 10.50):
            if Swimming <= 10.50:
              Predict: Dog
            else (Swimming > 10.50):
              Predict: Duck
        else (Running > 9.50):
          if Art <= 6.50:
            if Gardening <= 4.50:
              Predict: Duck
            else (Gardening > 4.50):
              Predict: Dog
          else (Art > 6.50):
            if Gardening <= 9.50:
              if Running <= 10.50:
                if Swimming <= 8.50:
                  Predict: Duck
                else (Swimming > 8.50):
                  Predict: Dog
              else (Running > 10.50):
                Predict: Duck
            else (Gardening > 9.50):
              Predict: Dog
  else (Art > 9.50):
    if Detective Stories <= 2.50:
      if Swimming <= 7.00:
        Predict: Cat
      else (Swimming > 7.00):
        Predict: Duck
    else (Detective Stories > 2.50):
      if Running <= 4.50:
        Predict: Cat
      else (Running > 4.50):
        if Swimming <= 7.50:
          if Gardening <= 11.50:
            if Gardening <= 2.00:
              if Swimming <= 2.50:
                Predict: Duck
              else (Swimming > 2.50):
                Predict: Cat
            else (Gardening > 2.00):
              Predict: Cat
          else (Gardening > 11.50):
            Predict: Dog
        else (Swimming > 7.50):
          if Swimming <= 9.50:
            if Gardening <= 4.50:
              Predict: Duck
            else (Gardening > 4.50):
              if Running <= 12.00:
                Predict: Duck
              else (Running > 12.00):
                Predict: Dog
          else (Swimming > 9.50):
            Predict: Duck

Tree 3 Rules:
if Running <= 4.50:
  if Gardening <= 10.00:
    if Art <= 5.50:
      if Swimming <= 10.50:
        if Gardening <= 4.00:
          Predict: Cat
        else (Gardening > 4.00):
          if Detective Stories <= 11.50:
            Predict: Cat
          else (Detective Stories > 11.50):
            Predict: Dog
      else (Swimming > 10.50):
        if Detective Stories <= 6.00:
          Predict: Dog
        else (Detective Stories > 6.00):
          Predict: Duck
    else (Art > 5.50):
      if Swimming <= 7.00:
        Predict: Cat
      else (Swimming > 7.00):
        if Running <= 2.50:
          if Gardening <= 2.50:
            Predict: Duck
          else (Gardening > 2.50):
            if Swimming <= 12.50:
              if Art <= 10.00:
                Predict: Duck
              else (Art > 10.00):
                Predict: Cat
            else (Swimming > 12.50):
              Predict: Cat
        else (Running > 2.50):
          Predict: Cat
  else (Gardening > 10.00):
    if Detective Stories <= 9.50:
      Predict: Dog
    else (Detective Stories > 9.50):
      if Art <= 5.50:
        Predict: Dog
      else (Art > 5.50):
        Predict: Cat
else (Running > 4.50):
  if Art <= 4.50:
    if Running <= 6.50:
      if Detective Stories <= 3.50:
        Predict: Duck
      else (Detective Stories > 3.50):
        Predict: Dog
    else (Running > 6.50):
      if Swimming <= 13.50:
        Predict: Dog
      else (Swimming > 13.50):
        if Running <= 12.50:
          if Gardening <= 5.50:
            Predict: Duck
          else (Gardening > 5.50):
            Predict: Dog
        else (Running > 12.50):
          Predict: Dog
  else (Art > 4.50):
    if Running <= 10.50:
      if Gardening <= 4.50:
        if Swimming <= 5.00:
          if Art <= 11.50:
            Predict: Duck
          else (Art > 11.50):
            if Running <= 9.50:
              Predict: Cat
            else (Running > 9.50):
              Predict: Duck
        else (Swimming > 5.00):
          Predict: Duck
      else (Gardening > 4.50):
        if Swimming <= 4.00:
          if Gardening <= 8.00:
            Predict: Cat
          else (Gardening > 8.00):
            if Art <= 7.50:
              Predict: Dog
            else (Art > 7.50):
              Predict: Cat
        else (Swimming > 4.00):
          if Art <= 5.50:
            if Running <= 8.50:
              Predict: Dog
            else (Running > 8.50):
              Predict: Duck
          else (Art > 5.50):
            if Detective Stories <= 3.00:
              Predict: Dog
            else (Detective Stories > 3.00):
              Predict: Duck
    else (Running > 10.50):
      if Gardening <= 4.50:
        if Swimming <= 4.50:
          if Gardening <= 3.50:
            Predict: Cat
          else (Gardening > 3.50):
            if Running <= 13.50:
              Predict: Cat
            else (Running > 13.50):
              Predict: Duck
        else (Swimming > 4.50):
          Predict: Duck
      else (Gardening > 4.50):
        if Art <= 9.50:
          if Gardening <= 6.50:
            if Running <= 13.50:
              Predict: Dog
            else (Running > 13.50):
              Predict: Duck
          else (Gardening > 6.50):
            if Detective Stories <= 4.50:
              if Art <= 7.00:
                Predict: Duck
              else (Art > 7.00):
                Predict: Dog
            else (Detective Stories > 4.50):
              Predict: Dog
        else (Art > 9.50):
          if Swimming <= 5.00:
            Predict: Cat
          else (Swimming > 5.00):
            if Detective Stories <= 12.00:
              if Running <= 12.00:
                Predict: Duck
              else (Running > 12.00):
                if Running <= 13.50:
                  Predict: Dog
                else (Running > 13.50):
                  Predict: Cat
            else (Detective Stories > 12.00):
              Predict: Dog

Tree 4 Rules:
if Swimming <= 4.50:
  if Running <= 13.50:
    if Running <= 9.00:
      if Detective Stories <= 5.50:
        if Running <= 4.00:
          Predict: Cat
        else (Running > 4.00):
          Predict: Dog
      else (Detective Stories > 5.50):
        Predict: Cat
    else (Running > 9.00):
      if Running <= 10.50:
        Predict: Duck
      else (Running > 10.50):
        if Art <= 12.00:
          if Art <= 7.50:
            if Gardening <= 3.00:
              Predict: Cat
            else (Gardening > 3.00):
              if Gardening <= 6.50:
                Predict: Duck
              else (Gardening > 6.50):
                Predict: Dog
          else (Art > 7.50):
            Predict: Cat
        else (Art > 12.00):
          if Gardening <= 3.00:
            Predict: Duck
          else (Gardening > 3.00):
            Predict: Cat
  else (Running > 13.50):
    Predict: Duck
else (Swimming > 4.50):
  if Gardening <= 4.50:
    if Running <= 13.50:
      if Running <= 3.50:
        if Detective Stories <= 11.00:
          Predict: Cat
        else (Detective Stories > 11.00):
          Predict: Duck
      else (Running > 3.50):
        Predict: Duck
    else (Running > 13.50):
      if Art <= 5.50:
        Predict: Dog
      else (Art > 5.50):
        Predict: Duck
  else (Gardening > 4.50):
    if Running <= 9.50:
      if Art <= 6.50:
        if Gardening <= 8.50:
          if Art <= 1.50:
            Predict: Dog
          else (Art > 1.50):
            if Art <= 2.50:
              Predict: Duck
            else (Art > 2.50):
              if Running <= 6.50:
                if Gardening <= 6.50:
                  Predict: Dog
                else (Gardening > 6.50):
                  Predict: Duck
              else (Running > 6.50):
                if Swimming <= 10.00:
                  if Gardening <= 7.00:
                    Predict: Dog
                  else (Gardening > 7.00):
                    Predict: Duck
                else (Swimming > 10.00):
                  Predict: Dog
        else (Gardening > 8.50):
          Predict: Dog
      else (Art > 6.50):
        if Running <= 4.50:
          Predict: Cat
        else (Running > 4.50):
          Predict: Duck
    else (Running > 9.50):
      if Art <= 4.50:
        Predict: Dog
      else (Art > 4.50):
        if Swimming <= 9.50:
          Predict: Dog
        else (Swimming > 9.50):
          if Running <= 13.50:
            if Detective Stories <= 12.00:
              if Swimming <= 12.50:
                Predict: Duck
              else (Swimming > 12.50):
                Predict: Dog
            else (Detective Stories > 12.00):
              Predict: Dog
          else (Running > 13.50):
            if Swimming <= 12.50:
              Predict: Duck
            else (Swimming > 12.50):
              Predict: Cat

Tree 5 Rules:
if Running <= 4.50:
  if Art <= 4.50:
    if Gardening <= 5.50:
      Predict: Duck
    else (Gardening > 5.50):
      Predict: Dog
  else (Art > 4.50):
    if Art <= 7.50:
      if Swimming <= 7.00:
        Predict: Cat
      else (Swimming > 7.00):
        Predict: Duck
    else (Art > 7.50):
      Predict: Cat
else (Running > 4.50):
  if Art <= 4.50:
    if Gardening <= 4.50:
      if Running <= 13.50:
        if Running <= 12.50:
          if Running <= 11.00:
            if Swimming <= 11.50:
              Predict: Dog
            else (Swimming > 11.50):
              Predict: Duck
          else (Running > 11.00):
            Predict: Duck
        else (Running > 12.50):
          Predict: Cat
      else (Running > 13.50):
        Predict: Dog
    else (Gardening > 4.50):
      if Detective Stories <= 2.50:
        if Gardening <= 6.50:
          Predict: Duck
        else (Gardening > 6.50):
          Predict: Dog
      else (Detective Stories > 2.50):
        if Running <= 6.50:
          if Swimming <= 8.50:
            if Gardening <= 10.50:
              Predict: Duck
            else (Gardening > 10.50):
              Predict: Dog
          else (Swimming > 8.50):
            Predict: Dog
        else (Running > 6.50):
          Predict: Dog
  else (Art > 4.50):
    if Swimming <= 4.50:
      if Detective Stories <= 6.50:
        if Art <= 9.50:
          Predict: Duck
        else (Art > 9.50):
          Predict: Cat
      else (Detective Stories > 6.50):
        if Art <= 9.50:
          if Running <= 8.00:
            Predict: Cat
          else (Running > 8.00):
            Predict: Duck
        else (Art > 9.50):
          if Swimming <= 1.50:
            if Gardening <= 2.00:
              if Running <= 9.50:
                Predict: Cat
              else (Running > 9.50):
                Predict: Duck
            else (Gardening > 2.00):
              Predict: Cat
          else (Swimming > 1.50):
            Predict: Cat
    else (Swimming > 4.50):
      if Running <= 10.50:
        if Detective Stories <= 5.50:
          if Swimming <= 10.50:
            Predict: Dog
          else (Swimming > 10.50):
            Predict: Duck
        else (Detective Stories > 5.50):
          Predict: Duck
      else (Running > 10.50):
        if Art <= 6.50:
          if Swimming <= 12.50:
            Predict: Dog
          else (Swimming > 12.50):
            Predict: Duck
        else (Art > 6.50):
          if Gardening <= 4.00:
            Predict: Duck
          else (Gardening > 4.00):
            if Detective Stories <= 12.50:
              if Art <= 9.00:
                Predict: Dog
              else (Art > 9.00):
                if Detective Stories <= 11.50:
                  Predict: Cat
                else (Detective Stories > 11.50):
                  Predict: Duck
            else (Detective Stories > 12.50):
              Predict: Dog

Tree 6 Rules:
if Swimming <= 4.50:
  if Running <= 13.50:
    if Running <= 5.50:
      if Detective Stories <= 2.50:
        if Running <= 1.50:
          Predict: Cat
        else (Running > 1.50):
          Predict: Dog
      else (Detective Stories > 2.50):
        Predict: Cat
    else (Running > 5.50):
      if Art <= 6.50:
        if Gardening <= 6.50:
          if Art <= 2.00:
            Predict: Cat
          else (Art > 2.00):
            Predict: Duck
        else (Gardening > 6.50):
          Predict: Dog
      else (Art > 6.50):
        if Art <= 9.50:
          if Swimming <= 2.00:
            Predict: Duck
          else (Swimming > 2.00):
            Predict: Cat
        else (Art > 9.50):
          if Running <= 9.50:
            Predict: Cat
          else (Running > 9.50):
            if Gardening <= 1.00:
              Predict: Duck
            else (Gardening > 1.00):
              if Running <= 10.50:
                Predict: Duck
              else (Running > 10.50):
                Predict: Cat
  else (Running > 13.50):
    if Swimming <= 1.50:
      Predict: Dog
    else (Swimming > 1.50):
      Predict: Duck
else (Swimming > 4.50):
  if Art <= 4.50:
    if Gardening <= 2.50:
      if Art <= 3.50:
        if Gardening <= 0.50:
          Predict: Duck
        else (Gardening > 0.50):
          if Running <= 8.50:
            Predict: Duck
          else (Running > 8.50):
            Predict: Dog
      else (Art > 3.50):
        if Swimming <= 11.00:
          Predict: Dog
        else (Swimming > 11.00):
          Predict: Duck
    else (Gardening > 2.50):
      if Detective Stories <= 3.50:
        if Gardening <= 6.50:
          Predict: Duck
        else (Gardening > 6.50):
          Predict: Dog
      else (Detective Stories > 3.50):
        if Swimming <= 8.50:
          if Swimming <= 7.50:
            Predict: Dog
          else (Swimming > 7.50):
            if Running <= 8.00:
              Predict: Duck
            else (Running > 8.00):
              Predict: Dog
        else (Swimming > 8.50):
          Predict: Dog
  else (Art > 4.50):
    if Detective Stories <= 1.50:
      if Art <= 9.00:
        Predict: Dog
      else (Art > 9.00):
        Predict: Duck
    else (Detective Stories > 1.50):
      if Running <= 10.50:
        if Gardening <= 10.50:
          if Art <= 9.50:
            if Detective Stories <= 5.50:
              if Running <= 7.50:
                Predict: Duck
              else (Running > 7.50):
                Predict: Dog
            else (Detective Stories > 5.50):
              if Swimming <= 13.50:
                if Running <= 9.50:
                  Predict: Duck
                else (Running > 9.50):
                  if Gardening <= 4.00:
                    Predict: Duck
                  else (Gardening > 4.00):
                    Predict: Dog
              else (Swimming > 13.50):
                if Detective Stories <= 11.50:
                  Predict: Duck
                else (Detective Stories > 11.50):
                  Predict: Dog
          else (Art > 9.50):
            if Running <= 4.00:
              Predict: Cat
            else (Running > 4.00):
              Predict: Duck
        else (Gardening > 10.50):
          Predict: Dog
      else (Running > 10.50):
        if Swimming <= 8.50:
          if Gardening <= 2.00:
            Predict: Duck
          else (Gardening > 2.00):
            Predict: Dog
        else (Swimming > 8.50):
          if Running <= 13.50:
            if Swimming <= 13.50:
              if Detective Stories <= 12.50:
                if Gardening <= 13.50:
                  if Running <= 11.50:
                    if Art <= 8.00:
                      Predict: Dog
                    else (Art > 8.00):
                      Predict: Duck
                  else (Running > 11.50):
                    Predict: Duck
                else (Gardening > 13.50):
                  Predict: Dog
              else (Detective Stories > 12.50):
                Predict: Dog
            else (Swimming > 13.50):
              if Gardening <= 5.50:
                Predict: Duck
              else (Gardening > 5.50):
                Predict: Dog
          else (Running > 13.50):
            Predict: Duck

Tree 7 Rules:
if Running <= 4.50:
  if Art <= 5.50:
    if Swimming <= 10.00:
      if Art <= 0.50:
        Predict: Cat
      else (Art > 0.50):
        Predict: Dog
    else (Swimming > 10.00):
      Predict: Duck
  else (Art > 5.50):
    if Art <= 9.50:
      if Swimming <= 7.00:
        Predict: Cat
      else (Swimming > 7.00):
        Predict: Duck
    else (Art > 9.50):
      Predict: Cat
else (Running > 4.50):
  if Swimming <= 9.50:
    if Art <= 4.50:
      if Swimming <= 7.50:
        if Gardening <= 3.00:
          Predict: Duck
        else (Gardening > 3.00):
          Predict: Dog
      else (Swimming > 7.50):
        Predict: Dog
    else (Art > 4.50):
      if Gardening <= 9.50:
        if Swimming <= 4.50:
          if Detective Stories <= 7.50:
            Predict: Duck
          else (Detective Stories > 7.50):
            if Running <= 13.50:
              if Art <= 9.50:
                Predict: Duck
              else (Art > 9.50):
                Predict: Cat
            else (Running > 13.50):
              Predict: Duck
        else (Swimming > 4.50):
          if Gardening <= 5.00:
            Predict: Duck
          else (Gardening > 5.00):
            if Art <= 10.00:
              if Gardening <= 7.50:
                Predict: Dog
              else (Gardening > 7.50):
                if Gardening <= 8.50:
                  Predict: Duck
                else (Gardening > 8.50):
                  if Art <= 6.50:
                    Predict: Dog
                  else (Art > 6.50):
                    Predict: Duck
            else (Art > 10.00):
              Predict: Dog
      else (Gardening > 9.50):
        Predict: Dog
  else (Swimming > 9.50):
    if Gardening <= 4.50:
      Predict: Duck
    else (Gardening > 4.50):
      if Art <= 4.50:
        Predict: Dog
      else (Art > 4.50):
        if Gardening <= 8.50:
          if Swimming <= 12.50:
            Predict: Duck
          else (Swimming > 12.50):
            if Running <= 13.50:
              if Detective Stories <= 5.50:
                Predict: Dog
              else (Detective Stories > 5.50):
                Predict: Duck
            else (Running > 13.50):
              Predict: Cat
        else (Gardening > 8.50):
          if Swimming <= 11.00:
            if Art <= 8.50:
              Predict: Dog
            else (Art > 8.50):
              Predict: Duck
          else (Swimming > 11.00):
            if Gardening <= 13.50:
              Predict: Dog
            else (Gardening > 13.50):
              if Running <= 11.00:
                Predict: Duck
              else (Running > 11.00):
                Predict: Dog

Tree 8 Rules:
if Swimming <= 4.50:
  if Gardening <= 8.50:
    if Gardening <= 2.50:
      if Detective Stories <= 11.50:
        if Art <= 10.50:
          Predict: Cat
        else (Art > 10.50):
          if Art <= 11.50:
            if Gardening <= 0.50:
              if Running <= 6.00:
                Predict: Cat
              else (Running > 6.00):
                Predict: Duck
            else (Gardening > 0.50):
              Predict: Cat
          else (Art > 11.50):
            Predict: Cat
      else (Detective Stories > 11.50):
        if Running <= 6.50:
          Predict: Cat
        else (Running > 6.50):
          Predict: Duck
    else (Gardening > 2.50):
      if Detective Stories <= 6.50:
        if Running <= 8.00:
          Predict: Cat
        else (Running > 8.00):
          Predict: Duck
      else (Detective Stories > 6.50):
        if Detective Stories <= 10.50:
          if Gardening <= 3.50:
            if Running <= 7.00:
              Predict: Cat
            else (Running > 7.00):
              Predict: Duck
          else (Gardening > 3.50):
            Predict: Cat
        else (Detective Stories > 10.50):
          Predict: Cat
  else (Gardening > 8.50):
    if Running <= 5.00:
      Predict: Cat
    else (Running > 5.00):
      if Art <= 7.50:
        Predict: Dog
      else (Art > 7.50):
        Predict: Cat
else (Swimming > 4.50):
  if Running <= 9.50:
    if Art <= 5.50:
      if Art <= 1.50:
        if Art <= 0.50:
          if Running <= 3.50:
            Predict: Duck
          else (Running > 3.50):
            Predict: Dog
        else (Art > 0.50):
          Predict: Dog
      else (Art > 1.50):
        if Detective Stories <= 4.50:
          Predict: Duck
        else (Detective Stories > 4.50):
          if Gardening <= 4.50:
            Predict: Duck
          else (Gardening > 4.50):
            if Gardening <= 9.50:
              if Detective Stories <= 11.00:
                Predict: Dog
              else (Detective Stories > 11.00):
                if Swimming <= 10.50:
                  Predict: Duck
                else (Swimming > 10.50):
                  Predict: Dog
            else (Gardening > 9.50):
              Predict: Dog
    else (Art > 5.50):
      Predict: Duck
  else (Running > 9.50):
    if Swimming <= 9.50:
      if Detective Stories <= 12.50:
        if Detective Stories <= 10.50:
          if Running <= 13.50:
            if Art <= 9.00:
              Predict: Dog
            else (Art > 9.00):
              if Gardening <= 8.00:
                Predict: Dog
              else (Gardening > 8.00):
                Predict: Duck
          else (Running > 13.50):
            if Art <= 10.50:
              Predict: Dog
            else (Art > 10.50):
              Predict: Duck
        else (Detective Stories > 10.50):
          Predict: Dog
      else (Detective Stories > 12.50):
        if Gardening <= 4.50:
          Predict: Duck
        else (Gardening > 4.50):
          Predict: Dog
    else (Swimming > 9.50):
      if Gardening <= 6.50:
        if Running <= 10.50:
          if Swimming <= 13.00:
            Predict: Duck
          else (Swimming > 13.00):
            Predict: Dog
        else (Running > 10.50):
          if Running <= 12.50:
            Predict: Duck
          else (Running > 12.50):
            if Art <= 4.50:
              Predict: Dog
            else (Art > 4.50):
              if Gardening <= 4.00:
                Predict: Duck
              else (Gardening > 4.00):
                if Detective Stories <= 8.50:
                  Predict: Dog
                else (Detective Stories > 8.50):
                  if Running <= 13.50:
                    Predict: Duck
                  else (Running > 13.50):
                    if Swimming <= 12.50:
                      Predict: Duck
                    else (Swimming > 12.50):
                      Predict: Cat
      else (Gardening > 6.50):
        if Detective Stories <= 4.50:
          if Running <= 11.50:
            Predict: Dog
          else (Running > 11.50):
            Predict: Duck
        else (Detective Stories > 4.50):
          if Gardening <= 13.50:
            if Art <= 9.00:
              Predict: Dog
            else (Art > 9.00):
              if Art <= 12.50:
                Predict: Duck
              else (Art > 12.50):
                Predict: Dog
          else (Gardening > 13.50):
            if Swimming <= 12.50:
              Predict: Duck
            else (Swimming > 12.50):
              Predict: Dog

Tree 9 Rules:
if Running <= 4.50:
  if Running <= 3.50:
    if Art <= 0.50:
      Predict: Duck
    else (Art > 0.50):
      if Swimming <= 5.00:
        Predict: Cat
      else (Swimming > 5.00):
        if Swimming <= 10.50:
          if Detective Stories <= 9.50:
            Predict: Dog
          else (Detective Stories > 9.50):
            Predict: Duck
        else (Swimming > 10.50):
          Predict: Cat
  else (Running > 3.50):
    if Gardening <= 4.50:
      Predict: Cat
    else (Gardening > 4.50):
      if Detective Stories <= 11.00:
        if Art <= 5.50:
          Predict: Duck
        else (Art > 5.50):
          Predict: Cat
      else (Detective Stories > 11.00):
        Predict: Cat
else (Running > 4.50):
  if Art <= 4.50:
    if Running <= 9.00:
      if Art <= 1.50:
        Predict: Dog
      else (Art > 1.50):
        if Gardening <= 9.00:
          if Swimming <= 6.50:
            if Art <= 3.50:
              Predict: Duck
            else (Art > 3.50):
              Predict: Dog
          else (Swimming > 6.50):
            Predict: Duck
        else (Gardening > 9.00):
          Predict: Dog
    else (Running > 9.00):
      if Gardening <= 3.50:
        if Swimming <= 11.50:
          if Running <= 11.50:
            Predict: Dog
          else (Running > 11.50):
            Predict: Cat
        else (Swimming > 11.50):
          Predict: Duck
      else (Gardening > 3.50):
        Predict: Dog
  else (Art > 4.50):
    if Swimming <= 4.50:
      if Art <= 9.50:
        if Art <= 8.50:
          if Detective Stories <= 8.50:
            Predict: Dog
          else (Detective Stories > 8.50):
            Predict: Cat
        else (Art > 8.50):
          Predict: Duck
      else (Art > 9.50):
        if Running <= 9.00:
          Predict: Cat
        else (Running > 9.00):
          if Gardening <= 1.00:
            Predict: Duck
          else (Gardening > 1.00):
            Predict: Cat
    else (Swimming > 4.50):
      if Detective Stories <= 1.50:
        Predict: Dog
      else (Detective Stories > 1.50):
        if Gardening <= 4.50:
          if Art <= 7.50:
            if Gardening <= 3.50:
              Predict: Duck
            else (Gardening > 3.50):
              if Running <= 10.50:
                Predict: Duck
              else (Running > 10.50):
                Predict: Dog
          else (Art > 7.50):
            Predict: Duck
        else (Gardening > 4.50):
          if Running <= 9.50:
            if Detective Stories <= 11.50:
              Predict: Duck
            else (Detective Stories > 11.50):
              if Gardening <= 8.50:
                Predict: Duck
              else (Gardening > 8.50):
                if Detective Stories <= 12.50:
                  Predict: Dog
                else (Detective Stories > 12.50):
                  Predict: Duck
          else (Running > 9.50):
            if Art <= 5.50:
              Predict: Duck
            else (Art > 5.50):
              if Detective Stories <= 4.50:
                Predict: Duck
              else (Detective Stories > 4.50):
                if Swimming <= 9.50:
                  Predict: Dog
                else (Swimming > 9.50):
                  if Detective Stories <= 10.50:
                    Predict: Dog
                  else (Detective Stories > 10.50):
                    if Detective Stories <= 12.50:
                      Predict: Duck
                    else (Detective Stories > 12.50):
                      Predict: Dog

Tree 10 Rules:
if Running <= 4.50:
  if Detective Stories <= 2.50:
    if Gardening <= 8.00:
      Predict: Dog
    else (Gardening > 8.00):
      Predict: Cat
  else (Detective Stories > 2.50):
    if Gardening <= 5.50:
      if Art <= 9.50:
        if Swimming <= 7.00:
          Predict: Cat
        else (Swimming > 7.00):
          if Detective Stories <= 13.00:
            Predict: Duck
          else (Detective Stories > 13.00):
            Predict: Dog
      else (Art > 9.50):
        Predict: Cat
    else (Gardening > 5.50):
      if Art <= 5.00:
        Predict: Dog
      else (Art > 5.00):
        Predict: Cat
else (Running > 4.50):
  if Running <= 9.50:
    if Art <= 5.50:
      if Gardening <= 4.50:
        Predict: Duck
      else (Gardening > 4.50):
        if Swimming <= 8.50:
          if Swimming <= 7.50:
            Predict: Dog
          else (Swimming > 7.50):
            if Art <= 1.50:
              Predict: Dog
            else (Art > 1.50):
              Predict: Duck
        else (Swimming > 8.50):
          Predict: Dog
    else (Art > 5.50):
      if Art <= 9.50:
        Predict: Duck
      else (Art > 9.50):
        if Art <= 13.50:
          if Gardening <= 1.50:
            if Swimming <= 5.00:
              Predict: Cat
            else (Swimming > 5.00):
              Predict: Duck
          else (Gardening > 1.50):
            if Running <= 8.50:
              if Gardening <= 4.00:
                Predict: Cat
              else (Gardening > 4.00):
                if Gardening <= 6.00:
                  if Running <= 5.50:
                    Predict: Cat
                  else (Running > 5.50):
                    Predict: Duck
                else (Gardening > 6.00):
                  Predict: Cat
            else (Running > 8.50):
              Predict: Duck
        else (Art > 13.50):
          Predict: Duck
  else (Running > 9.50):
    if Swimming <= 4.50:
      if Running <= 10.50:
        Predict: Duck
      else (Running > 10.50):
        if Gardening <= 1.50:
          Predict: Duck
        else (Gardening > 1.50):
          if Art <= 10.00:
            if Art <= 5.00:
              Predict: Cat
            else (Art > 5.00):
              Predict: Duck
          else (Art > 10.00):
            Predict: Cat
    else (Swimming > 4.50):
      if Gardening <= 3.50:
        if Running <= 10.50:
          if Swimming <= 11.50:
            if Art <= 4.50:
              Predict: Dog
            else (Art > 4.50):
              Predict: Duck
          else (Swimming > 11.50):
            Predict: Duck
        else (Running > 10.50):
          Predict: Duck
      else (Gardening > 3.50):
        if Art <= 7.50:
          Predict: Dog
        else (Art > 7.50):
          if Art <= 12.50:
            if Detective Stories <= 10.50:
              Predict: Dog
            else (Detective Stories > 10.50):
              if Running <= 12.00:
                if Gardening <= 4.50:
                  Predict: Duck
                else (Gardening > 4.50):
                  if Detective Stories <= 12.50:
                    Predict: Duck
                  else (Detective Stories > 12.50):
                    Predict: Dog
              else (Running > 12.00):
                if Running <= 13.50:
                  Predict: Duck
                else (Running > 13.50):
                  if Art <= 9.00:
                    Predict: Duck
                  else (Art > 9.00):
                    Predict: Cat
          else (Art > 12.50):
            Predict: Dog

Tree 11 Rules:
if Swimming <= 4.50:
  if Running <= 8.50:
    if Gardening <= 10.50:
      Predict: Cat
    else (Gardening > 10.50):
      if Swimming <= 1.50:
        Predict: Dog
      else (Swimming > 1.50):
        Predict: Cat
  else (Running > 8.50):
    if Art <= 10.00:
      if Gardening <= 5.00:
        Predict: Duck
      else (Gardening > 5.00):
        Predict: Dog
    else (Art > 10.00):
      if Gardening <= 3.50:
        if Swimming <= 0.50:
          Predict: Duck
        else (Swimming > 0.50):
          if Gardening <= 2.50:
            if Swimming <= 2.50:
              if Art <= 12.00:
                Predict: Duck
              else (Art > 12.00):
                Predict: Cat
            else (Swimming > 2.50):
              Predict: Cat
          else (Gardening > 2.50):
            Predict: Duck
      else (Gardening > 3.50):
        Predict: Cat
else (Swimming > 4.50):
  if Running <= 9.50:
    if Running <= 4.50:
      if Gardening <= 10.50:
        if Art <= 7.50:
          if Gardening <= 6.00:
            if Running <= 3.00:
              Predict: Dog
            else (Running > 3.00):
              Predict: Duck
          else (Gardening > 6.00):
            Predict: Dog
        else (Art > 7.50):
          Predict: Cat
      else (Gardening > 10.50):
        Predict: Dog
    else (Running > 4.50):
      if Detective Stories <= 6.50:
        if Running <= 7.50:
          if Gardening <= 8.50:
            Predict: Duck
          else (Gardening > 8.50):
            Predict: Dog
        else (Running > 7.50):
          if Running <= 8.50:
            Predict: Dog
          else (Running > 8.50):
            Predict: Duck
      else (Detective Stories > 6.50):
        if Swimming <= 6.50:
          if Gardening <= 3.00:
            Predict: Duck
          else (Gardening > 3.00):
            Predict: Dog
        else (Swimming > 6.50):
          if Detective Stories <= 10.50:
            Predict: Duck
          else (Detective Stories > 10.50):
            if Art <= 4.50:
              if Gardening <= 4.00:
                Predict: Duck
              else (Gardening > 4.00):
                Predict: Dog
            else (Art > 4.50):
              if Swimming <= 13.50:
                Predict: Duck
              else (Swimming > 13.50):
                if Gardening <= 5.50:
                  Predict: Duck
                else (Gardening > 5.50):
                  Predict: Dog
  else (Running > 9.50):
    if Gardening <= 4.50:
      if Art <= 3.00:
        Predict: Dog
      else (Art > 3.00):
        if Running <= 13.50:
          Predict: Duck
        else (Running > 13.50):
          if Gardening <= 1.00:
            if Art <= 7.50:
              Predict: Dog
            else (Art > 7.50):
              Predict: Duck
          else (Gardening > 1.00):
            Predict: Duck
    else (Gardening > 4.50):
      if Gardening <= 6.50:
        if Swimming <= 11.50:
          Predict: Dog
        else (Swimming > 11.50):
          if Swimming <= 13.50:
            if Detective Stories <= 11.50:
              if Swimming <= 12.50:
                Predict: Duck
              else (Swimming > 12.50):
                Predict: Cat
            else (Detective Stories > 11.50):
              if Swimming <= 12.50:
                Predict: Dog
              else (Swimming > 12.50):
                Predict: Duck
          else (Swimming > 13.50):
            Predict: Dog
      else (Gardening > 6.50):
        if Running <= 10.50:
          if Gardening <= 12.50:
            Predict: Dog
          else (Gardening > 12.50):
            Predict: Duck
        else (Running > 10.50):
          Predict: Dog

Tree 12 Rules:
if Swimming <= 4.50:
  if Detective Stories <= 5.50:
    if Art <= 4.50:
      Predict: Dog
    else (Art > 4.50):
      Predict: Cat
  else (Detective Stories > 5.50):
    if Gardening <= 4.50:
      if Swimming <= 3.50:
        if Running <= 8.50:
          Predict: Cat
        else (Running > 8.50):
          if Detective Stories <= 11.50:
            if Running <= 9.50:
              Predict: Cat
            else (Running > 9.50):
              if Gardening <= 1.50:
                Predict: Duck
              else (Gardening > 1.50):
                Predict: Cat
          else (Detective Stories > 11.50):
            if Detective Stories <= 12.50:
              Predict: Duck
            else (Detective Stories > 12.50):
              Predict: Cat
      else (Swimming > 3.50):
        if Running <= 9.00:
          Predict: Cat
        else (Running > 9.00):
          if Running <= 12.50:
            Predict: Duck
          else (Running > 12.50):
            Predict: Cat
    else (Gardening > 4.50):
      if Running <= 13.00:
        Predict: Cat
      else (Running > 13.00):
        Predict: Dog
else (Swimming > 4.50):
  if Swimming <= 9.50:
    if Running <= 9.50:
      if Gardening <= 5.00:
        Predict: Duck
      else (Gardening > 5.00):
        if Art <= 5.50:
          Predict: Dog
        else (Art > 5.50):
          Predict: Duck
    else (Running > 9.50):
      if Art <= 6.50:
        Predict: Dog
      else (Art > 6.50):
        if Detective Stories <= 13.50:
          if Gardening <= 4.50:
            if Swimming <= 7.50:
              Predict: Duck
            else (Swimming > 7.50):
              Predict: Dog
          else (Gardening > 4.50):
            if Gardening <= 9.50:
              if Detective Stories <= 11.50:
                if Gardening <= 8.00:
                  Predict: Dog
                else (Gardening > 8.00):
                  Predict: Duck
              else (Detective Stories > 11.50):
                Predict: Dog
            else (Gardening > 9.50):
              Predict: Dog
        else (Detective Stories > 13.50):
          if Running <= 10.50:
            Predict: Duck
          else (Running > 10.50):
            if Gardening <= 8.00:
              Predict: Duck
            else (Gardening > 8.00):
              Predict: Dog
  else (Swimming > 9.50):
    if Running <= 4.50:
      if Running <= 2.50:
        if Art <= 9.50:
          if Art <= 4.50:
            if Gardening <= 4.50:
              Predict: Duck
            else (Gardening > 4.50):
              Predict: Dog
          else (Art > 4.50):
            Predict: Duck
        else (Art > 9.50):
          Predict: Cat
      else (Running > 2.50):
        Predict: Cat
    else (Running > 4.50):
      if Art <= 4.50:
        if Detective Stories <= 5.00:
          if Swimming <= 11.00:
            Predict: Dog
          else (Swimming > 11.00):
            Predict: Duck
        else (Detective Stories > 5.00):
          if Swimming <= 13.50:
            if Art <= 3.50:
              Predict: Dog
            else (Art > 3.50):
              if Swimming <= 11.50:
                Predict: Dog
              else (Swimming > 11.50):
                if Swimming <= 12.50:
                  Predict: Duck
                else (Swimming > 12.50):
                  Predict: Dog
          else (Swimming > 13.50):
            if Running <= 12.50:
              if Detective Stories <= 11.00:
                Predict: Dog
              else (Detective Stories > 11.00):
                Predict: Duck
            else (Running > 12.50):
              Predict: Dog
      else (Art > 4.50):
        if Detective Stories <= 13.50:
          if Gardening <= 5.50:
            Predict: Duck
          else (Gardening > 5.50):
            if Art <= 6.50:
              if Swimming <= 10.50:
                Predict: Duck
              else (Swimming > 10.50):
                Predict: Dog
            else (Art > 6.50):
              if Gardening <= 6.50:
                if Detective Stories <= 9.00:
                  Predict: Dog
                else (Detective Stories > 9.00):
                  Predict: Duck
              else (Gardening > 6.50):
                Predict: Duck
        else (Detective Stories > 13.50):
          if Art <= 11.50:
            Predict: Duck
          else (Art > 11.50):
            if Gardening <= 8.50:
              Predict: Duck
            else (Gardening > 8.50):
              Predict: Dog

Tree 13 Rules:
if Swimming <= 4.50:
  if Art <= 4.50:
    if Gardening <= 5.50:
      Predict: Cat
    else (Gardening > 5.50):
      Predict: Dog
  else (Art > 4.50):
    if Running <= 13.50:
      Predict: Cat
    else (Running > 13.50):
      Predict: Duck
else (Swimming > 4.50):
  if Running <= 9.50:
    if Art <= 4.50:
      if Gardening <= 4.50:
        Predict: Duck
      else (Gardening > 4.50):
        if Art <= 0.50:
          if Detective Stories <= 6.50:
            Predict: Duck
          else (Detective Stories > 6.50):
            Predict: Dog
        else (Art > 0.50):
          Predict: Dog
    else (Art > 4.50):
      if Gardening <= 6.50:
        Predict: Duck
      else (Gardening > 6.50):
        if Swimming <= 13.50:
          if Detective Stories <= 10.50:
            if Running <= 4.50:
              Predict: Cat
            else (Running > 4.50):
              Predict: Duck
          else (Detective Stories > 10.50):
            Predict: Duck
        else (Swimming > 13.50):
          if Gardening <= 9.50:
            Predict: Dog
          else (Gardening > 9.50):
            Predict: Cat
  else (Running > 9.50):
    if Art <= 3.50:
      if Swimming <= 11.50:
        Predict: Dog
      else (Swimming > 11.50):
        if Running <= 11.50:
          if Gardening <= 2.50:
            Predict: Duck
          else (Gardening > 2.50):
            Predict: Dog
        else (Running > 11.50):
          Predict: Dog
    else (Art > 3.50):
      if Gardening <= 3.50:
        if Art <= 4.50:
          if Swimming <= 11.00:
            Predict: Dog
          else (Swimming > 11.00):
            Predict: Duck
        else (Art > 4.50):
          Predict: Duck
      else (Gardening > 3.50):
        if Gardening <= 6.50:
          if Swimming <= 10.50:
            if Art <= 8.00:
              Predict: Dog
            else (Art > 8.00):
              if Swimming <= 8.00:
                Predict: Duck
              else (Swimming > 8.00):
                Predict: Dog
          else (Swimming > 10.50):
            if Running <= 13.50:
              if Art <= 9.00:
                Predict: Dog
              else (Art > 9.00):
                Predict: Duck
            else (Running > 13.50):
              if Swimming <= 12.50:
                Predict: Duck
              else (Swimming > 12.50):
                Predict: Cat
        else (Gardening > 6.50):
          if Art <= 5.50:
            if Running <= 10.50:
              Predict: Duck
            else (Running > 10.50):
              Predict: Dog
          else (Art > 5.50):
            Predict: Dog

Tree 14 Rules:
if Gardening <= 4.50:
  if Art <= 9.50:
    if Art <= 3.50:
      if Swimming <= 4.50:
        if Running <= 8.00:
          Predict: Cat
        else (Running > 8.00):
          Predict: Duck
      else (Swimming > 4.50):
        if Swimming <= 11.50:
          if Detective Stories <= 12.50:
            if Running <= 9.00:
              Predict: Duck
            else (Running > 9.00):
              Predict: Dog
          else (Detective Stories > 12.50):
            Predict: Dog
        else (Swimming > 11.50):
          Predict: Duck
    else (Art > 3.50):
      if Swimming <= 4.00:
        Predict: Cat
      else (Swimming > 4.00):
        if Detective Stories <= 3.00:
          if Gardening <= 3.50:
            Predict: Duck
          else (Gardening > 3.50):
            Predict: Dog
        else (Detective Stories > 3.00):
          if Art <= 6.50:
            Predict: Duck
          else (Art > 6.50):
            if Gardening <= 3.50:
              Predict: Duck
            else (Gardening > 3.50):
              if Detective Stories <= 12.00:
                Predict: Dog
              else (Detective Stories > 12.00):
                Predict: Duck
  else (Art > 9.50):
    if Art <= 12.50:
      if Gardening <= 3.50:
        if Running <= 8.50:
          if Running <= 5.00:
            Predict: Cat
          else (Running > 5.00):
            if Swimming <= 7.50:
              Predict: Cat
            else (Swimming > 7.50):
              Predict: Duck
        else (Running > 8.50):
          Predict: Duck
      else (Gardening > 3.50):
        Predict: Cat
    else (Art > 12.50):
      if Detective Stories <= 5.00:
        Predict: Cat
      else (Detective Stories > 5.00):
        if Gardening <= 3.50:
          if Detective Stories <= 10.50:
            if Swimming <= 3.50:
              Predict: Cat
            else (Swimming > 3.50):
              Predict: Duck
          else (Detective Stories > 10.50):
            if Detective Stories <= 12.50:
              Predict: Duck
            else (Detective Stories > 12.50):
              if Art <= 13.50:
                if Running <= 5.00:
                  Predict: Cat
                else (Running > 5.00):
                  Predict: Duck
              else (Art > 13.50):
                if Running <= 5.00:
                  Predict: Cat
                else (Running > 5.00):
                  Predict: Duck
        else (Gardening > 3.50):
          if Detective Stories <= 13.00:
            Predict: Cat
          else (Detective Stories > 13.00):
            if Swimming <= 6.00:
              Predict: Cat
            else (Swimming > 6.00):
              Predict: Duck
else (Gardening > 4.50):
  if Swimming <= 4.50:
    if Running <= 11.50:
      if Detective Stories <= 5.50:
        if Gardening <= 8.50:
          Predict: Cat
        else (Gardening > 8.50):
          Predict: Dog
      else (Detective Stories > 5.50):
        Predict: Cat
    else (Running > 11.50):
      if Running <= 13.00:
        if Detective Stories <= 13.50:
          Predict: Dog
        else (Detective Stories > 13.50):
          Predict: Cat
      else (Running > 13.00):
        Predict: Dog
  else (Swimming > 4.50):
    if Art <= 5.50:
      if Running <= 6.50:
        if Detective Stories <= 4.00:
          Predict: Duck
        else (Detective Stories > 4.00):
          if Gardening <= 8.50:
            if Swimming <= 12.00:
              if Detective Stories <= 13.00:
                Predict: Duck
              else (Detective Stories > 13.00):
                Predict: Dog
            else (Swimming > 12.00):
              Predict: Dog
          else (Gardening > 8.50):
            Predict: Dog
      else (Running > 6.50):
        Predict: Dog
    else (Art > 5.50):
      if Swimming <= 9.50:
        if Running <= 9.00:
          Predict: Duck
        else (Running > 9.00):
          Predict: Dog
      else (Swimming > 9.50):
        if Art <= 12.50:
          if Detective Stories <= 13.50:
            if Running <= 10.00:
              Predict: Duck
            else (Running > 10.00):
              if Detective Stories <= 10.50:
                Predict: Dog
              else (Detective Stories > 10.50):
                if Art <= 7.00:
                  Predict: Dog
                else (Art > 7.00):
                  if Gardening <= 5.50:
                    if Swimming <= 13.50:
                      Predict: Cat
                    else (Swimming > 13.50):
                      Predict: Duck
                  else (Gardening > 5.50):
                    Predict: Duck
          else (Detective Stories > 13.50):
            Predict: Cat
        else (Art > 12.50):
          if Running <= 9.00:
            if Gardening <= 9.50:
              if Running <= 3.00:
                Predict: Cat
              else (Running > 3.00):
                Predict: Duck
            else (Gardening > 9.50):
              Predict: Cat
          else (Running > 9.00):
            Predict: Dog

Tree 15 Rules:
if Art <= 9.50:
  if Art <= 4.50:
    if Swimming <= 11.50:
      if Gardening <= 3.50:
        if Swimming <= 4.50:
          Predict: Cat
        else (Swimming > 4.50):
          Predict: Duck
      else (Gardening > 3.50):
        Predict: Dog
    else (Swimming > 11.50):
      if Detective Stories <= 13.50:
        if Gardening <= 4.00:
          Predict: Duck
        else (Gardening > 4.00):
          if Running <= 5.00:
            if Gardening <= 6.00:
              Predict: Duck
            else (Gardening > 6.00):
              Predict: Dog
          else (Running > 5.00):
            Predict: Dog
      else (Detective Stories > 13.50):
        Predict: Dog
  else (Art > 4.50):
    if Swimming <= 4.50:
      if Swimming <= 1.50:
        if Art <= 5.50:
          Predict: Dog
        else (Art > 5.50):
          if Running <= 6.50:
            Predict: Cat
          else (Running > 6.50):
            Predict: Duck
      else (Swimming > 1.50):
        Predict: Cat
    else (Swimming > 4.50):
      if Gardening <= 4.50:
        if Gardening <= 3.50:
          Predict: Duck
        else (Gardening > 3.50):
          if Running <= 11.00:
            Predict: Duck
          else (Running > 11.00):
            Predict: Dog
      else (Gardening > 4.50):
        if Running <= 9.00:
          if Art <= 6.50:
            if Running <= 5.00:
              Predict: Duck
            else (Running > 5.00):
              Predict: Dog
          else (Art > 6.50):
            Predict: Duck
        else (Running > 9.00):
          Predict: Dog
else (Art > 9.50):
  if Running <= 4.50:
    Predict: Cat
  else (Running > 4.50):
    if Gardening <= 11.50:
      if Detective Stories <= 12.50:
        if Running <= 9.50:
          if Detective Stories <= 10.50:
            if Swimming <= 5.00:
              Predict: Cat
            else (Swimming > 5.00):
              Predict: Duck
          else (Detective Stories > 10.50):
            if Gardening <= 2.50:
              Predict: Duck
            else (Gardening > 2.50):
              if Running <= 5.50:
                Predict: Cat
              else (Running > 5.50):
                if Detective Stories <= 11.50:
                  Predict: Duck
                else (Detective Stories > 11.50):
                  Predict: Cat
        else (Running > 9.50):
          if Detective Stories <= 10.50:
            Predict: Duck
          else (Detective Stories > 10.50):
            if Art <= 13.50:
              if Running <= 13.50:
                if Swimming <= 5.00:
                  if Art <= 12.00:
                    Predict: Cat
                  else (Art > 12.00):
                    Predict: Duck
                else (Swimming > 5.00):
                  Predict: Duck
              else (Running > 13.50):
                Predict: Cat
            else (Art > 13.50):
              Predict: Cat
      else (Detective Stories > 12.50):
        if Gardening <= 3.50:
          Predict: Duck
        else (Gardening > 3.50):
          if Art <= 12.00:
            if Gardening <= 6.50:
              if Detective Stories <= 13.50:
                Predict: Cat
              else (Detective Stories > 13.50):
                Predict: Duck
            else (Gardening > 6.50):
              Predict: Cat
          else (Art > 12.00):
            if Swimming <= 5.50:
              Predict: Cat
            else (Swimming > 5.50):
              Predict: Duck
    else (Gardening > 11.50):
      Predict: Dog

Tree 16 Rules:
if Art <= 4.50:
  if Running <= 4.50:
    if Gardening <= 3.50:
      Predict: Cat
    else (Gardening > 3.50):
      if Art <= 2.50:
        if Running <= 3.00:
          if Running <= 1.50:
            Predict: Duck
          else (Running > 1.50):
            Predict: Dog
        else (Running > 3.00):
          Predict: Duck
      else (Art > 2.50):
        Predict: Dog
  else (Running > 4.50):
    if Running <= 8.50:
      if Detective Stories <= 4.50:
        Predict: Duck
      else (Detective Stories > 4.50):
        if Gardening <= 2.50:
          Predict: Duck
        else (Gardening > 2.50):
          if Gardening <= 8.50:
            if Swimming <= 8.50:
              if Running <= 6.50:
                Predict: Duck
              else (Running > 6.50):
                Predict: Dog
            else (Swimming > 8.50):
              Predict: Dog
          else (Gardening > 8.50):
            Predict: Dog
    else (Running > 8.50):
      if Swimming <= 4.50:
        if Art <= 2.50:
          Predict: Dog
        else (Art > 2.50):
          Predict: Duck
      else (Swimming > 4.50):
        if Gardening <= 0.50:
          Predict: Duck
        else (Gardening > 0.50):
          if Art <= 3.50:
            Predict: Dog
          else (Art > 3.50):
            if Gardening <= 3.00:
              Predict: Duck
            else (Gardening > 3.00):
              Predict: Dog
else (Art > 4.50):
  if Swimming <= 4.50:
    if Running <= 9.50:
      if Detective Stories <= 5.50:
        if Art <= 8.00:
          Predict: Dog
        else (Art > 8.00):
          Predict: Cat
      else (Detective Stories > 5.50):
        Predict: Cat
    else (Running > 9.50):
      Predict: Duck
  else (Swimming > 4.50):
    if Running <= 4.50:
      if Art <= 9.50:
        Predict: Duck
      else (Art > 9.50):
        Predict: Cat
    else (Running > 4.50):
      if Gardening <= 4.50:
        Predict: Duck
      else (Gardening > 4.50):
        if Detective Stories <= 13.50:
          if Swimming <= 8.50:
            if Detective Stories <= 6.50:
              Predict: Duck
            else (Detective Stories > 6.50):
              if Running <= 8.00:
                Predict: Duck
              else (Running > 8.00):
                Predict: Dog
          else (Swimming > 8.50):
            if Detective Stories <= 6.50:
              if Swimming <= 12.00:
                if Art <= 5.50:
                  Predict: Dog
                else (Art > 5.50):
                  Predict: Duck
              else (Swimming > 12.00):
                Predict: Dog
            else (Detective Stories > 6.50):
              if Running <= 11.50:
                if Gardening <= 11.50:
                  Predict: Duck
                else (Gardening > 11.50):
                  if Art <= 10.50:
                    Predict: Duck
                  else (Art > 10.50):
                    Predict: Dog
              else (Running > 11.50):
                if Art <= 9.00:
                  Predict: Dog
                else (Art > 9.00):
                  Predict: Duck
        else (Detective Stories > 13.50):
          Predict: Dog

Tree 17 Rules:
if Art <= 4.50:
  if Running <= 4.50:
    if Swimming <= 5.00:
      Predict: Cat
    else (Swimming > 5.00):
      Predict: Dog
  else (Running > 4.50):
    if Swimming <= 13.50:
      if Gardening <= 4.50:
        if Art <= 1.50:
          if Swimming <= 6.00:
            Predict: Cat
          else (Swimming > 6.00):
            Predict: Dog
        else (Art > 1.50):
          Predict: Duck
      else (Gardening > 4.50):
        if Gardening <= 6.50:
          if Detective Stories <= 4.50:
            Predict: Duck
          else (Detective Stories > 4.50):
            Predict: Dog
        else (Gardening > 6.50):
          Predict: Dog
    else (Swimming > 13.50):
      if Art <= 2.00:
        Predict: Duck
      else (Art > 2.00):
        Predict: Dog
else (Art > 4.50):
  if Running <= 4.50:
    if Art <= 6.50:
      if Swimming <= 7.00:
        Predict: Cat
      else (Swimming > 7.00):
        Predict: Duck
    else (Art > 6.50):
      if Art <= 9.50:
        if Swimming <= 7.00:
          Predict: Cat
        else (Swimming > 7.00):
          Predict: Duck
      else (Art > 9.50):
        Predict: Cat
  else (Running > 4.50):
    if Running <= 10.50:
      if Art <= 9.50:
        if Swimming <= 4.50:
          Predict: Cat
        else (Swimming > 4.50):
          if Gardening <= 4.50:
            Predict: Duck
          else (Gardening > 4.50):
            if Art <= 7.50:
              if Gardening <= 7.00:
                Predict: Dog
              else (Gardening > 7.00):
                if Swimming <= 13.00:
                  if Swimming <= 11.00:
                    if Running <= 7.50:
                      Predict: Dog
                    else (Running > 7.50):
                      Predict: Duck
                  else (Swimming > 11.00):
                    Predict: Duck
                else (Swimming > 13.00):
                  Predict: Dog
            else (Art > 7.50):
              Predict: Duck
      else (Art > 9.50):
        if Running <= 9.50:
          if Gardening <= 2.50:
            if Swimming <= 5.00:
              Predict: Cat
            else (Swimming > 5.00):
              Predict: Duck
          else (Gardening > 2.50):
            if Swimming <= 6.50:
              Predict: Cat
            else (Swimming > 6.50):
              Predict: Duck
        else (Running > 9.50):
          Predict: Duck
    else (Running > 10.50):
      if Gardening <= 4.50:
        if Swimming <= 5.00:
          if Running <= 13.50:
            if Gardening <= 1.50:
              Predict: Duck
            else (Gardening > 1.50):
              Predict: Cat
          else (Running > 13.50):
            Predict: Duck
        else (Swimming > 5.00):
          if Gardening <= 3.50:
            Predict: Duck
          else (Gardening > 3.50):
            if Running <= 12.50:
              Predict: Duck
            else (Running > 12.50):
              Predict: Dog
      else (Gardening > 4.50):
        if Swimming <= 9.50:
          if Swimming <= 4.50:
            if Art <= 8.50:
              Predict: Duck
            else (Art > 8.50):
              Predict: Cat
          else (Swimming > 4.50):
            if Art <= 10.50:
              Predict: Dog
            else (Art > 10.50):
              if Detective Stories <= 11.50:
                if Running <= 12.00:
                  Predict: Duck
                else (Running > 12.00):
                  Predict: Dog
              else (Detective Stories > 11.50):
                Predict: Dog
        else (Swimming > 9.50):
          if Detective Stories <= 8.50:
            if Art <= 7.00:
              Predict: Duck
            else (Art > 7.00):
              Predict: Dog
          else (Detective Stories > 8.50):
            if Detective Stories <= 12.00:
              Predict: Duck
            else (Detective Stories > 12.00):
              Predict: Dog

Tree 18 Rules:
if Gardening <= 4.50:
  if Swimming <= 4.50:
    if Running <= 8.50:
      Predict: Cat
    else (Running > 8.50):
      if Art <= 2.00:
        Predict: Cat
      else (Art > 2.00):
        if Detective Stories <= 5.00:
          Predict: Cat
        else (Detective Stories > 5.00):
          if Running <= 12.50:
            if Art <= 10.00:
              Predict: Duck
            else (Art > 10.00):
              if Detective Stories <= 10.50:
                if Running <= 9.50:
                  Predict: Cat
                else (Running > 9.50):
                  Predict: Duck
              else (Detective Stories > 10.50):
                Predict: Cat
          else (Running > 12.50):
            Predict: Cat
  else (Swimming > 4.50):
    if Gardening <= 3.50:
      if Running <= 3.50:
        if Art <= 9.50:
          Predict: Duck
        else (Art > 9.50):
          Predict: Cat
      else (Running > 3.50):
        if Running <= 13.50:
          Predict: Duck
        else (Running > 13.50):
          if Detective Stories <= 11.00:
            Predict: Duck
          else (Detective Stories > 11.00):
            if Detective Stories <= 13.00:
              Predict: Dog
            else (Detective Stories > 13.00):
              Predict: Duck
    else (Gardening > 3.50):
      if Running <= 12.50:
        Predict: Duck
      else (Running > 12.50):
        Predict: Dog
else (Gardening > 4.50):
  if Swimming <= 4.50:
    if Detective Stories <= 6.50:
      if Gardening <= 6.50:
        if Running <= 8.00:
          Predict: Cat
        else (Running > 8.00):
          Predict: Duck
      else (Gardening > 6.50):
        if Art <= 4.50:
          Predict: Dog
        else (Art > 4.50):
          Predict: Cat
    else (Detective Stories > 6.50):
      Predict: Cat
  else (Swimming > 4.50):
    if Swimming <= 9.50:
      if Art <= 5.50:
        if Running <= 6.50:
          if Art <= 2.50:
            Predict: Dog
          else (Art > 2.50):
            if Gardening <= 11.00:
              Predict: Duck
            else (Gardening > 11.00):
              Predict: Dog
        else (Running > 6.50):
          Predict: Dog
      else (Art > 5.50):
        if Running <= 9.50:
          Predict: Duck
        else (Running > 9.50):
          Predict: Dog
    else (Swimming > 9.50):
      if Detective Stories <= 12.50:
        if Gardening <= 8.50:
          if Detective Stories <= 6.50:
            Predict: Dog
          else (Detective Stories > 6.50):
            if Art <= 4.50:
              if Running <= 5.00:
                Predict: Duck
              else (Running > 5.00):
                Predict: Dog
            else (Art > 4.50):
              if Art <= 9.50:
                Predict: Duck
              else (Art > 9.50):
                if Detective Stories <= 11.50:
                  if Gardening <= 6.50:
                    if Swimming <= 12.00:
                      Predict: Duck
                    else (Swimming > 12.00):
                      if Running <= 13.50:
                        Predict: Duck
                      else (Running > 13.50):
                        Predict: Cat
                  else (Gardening > 6.50):
                    Predict: Cat
                else (Detective Stories > 11.50):
                  Predict: Duck
        else (Gardening > 8.50):
          if Art <= 5.50:
            Predict: Dog
          else (Art > 5.50):
            if Art <= 7.50:
              if Running <= 10.00:
                Predict: Duck
              else (Running > 10.00):
                if Gardening <= 12.00:
                  Predict: Dog
                else (Gardening > 12.00):
                  Predict: Duck
            else (Art > 7.50):
              if Gardening <= 9.50:
                Predict: Dog
              else (Gardening > 9.50):
                if Swimming <= 13.50:
                  Predict: Dog
                else (Swimming > 13.50):
                  Predict: Cat
      else (Detective Stories > 12.50):
        if Art <= 13.50:
          if Swimming <= 10.50:
            if Art <= 6.00:
              Predict: Dog
            else (Art > 6.00):
              Predict: Duck
          else (Swimming > 10.50):
            Predict: Dog
        else (Art > 13.50):
          Predict: Duck

Tree 19 Rules:
if Gardening <= 4.50:
  if Swimming <= 4.50:
    if Detective Stories <= 10.50:
      if Running <= 9.50:
        Predict: Cat
      else (Running > 9.50):
        if Art <= 2.00:
          Predict: Cat
        else (Art > 2.00):
          Predict: Duck
    else (Detective Stories > 10.50):
      if Running <= 13.50:
        if Swimming <= 1.50:
          if Art <= 9.50:
            Predict: Duck
          else (Art > 9.50):
            Predict: Cat
        else (Swimming > 1.50):
          Predict: Cat
      else (Running > 13.50):
        Predict: Duck
  else (Swimming > 4.50):
    if Art <= 11.50:
      if Art <= 2.50:
        if Detective Stories <= 11.00:
          if Swimming <= 11.50:
            Predict: Dog
          else (Swimming > 11.50):
            Predict: Duck
        else (Detective Stories > 11.00):
          Predict: Duck
      else (Art > 2.50):
        Predict: Duck
    else (Art > 11.50):
      if Running <= 4.50:
        Predict: Cat
      else (Running > 4.50):
        Predict: Duck
else (Gardening > 4.50):
  if Art <= 9.50:
    if Running <= 3.50:
      if Swimming <= 5.00:
        if Art <= 4.00:
          Predict: Dog
        else (Art > 4.00):
          Predict: Cat
      else (Swimming > 5.00):
        Predict: Dog
    else (Running > 3.50):
      if Detective Stories <= 4.50:
        if Art <= 1.00:
          Predict: Duck
        else (Art > 1.00):
          if Detective Stories <= 2.50:
            Predict: Dog
          else (Detective Stories > 2.50):
            if Art <= 7.00:
              Predict: Duck
            else (Art > 7.00):
              Predict: Dog
      else (Detective Stories > 4.50):
        if Art <= 4.50:
          if Swimming <= 13.50:
            if Running <= 6.50:
              if Swimming <= 7.50:
                Predict: Dog
              else (Swimming > 7.50):
                if Swimming <= 8.50:
                  Predict: Duck
                else (Swimming > 8.50):
                  Predict: Dog
            else (Running > 6.50):
              Predict: Dog
          else (Swimming > 13.50):
            if Gardening <= 6.50:
              if Running <= 7.00:
                Predict: Duck
              else (Running > 7.00):
                Predict: Dog
            else (Gardening > 6.50):
              Predict: Dog
        else (Art > 4.50):
          if Gardening <= 9.50:
            if Swimming <= 11.50:
              if Running <= 5.00:
                Predict: Duck
              else (Running > 5.00):
                if Running <= 9.00:
                  if Art <= 6.00:
                    Predict: Dog
                  else (Art > 6.00):
                    Predict: Duck
                else (Running > 9.00):
                  if Detective Stories <= 8.00:
                    Predict: Duck
                  else (Detective Stories > 8.00):
                    Predict: Dog
            else (Swimming > 11.50):
              Predict: Dog
          else (Gardening > 9.50):
            if Running <= 10.50:
              if Gardening <= 13.50:
                if Art <= 5.50:
                  Predict: Dog
                else (Art > 5.50):
                  if Swimming <= 8.50:
                    if Gardening <= 11.50:
                      Predict: Duck
                    else (Gardening > 11.50):
                      Predict: Dog
                  else (Swimming > 8.50):
                    Predict: Duck
              else (Gardening > 13.50):
                Predict: Duck
            else (Running > 10.50):
              Predict: Dog
  else (Art > 9.50):
    if Running <= 9.00:
      if Running <= 7.50:
        Predict: Cat
      else (Running > 7.50):
        if Detective Stories <= 11.50:
          Predict: Cat
        else (Detective Stories > 11.50):
          Predict: Duck
    else (Running > 9.00):
      if Detective Stories <= 12.00:
        if Gardening <= 8.00:
          if Detective Stories <= 10.50:
            Predict: Dog
          else (Detective Stories > 10.50):
            if Running <= 13.50:
              Predict: Duck
            else (Running > 13.50):
              Predict: Cat
        else (Gardening > 8.00):
          Predict: Duck
      else (Detective Stories > 12.00):
        if Art <= 11.50:
          Predict: Cat
        else (Art > 11.50):
          Predict: Dog

Aggregated Rules (Across Forest):

Class: Dog

Simple Rules (Single Trees):

Tree 0 Rules:
if Swimming <= 4.50:
  if Detective Stories <= 2.50:
    if Swimming <= 2.50:
      Predict: Dog
    else (Swimming > 2.50):
      Predict: Cat
  else (Detective Stories > 2.50):
    if Running <= 8.50:
      if Swimming <= 0.50:
        if Detective Stories <= 6.00:
          Predict: Dog
        else (Detective Stories > 6.00):
          Predict: Cat
      else (Swimming > 0.50):
        Predict: Cat
    else (Running > 8.50):
      if Detective Stories <= 8.00:
        Predict: Dog
      else (Detective Stories > 8.00):
        if Gardening <= 1.50:
          if Gardening <= 0.50:
            if Art <= 12.00:
              Predict: Duck
            else (Art > 12.00):
              Predict: Cat
          else (Gardening > 0.50):
            Predict: Duck
        else (Gardening > 1.50):
          if Gardening <= 3.50:
            if Swimming <= 2.00:
              Predict: Duck
            else (Swimming > 2.00):
              if Detective Stories <= 11.00:
                if Detective Stories <= 9.50:
                  Predict: Cat
                else (Detective Stories > 9.50):
                  Predict: Duck
              else (Detective Stories > 11.00):
                Predict: Cat
          else (Gardening > 3.50):
            Predict: Cat
else (Swimming > 4.50):
  if Art <= 4.50:
    if Running <= 6.50:
      if Gardening <= 6.50:
        Predict: Duck
      else (Gardening > 6.50):
        if Detective Stories <= 9.00:
          Predict: Dog
        else (Detective Stories > 9.00):
          if Detective Stories <= 13.00:
            if Gardening <= 11.00:
              Predict: Duck
            else (Gardening > 11.00):
              Predict: Dog
          else (Detective Stories > 13.00):
            Predict: Dog
    else (Running > 6.50):
      if Gardening <= 3.50:
        Predict: Duck
      else (Gardening > 3.50):
        Predict: Dog
  else (Art > 4.50):
    if Gardening <= 9.50:
      if Swimming <= 7.50:
        if Detective Stories <= 8.00:
          Predict: Duck
        else (Detective Stories > 8.00):
          if Art <= 6.50:
            Predict: Dog
          else (Art > 6.50):
            if Art <= 13.50:
              if Gardening <= 4.50:
                Predict: Duck
              else (Gardening > 4.50):
                if Swimming <= 6.50:
                  if Art <= 10.00:
                    Predict: Duck
                  else (Art > 10.00):
                    Predict: Dog
                else (Swimming > 6.50):
                  Predict: Dog
            else (Art > 13.50):
              Predict: Duck
      else (Swimming > 7.50):
        if Running <= 13.50:
          if Running <= 4.50:
            if Swimming <= 10.50:
              Predict: Duck
            else (Swimming > 10.50):
              if Detective Stories <= 10.50:
                if Gardening <= 6.50:
                  Predict: Duck
                else (Gardening > 6.50):
                  Predict: Cat
              else (Detective Stories > 10.50):
                Predict: Cat
          else (Running > 4.50):
            if Swimming <= 9.50:
              if Gardening <= 4.50:
                Predict: Duck
              else (Gardening > 4.50):
                if Gardening <= 7.50:
                  Predict: Dog
                else (Gardening > 7.50):
                  Predict: Duck
            else (Swimming > 9.50):
              Predict: Duck
        else (Running > 13.50):
          if Gardening <= 4.00:
            Predict: Duck
          else (Gardening > 4.00):
            if Swimming <= 12.50:
              Predict: Dog
            else (Swimming > 12.50):
              Predict: Cat
    else (Gardening > 9.50):
      if Swimming <= 9.50:
        Predict: Dog
      else (Swimming > 9.50):
        if Detective Stories <= 12.00:
          if Swimming <= 12.50:
            Predict: Duck
          else (Swimming > 12.50):
            if Running <= 9.00:
              Predict: Duck
            else (Running > 9.00):
              Predict: Dog
        else (Detective Stories > 12.00):
          Predict: Dog

Tree 1 Rules:
if Art <= 4.50:
  if Running <= 4.50:
    if Art <= 0.50:
      Predict: Cat
    else (Art > 0.50):
      if Swimming <= 5.00:
        if Detective Stories <= 4.00:
          Predict: Dog
        else (Detective Stories > 4.00):
          Predict: Cat
      else (Swimming > 5.00):
        if Running <= 3.00:
          Predict: Dog
        else (Running > 3.00):
          if Art <= 1.50:
            Predict: Dog
          else (Art > 1.50):
            if Art <= 3.00:
              Predict: Duck
            else (Art > 3.00):
              Predict: Dog
  else (Running > 4.50):
    if Swimming <= 3.50:
      Predict: Cat
    else (Swimming > 3.50):
      if Swimming <= 11.50:
        if Gardening <= 4.50:
          if Detective Stories <= 11.00:
            Predict: Duck
          else (Detective Stories > 11.00):
            Predict: Dog
        else (Gardening > 4.50):
          if Running <= 6.50:
            if Gardening <= 10.50:
              Predict: Duck
            else (Gardening > 10.50):
              Predict: Dog
          else (Running > 6.50):
            Predict: Dog
      else (Swimming > 11.50):
        if Detective Stories <= 5.50:
          Predict: Duck
        else (Detective Stories > 5.50):
          if Running <= 10.50:
            if Gardening <= 4.00:
              Predict: Duck
            else (Gardening > 4.00):
              Predict: Dog
          else (Running > 10.50):
            if Gardening <= 4.00:
              Predict: Duck
            else (Gardening > 4.00):
              Predict: Dog
else (Art > 4.50):
  if Swimming <= 4.50:
    if Running <= 8.50:
      if Swimming <= 0.50:
        if Detective Stories <= 6.00:
          Predict: Dog
        else (Detective Stories > 6.00):
          Predict: Cat
      else (Swimming > 0.50):
        Predict: Cat
    else (Running > 8.50):
      if Running <= 10.50:
        if Detective Stories <= 5.50:
          Predict: Cat
        else (Detective Stories > 5.50):
          Predict: Duck
      else (Running > 10.50):
        if Art <= 8.50:
          Predict: Duck
        else (Art > 8.50):
          Predict: Cat
  else (Swimming > 4.50):
    if Gardening <= 5.50:
      if Running <= 13.50:
        if Swimming <= 12.50:
          Predict: Duck
        else (Swimming > 12.50):
          if Running <= 5.00:
            Predict: Cat
          else (Running > 5.00):
            Predict: Duck
      else (Running > 13.50):
        if Detective Stories <= 10.50:
          Predict: Duck
        else (Detective Stories > 10.50):
          if Art <= 8.50:
            Predict: Dog
          else (Art > 8.50):
            Predict: Cat
    else (Gardening > 5.50):
      if Running <= 4.50:
        Predict: Cat
      else (Running > 4.50):
        if Detective Stories <= 2.00:
          Predict: Dog
        else (Detective Stories > 2.00):
          if Running <= 9.50:
            if Swimming <= 10.50:
              if Art <= 5.50:
                Predict: Dog
              else (Art > 5.50):
                Predict: Duck
            else (Swimming > 10.50):
              Predict: Duck
          else (Running > 9.50):
            if Swimming <= 8.50:
              Predict: Dog
            else (Swimming > 8.50):
              if Running <= 10.50:
                Predict: Dog
              else (Running > 10.50):
                if Gardening <= 13.50:
                  if Detective Stories <= 7.50:
                    if Detective Stories <= 4.50:
                      Predict: Duck
                    else (Detective Stories > 4.50):
                      Predict: Dog
                  else (Detective Stories > 7.50):
                    Predict: Duck
                else (Gardening > 13.50):
                  Predict: Dog

Tree 2 Rules:
if Art <= 4.50:
  if Gardening <= 4.50:
    if Swimming <= 4.50:
      if Running <= 8.00:
        Predict: Cat
      else (Running > 8.00):
        Predict: Duck
    else (Swimming > 4.50):
      if Running <= 13.00:
        if Gardening <= 0.50:
          Predict: Duck
        else (Gardening > 0.50):
          if Running <= 8.50:
            Predict: Duck
          else (Running > 8.50):
            if Detective Stories <= 10.50:
              Predict: Dog
            else (Detective Stories > 10.50):
              Predict: Duck
      else (Running > 13.00):
        Predict: Dog
  else (Gardening > 4.50):
    if Swimming <= 13.50:
      if Running <= 6.50:
        if Running <= 5.50:
          Predict: Dog
        else (Running > 5.50):
          if Art <= 2.50:
            Predict: Dog
          else (Art > 2.50):
            if Swimming <= 10.00:
              if Swimming <= 4.00:
                Predict: Dog
              else (Swimming > 4.00):
                Predict: Duck
            else (Swimming > 10.00):
              Predict: Dog
      else (Running > 6.50):
        Predict: Dog
    else (Swimming > 13.50):
      if Running <= 7.00:
        Predict: Duck
      else (Running > 7.00):
        Predict: Dog
else (Art > 4.50):
  if Art <= 9.50:
    if Swimming <= 4.50:
      if Art <= 8.50:
        if Running <= 10.50:
          Predict: Cat
        else (Running > 10.50):
          Predict: Duck
      else (Art > 8.50):
        if Gardening <= 6.00:
          Predict: Duck
        else (Gardening > 6.00):
          Predict: Cat
    else (Swimming > 4.50):
      if Detective Stories <= 6.50:
        if Swimming <= 13.00:
          if Art <= 5.50:
            Predict: Dog
          else (Art > 5.50):
            if Gardening <= 9.50:
              Predict: Duck
            else (Gardening > 9.50):
              if Swimming <= 10.50:
                Predict: Duck
              else (Swimming > 10.50):
                Predict: Dog
        else (Swimming > 13.00):
          Predict: Dog
      else (Detective Stories > 6.50):
        if Running <= 9.50:
          if Gardening <= 10.50:
            Predict: Duck
          else (Gardening > 10.50):
            if Swimming <= 10.50:
              Predict: Dog
            else (Swimming > 10.50):
              Predict: Duck
        else (Running > 9.50):
          if Art <= 6.50:
            if Gardening <= 4.50:
              Predict: Duck
            else (Gardening > 4.50):
              Predict: Dog
          else (Art > 6.50):
            if Gardening <= 9.50:
              if Running <= 10.50:
                if Swimming <= 8.50:
                  Predict: Duck
                else (Swimming > 8.50):
                  Predict: Dog
              else (Running > 10.50):
                Predict: Duck
            else (Gardening > 9.50):
              Predict: Dog
  else (Art > 9.50):
    if Detective Stories <= 2.50:
      if Swimming <= 7.00:
        Predict: Cat
      else (Swimming > 7.00):
        Predict: Duck
    else (Detective Stories > 2.50):
      if Running <= 4.50:
        Predict: Cat
      else (Running > 4.50):
        if Swimming <= 7.50:
          if Gardening <= 11.50:
            if Gardening <= 2.00:
              if Swimming <= 2.50:
                Predict: Duck
              else (Swimming > 2.50):
                Predict: Cat
            else (Gardening > 2.00):
              Predict: Cat
          else (Gardening > 11.50):
            Predict: Dog
        else (Swimming > 7.50):
          if Swimming <= 9.50:
            if Gardening <= 4.50:
              Predict: Duck
            else (Gardening > 4.50):
              if Running <= 12.00:
                Predict: Duck
              else (Running > 12.00):
                Predict: Dog
          else (Swimming > 9.50):
            Predict: Duck

Tree 3 Rules:
if Running <= 4.50:
  if Gardening <= 10.00:
    if Art <= 5.50:
      if Swimming <= 10.50:
        if Gardening <= 4.00:
          Predict: Cat
        else (Gardening > 4.00):
          if Detective Stories <= 11.50:
            Predict: Cat
          else (Detective Stories > 11.50):
            Predict: Dog
      else (Swimming > 10.50):
        if Detective Stories <= 6.00:
          Predict: Dog
        else (Detective Stories > 6.00):
          Predict: Duck
    else (Art > 5.50):
      if Swimming <= 7.00:
        Predict: Cat
      else (Swimming > 7.00):
        if Running <= 2.50:
          if Gardening <= 2.50:
            Predict: Duck
          else (Gardening > 2.50):
            if Swimming <= 12.50:
              if Art <= 10.00:
                Predict: Duck
              else (Art > 10.00):
                Predict: Cat
            else (Swimming > 12.50):
              Predict: Cat
        else (Running > 2.50):
          Predict: Cat
  else (Gardening > 10.00):
    if Detective Stories <= 9.50:
      Predict: Dog
    else (Detective Stories > 9.50):
      if Art <= 5.50:
        Predict: Dog
      else (Art > 5.50):
        Predict: Cat
else (Running > 4.50):
  if Art <= 4.50:
    if Running <= 6.50:
      if Detective Stories <= 3.50:
        Predict: Duck
      else (Detective Stories > 3.50):
        Predict: Dog
    else (Running > 6.50):
      if Swimming <= 13.50:
        Predict: Dog
      else (Swimming > 13.50):
        if Running <= 12.50:
          if Gardening <= 5.50:
            Predict: Duck
          else (Gardening > 5.50):
            Predict: Dog
        else (Running > 12.50):
          Predict: Dog
  else (Art > 4.50):
    if Running <= 10.50:
      if Gardening <= 4.50:
        if Swimming <= 5.00:
          if Art <= 11.50:
            Predict: Duck
          else (Art > 11.50):
            if Running <= 9.50:
              Predict: Cat
            else (Running > 9.50):
              Predict: Duck
        else (Swimming > 5.00):
          Predict: Duck
      else (Gardening > 4.50):
        if Swimming <= 4.00:
          if Gardening <= 8.00:
            Predict: Cat
          else (Gardening > 8.00):
            if Art <= 7.50:
              Predict: Dog
            else (Art > 7.50):
              Predict: Cat
        else (Swimming > 4.00):
          if Art <= 5.50:
            if Running <= 8.50:
              Predict: Dog
            else (Running > 8.50):
              Predict: Duck
          else (Art > 5.50):
            if Detective Stories <= 3.00:
              Predict: Dog
            else (Detective Stories > 3.00):
              Predict: Duck
    else (Running > 10.50):
      if Gardening <= 4.50:
        if Swimming <= 4.50:
          if Gardening <= 3.50:
            Predict: Cat
          else (Gardening > 3.50):
            if Running <= 13.50:
              Predict: Cat
            else (Running > 13.50):
              Predict: Duck
        else (Swimming > 4.50):
          Predict: Duck
      else (Gardening > 4.50):
        if Art <= 9.50:
          if Gardening <= 6.50:
            if Running <= 13.50:
              Predict: Dog
            else (Running > 13.50):
              Predict: Duck
          else (Gardening > 6.50):
            if Detective Stories <= 4.50:
              if Art <= 7.00:
                Predict: Duck
              else (Art > 7.00):
                Predict: Dog
            else (Detective Stories > 4.50):
              Predict: Dog
        else (Art > 9.50):
          if Swimming <= 5.00:
            Predict: Cat
          else (Swimming > 5.00):
            if Detective Stories <= 12.00:
              if Running <= 12.00:
                Predict: Duck
              else (Running > 12.00):
                if Running <= 13.50:
                  Predict: Dog
                else (Running > 13.50):
                  Predict: Cat
            else (Detective Stories > 12.00):
              Predict: Dog

Tree 4 Rules:
if Swimming <= 4.50:
  if Running <= 13.50:
    if Running <= 9.00:
      if Detective Stories <= 5.50:
        if Running <= 4.00:
          Predict: Cat
        else (Running > 4.00):
          Predict: Dog
      else (Detective Stories > 5.50):
        Predict: Cat
    else (Running > 9.00):
      if Running <= 10.50:
        Predict: Duck
      else (Running > 10.50):
        if Art <= 12.00:
          if Art <= 7.50:
            if Gardening <= 3.00:
              Predict: Cat
            else (Gardening > 3.00):
              if Gardening <= 6.50:
                Predict: Duck
              else (Gardening > 6.50):
                Predict: Dog
          else (Art > 7.50):
            Predict: Cat
        else (Art > 12.00):
          if Gardening <= 3.00:
            Predict: Duck
          else (Gardening > 3.00):
            Predict: Cat
  else (Running > 13.50):
    Predict: Duck
else (Swimming > 4.50):
  if Gardening <= 4.50:
    if Running <= 13.50:
      if Running <= 3.50:
        if Detective Stories <= 11.00:
          Predict: Cat
        else (Detective Stories > 11.00):
          Predict: Duck
      else (Running > 3.50):
        Predict: Duck
    else (Running > 13.50):
      if Art <= 5.50:
        Predict: Dog
      else (Art > 5.50):
        Predict: Duck
  else (Gardening > 4.50):
    if Running <= 9.50:
      if Art <= 6.50:
        if Gardening <= 8.50:
          if Art <= 1.50:
            Predict: Dog
          else (Art > 1.50):
            if Art <= 2.50:
              Predict: Duck
            else (Art > 2.50):
              if Running <= 6.50:
                if Gardening <= 6.50:
                  Predict: Dog
                else (Gardening > 6.50):
                  Predict: Duck
              else (Running > 6.50):
                if Swimming <= 10.00:
                  if Gardening <= 7.00:
                    Predict: Dog
                  else (Gardening > 7.00):
                    Predict: Duck
                else (Swimming > 10.00):
                  Predict: Dog
        else (Gardening > 8.50):
          Predict: Dog
      else (Art > 6.50):
        if Running <= 4.50:
          Predict: Cat
        else (Running > 4.50):
          Predict: Duck
    else (Running > 9.50):
      if Art <= 4.50:
        Predict: Dog
      else (Art > 4.50):
        if Swimming <= 9.50:
          Predict: Dog
        else (Swimming > 9.50):
          if Running <= 13.50:
            if Detective Stories <= 12.00:
              if Swimming <= 12.50:
                Predict: Duck
              else (Swimming > 12.50):
                Predict: Dog
            else (Detective Stories > 12.00):
              Predict: Dog
          else (Running > 13.50):
            if Swimming <= 12.50:
              Predict: Duck
            else (Swimming > 12.50):
              Predict: Cat

Tree 5 Rules:
if Running <= 4.50:
  if Art <= 4.50:
    if Gardening <= 5.50:
      Predict: Duck
    else (Gardening > 5.50):
      Predict: Dog
  else (Art > 4.50):
    if Art <= 7.50:
      if Swimming <= 7.00:
        Predict: Cat
      else (Swimming > 7.00):
        Predict: Duck
    else (Art > 7.50):
      Predict: Cat
else (Running > 4.50):
  if Art <= 4.50:
    if Gardening <= 4.50:
      if Running <= 13.50:
        if Running <= 12.50:
          if Running <= 11.00:
            if Swimming <= 11.50:
              Predict: Dog
            else (Swimming > 11.50):
              Predict: Duck
          else (Running > 11.00):
            Predict: Duck
        else (Running > 12.50):
          Predict: Cat
      else (Running > 13.50):
        Predict: Dog
    else (Gardening > 4.50):
      if Detective Stories <= 2.50:
        if Gardening <= 6.50:
          Predict: Duck
        else (Gardening > 6.50):
          Predict: Dog
      else (Detective Stories > 2.50):
        if Running <= 6.50:
          if Swimming <= 8.50:
            if Gardening <= 10.50:
              Predict: Duck
            else (Gardening > 10.50):
              Predict: Dog
          else (Swimming > 8.50):
            Predict: Dog
        else (Running > 6.50):
          Predict: Dog
  else (Art > 4.50):
    if Swimming <= 4.50:
      if Detective Stories <= 6.50:
        if Art <= 9.50:
          Predict: Duck
        else (Art > 9.50):
          Predict: Cat
      else (Detective Stories > 6.50):
        if Art <= 9.50:
          if Running <= 8.00:
            Predict: Cat
          else (Running > 8.00):
            Predict: Duck
        else (Art > 9.50):
          if Swimming <= 1.50:
            if Gardening <= 2.00:
              if Running <= 9.50:
                Predict: Cat
              else (Running > 9.50):
                Predict: Duck
            else (Gardening > 2.00):
              Predict: Cat
          else (Swimming > 1.50):
            Predict: Cat
    else (Swimming > 4.50):
      if Running <= 10.50:
        if Detective Stories <= 5.50:
          if Swimming <= 10.50:
            Predict: Dog
          else (Swimming > 10.50):
            Predict: Duck
        else (Detective Stories > 5.50):
          Predict: Duck
      else (Running > 10.50):
        if Art <= 6.50:
          if Swimming <= 12.50:
            Predict: Dog
          else (Swimming > 12.50):
            Predict: Duck
        else (Art > 6.50):
          if Gardening <= 4.00:
            Predict: Duck
          else (Gardening > 4.00):
            if Detective Stories <= 12.50:
              if Art <= 9.00:
                Predict: Dog
              else (Art > 9.00):
                if Detective Stories <= 11.50:
                  Predict: Cat
                else (Detective Stories > 11.50):
                  Predict: Duck
            else (Detective Stories > 12.50):
              Predict: Dog

Tree 6 Rules:
if Swimming <= 4.50:
  if Running <= 13.50:
    if Running <= 5.50:
      if Detective Stories <= 2.50:
        if Running <= 1.50:
          Predict: Cat
        else (Running > 1.50):
          Predict: Dog
      else (Detective Stories > 2.50):
        Predict: Cat
    else (Running > 5.50):
      if Art <= 6.50:
        if Gardening <= 6.50:
          if Art <= 2.00:
            Predict: Cat
          else (Art > 2.00):
            Predict: Duck
        else (Gardening > 6.50):
          Predict: Dog
      else (Art > 6.50):
        if Art <= 9.50:
          if Swimming <= 2.00:
            Predict: Duck
          else (Swimming > 2.00):
            Predict: Cat
        else (Art > 9.50):
          if Running <= 9.50:
            Predict: Cat
          else (Running > 9.50):
            if Gardening <= 1.00:
              Predict: Duck
            else (Gardening > 1.00):
              if Running <= 10.50:
                Predict: Duck
              else (Running > 10.50):
                Predict: Cat
  else (Running > 13.50):
    if Swimming <= 1.50:
      Predict: Dog
    else (Swimming > 1.50):
      Predict: Duck
else (Swimming > 4.50):
  if Art <= 4.50:
    if Gardening <= 2.50:
      if Art <= 3.50:
        if Gardening <= 0.50:
          Predict: Duck
        else (Gardening > 0.50):
          if Running <= 8.50:
            Predict: Duck
          else (Running > 8.50):
            Predict: Dog
      else (Art > 3.50):
        if Swimming <= 11.00:
          Predict: Dog
        else (Swimming > 11.00):
          Predict: Duck
    else (Gardening > 2.50):
      if Detective Stories <= 3.50:
        if Gardening <= 6.50:
          Predict: Duck
        else (Gardening > 6.50):
          Predict: Dog
      else (Detective Stories > 3.50):
        if Swimming <= 8.50:
          if Swimming <= 7.50:
            Predict: Dog
          else (Swimming > 7.50):
            if Running <= 8.00:
              Predict: Duck
            else (Running > 8.00):
              Predict: Dog
        else (Swimming > 8.50):
          Predict: Dog
  else (Art > 4.50):
    if Detective Stories <= 1.50:
      if Art <= 9.00:
        Predict: Dog
      else (Art > 9.00):
        Predict: Duck
    else (Detective Stories > 1.50):
      if Running <= 10.50:
        if Gardening <= 10.50:
          if Art <= 9.50:
            if Detective Stories <= 5.50:
              if Running <= 7.50:
                Predict: Duck
              else (Running > 7.50):
                Predict: Dog
            else (Detective Stories > 5.50):
              if Swimming <= 13.50:
                if Running <= 9.50:
                  Predict: Duck
                else (Running > 9.50):
                  if Gardening <= 4.00:
                    Predict: Duck
                  else (Gardening > 4.00):
                    Predict: Dog
              else (Swimming > 13.50):
                if Detective Stories <= 11.50:
                  Predict: Duck
                else (Detective Stories > 11.50):
                  Predict: Dog
          else (Art > 9.50):
            if Running <= 4.00:
              Predict: Cat
            else (Running > 4.00):
              Predict: Duck
        else (Gardening > 10.50):
          Predict: Dog
      else (Running > 10.50):
        if Swimming <= 8.50:
          if Gardening <= 2.00:
            Predict: Duck
          else (Gardening > 2.00):
            Predict: Dog
        else (Swimming > 8.50):
          if Running <= 13.50:
            if Swimming <= 13.50:
              if Detective Stories <= 12.50:
                if Gardening <= 13.50:
                  if Running <= 11.50:
                    if Art <= 8.00:
                      Predict: Dog
                    else (Art > 8.00):
                      Predict: Duck
                  else (Running > 11.50):
                    Predict: Duck
                else (Gardening > 13.50):
                  Predict: Dog
              else (Detective Stories > 12.50):
                Predict: Dog
            else (Swimming > 13.50):
              if Gardening <= 5.50:
                Predict: Duck
              else (Gardening > 5.50):
                Predict: Dog
          else (Running > 13.50):
            Predict: Duck

Tree 7 Rules:
if Running <= 4.50:
  if Art <= 5.50:
    if Swimming <= 10.00:
      if Art <= 0.50:
        Predict: Cat
      else (Art > 0.50):
        Predict: Dog
    else (Swimming > 10.00):
      Predict: Duck
  else (Art > 5.50):
    if Art <= 9.50:
      if Swimming <= 7.00:
        Predict: Cat
      else (Swimming > 7.00):
        Predict: Duck
    else (Art > 9.50):
      Predict: Cat
else (Running > 4.50):
  if Swimming <= 9.50:
    if Art <= 4.50:
      if Swimming <= 7.50:
        if Gardening <= 3.00:
          Predict: Duck
        else (Gardening > 3.00):
          Predict: Dog
      else (Swimming > 7.50):
        Predict: Dog
    else (Art > 4.50):
      if Gardening <= 9.50:
        if Swimming <= 4.50:
          if Detective Stories <= 7.50:
            Predict: Duck
          else (Detective Stories > 7.50):
            if Running <= 13.50:
              if Art <= 9.50:
                Predict: Duck
              else (Art > 9.50):
                Predict: Cat
            else (Running > 13.50):
              Predict: Duck
        else (Swimming > 4.50):
          if Gardening <= 5.00:
            Predict: Duck
          else (Gardening > 5.00):
            if Art <= 10.00:
              if Gardening <= 7.50:
                Predict: Dog
              else (Gardening > 7.50):
                if Gardening <= 8.50:
                  Predict: Duck
                else (Gardening > 8.50):
                  if Art <= 6.50:
                    Predict: Dog
                  else (Art > 6.50):
                    Predict: Duck
            else (Art > 10.00):
              Predict: Dog
      else (Gardening > 9.50):
        Predict: Dog
  else (Swimming > 9.50):
    if Gardening <= 4.50:
      Predict: Duck
    else (Gardening > 4.50):
      if Art <= 4.50:
        Predict: Dog
      else (Art > 4.50):
        if Gardening <= 8.50:
          if Swimming <= 12.50:
            Predict: Duck
          else (Swimming > 12.50):
            if Running <= 13.50:
              if Detective Stories <= 5.50:
                Predict: Dog
              else (Detective Stories > 5.50):
                Predict: Duck
            else (Running > 13.50):
              Predict: Cat
        else (Gardening > 8.50):
          if Swimming <= 11.00:
            if Art <= 8.50:
              Predict: Dog
            else (Art > 8.50):
              Predict: Duck
          else (Swimming > 11.00):
            if Gardening <= 13.50:
              Predict: Dog
            else (Gardening > 13.50):
              if Running <= 11.00:
                Predict: Duck
              else (Running > 11.00):
                Predict: Dog

Tree 8 Rules:
if Swimming <= 4.50:
  if Gardening <= 8.50:
    if Gardening <= 2.50:
      if Detective Stories <= 11.50:
        if Art <= 10.50:
          Predict: Cat
        else (Art > 10.50):
          if Art <= 11.50:
            if Gardening <= 0.50:
              if Running <= 6.00:
                Predict: Cat
              else (Running > 6.00):
                Predict: Duck
            else (Gardening > 0.50):
              Predict: Cat
          else (Art > 11.50):
            Predict: Cat
      else (Detective Stories > 11.50):
        if Running <= 6.50:
          Predict: Cat
        else (Running > 6.50):
          Predict: Duck
    else (Gardening > 2.50):
      if Detective Stories <= 6.50:
        if Running <= 8.00:
          Predict: Cat
        else (Running > 8.00):
          Predict: Duck
      else (Detective Stories > 6.50):
        if Detective Stories <= 10.50:
          if Gardening <= 3.50:
            if Running <= 7.00:
              Predict: Cat
            else (Running > 7.00):
              Predict: Duck
          else (Gardening > 3.50):
            Predict: Cat
        else (Detective Stories > 10.50):
          Predict: Cat
  else (Gardening > 8.50):
    if Running <= 5.00:
      Predict: Cat
    else (Running > 5.00):
      if Art <= 7.50:
        Predict: Dog
      else (Art > 7.50):
        Predict: Cat
else (Swimming > 4.50):
  if Running <= 9.50:
    if Art <= 5.50:
      if Art <= 1.50:
        if Art <= 0.50:
          if Running <= 3.50:
            Predict: Duck
          else (Running > 3.50):
            Predict: Dog
        else (Art > 0.50):
          Predict: Dog
      else (Art > 1.50):
        if Detective Stories <= 4.50:
          Predict: Duck
        else (Detective Stories > 4.50):
          if Gardening <= 4.50:
            Predict: Duck
          else (Gardening > 4.50):
            if Gardening <= 9.50:
              if Detective Stories <= 11.00:
                Predict: Dog
              else (Detective Stories > 11.00):
                if Swimming <= 10.50:
                  Predict: Duck
                else (Swimming > 10.50):
                  Predict: Dog
            else (Gardening > 9.50):
              Predict: Dog
    else (Art > 5.50):
      Predict: Duck
  else (Running > 9.50):
    if Swimming <= 9.50:
      if Detective Stories <= 12.50:
        if Detective Stories <= 10.50:
          if Running <= 13.50:
            if Art <= 9.00:
              Predict: Dog
            else (Art > 9.00):
              if Gardening <= 8.00:
                Predict: Dog
              else (Gardening > 8.00):
                Predict: Duck
          else (Running > 13.50):
            if Art <= 10.50:
              Predict: Dog
            else (Art > 10.50):
              Predict: Duck
        else (Detective Stories > 10.50):
          Predict: Dog
      else (Detective Stories > 12.50):
        if Gardening <= 4.50:
          Predict: Duck
        else (Gardening > 4.50):
          Predict: Dog
    else (Swimming > 9.50):
      if Gardening <= 6.50:
        if Running <= 10.50:
          if Swimming <= 13.00:
            Predict: Duck
          else (Swimming > 13.00):
            Predict: Dog
        else (Running > 10.50):
          if Running <= 12.50:
            Predict: Duck
          else (Running > 12.50):
            if Art <= 4.50:
              Predict: Dog
            else (Art > 4.50):
              if Gardening <= 4.00:
                Predict: Duck
              else (Gardening > 4.00):
                if Detective Stories <= 8.50:
                  Predict: Dog
                else (Detective Stories > 8.50):
                  if Running <= 13.50:
                    Predict: Duck
                  else (Running > 13.50):
                    if Swimming <= 12.50:
                      Predict: Duck
                    else (Swimming > 12.50):
                      Predict: Cat
      else (Gardening > 6.50):
        if Detective Stories <= 4.50:
          if Running <= 11.50:
            Predict: Dog
          else (Running > 11.50):
            Predict: Duck
        else (Detective Stories > 4.50):
          if Gardening <= 13.50:
            if Art <= 9.00:
              Predict: Dog
            else (Art > 9.00):
              if Art <= 12.50:
                Predict: Duck
              else (Art > 12.50):
                Predict: Dog
          else (Gardening > 13.50):
            if Swimming <= 12.50:
              Predict: Duck
            else (Swimming > 12.50):
              Predict: Dog

Tree 9 Rules:
if Running <= 4.50:
  if Running <= 3.50:
    if Art <= 0.50:
      Predict: Duck
    else (Art > 0.50):
      if Swimming <= 5.00:
        Predict: Cat
      else (Swimming > 5.00):
        if Swimming <= 10.50:
          if Detective Stories <= 9.50:
            Predict: Dog
          else (Detective Stories > 9.50):
            Predict: Duck
        else (Swimming > 10.50):
          Predict: Cat
  else (Running > 3.50):
    if Gardening <= 4.50:
      Predict: Cat
    else (Gardening > 4.50):
      if Detective Stories <= 11.00:
        if Art <= 5.50:
          Predict: Duck
        else (Art > 5.50):
          Predict: Cat
      else (Detective Stories > 11.00):
        Predict: Cat
else (Running > 4.50):
  if Art <= 4.50:
    if Running <= 9.00:
      if Art <= 1.50:
        Predict: Dog
      else (Art > 1.50):
        if Gardening <= 9.00:
          if Swimming <= 6.50:
            if Art <= 3.50:
              Predict: Duck
            else (Art > 3.50):
              Predict: Dog
          else (Swimming > 6.50):
            Predict: Duck
        else (Gardening > 9.00):
          Predict: Dog
    else (Running > 9.00):
      if Gardening <= 3.50:
        if Swimming <= 11.50:
          if Running <= 11.50:
            Predict: Dog
          else (Running > 11.50):
            Predict: Cat
        else (Swimming > 11.50):
          Predict: Duck
      else (Gardening > 3.50):
        Predict: Dog
  else (Art > 4.50):
    if Swimming <= 4.50:
      if Art <= 9.50:
        if Art <= 8.50:
          if Detective Stories <= 8.50:
            Predict: Dog
          else (Detective Stories > 8.50):
            Predict: Cat
        else (Art > 8.50):
          Predict: Duck
      else (Art > 9.50):
        if Running <= 9.00:
          Predict: Cat
        else (Running > 9.00):
          if Gardening <= 1.00:
            Predict: Duck
          else (Gardening > 1.00):
            Predict: Cat
    else (Swimming > 4.50):
      if Detective Stories <= 1.50:
        Predict: Dog
      else (Detective Stories > 1.50):
        if Gardening <= 4.50:
          if Art <= 7.50:
            if Gardening <= 3.50:
              Predict: Duck
            else (Gardening > 3.50):
              if Running <= 10.50:
                Predict: Duck
              else (Running > 10.50):
                Predict: Dog
          else (Art > 7.50):
            Predict: Duck
        else (Gardening > 4.50):
          if Running <= 9.50:
            if Detective Stories <= 11.50:
              Predict: Duck
            else (Detective Stories > 11.50):
              if Gardening <= 8.50:
                Predict: Duck
              else (Gardening > 8.50):
                if Detective Stories <= 12.50:
                  Predict: Dog
                else (Detective Stories > 12.50):
                  Predict: Duck
          else (Running > 9.50):
            if Art <= 5.50:
              Predict: Duck
            else (Art > 5.50):
              if Detective Stories <= 4.50:
                Predict: Duck
              else (Detective Stories > 4.50):
                if Swimming <= 9.50:
                  Predict: Dog
                else (Swimming > 9.50):
                  if Detective Stories <= 10.50:
                    Predict: Dog
                  else (Detective Stories > 10.50):
                    if Detective Stories <= 12.50:
                      Predict: Duck
                    else (Detective Stories > 12.50):
                      Predict: Dog

Tree 10 Rules:
if Running <= 4.50:
  if Detective Stories <= 2.50:
    if Gardening <= 8.00:
      Predict: Dog
    else (Gardening > 8.00):
      Predict: Cat
  else (Detective Stories > 2.50):
    if Gardening <= 5.50:
      if Art <= 9.50:
        if Swimming <= 7.00:
          Predict: Cat
        else (Swimming > 7.00):
          if Detective Stories <= 13.00:
            Predict: Duck
          else (Detective Stories > 13.00):
            Predict: Dog
      else (Art > 9.50):
        Predict: Cat
    else (Gardening > 5.50):
      if Art <= 5.00:
        Predict: Dog
      else (Art > 5.00):
        Predict: Cat
else (Running > 4.50):
  if Running <= 9.50:
    if Art <= 5.50:
      if Gardening <= 4.50:
        Predict: Duck
      else (Gardening > 4.50):
        if Swimming <= 8.50:
          if Swimming <= 7.50:
            Predict: Dog
          else (Swimming > 7.50):
            if Art <= 1.50:
              Predict: Dog
            else (Art > 1.50):
              Predict: Duck
        else (Swimming > 8.50):
          Predict: Dog
    else (Art > 5.50):
      if Art <= 9.50:
        Predict: Duck
      else (Art > 9.50):
        if Art <= 13.50:
          if Gardening <= 1.50:
            if Swimming <= 5.00:
              Predict: Cat
            else (Swimming > 5.00):
              Predict: Duck
          else (Gardening > 1.50):
            if Running <= 8.50:
              if Gardening <= 4.00:
                Predict: Cat
              else (Gardening > 4.00):
                if Gardening <= 6.00:
                  if Running <= 5.50:
                    Predict: Cat
                  else (Running > 5.50):
                    Predict: Duck
                else (Gardening > 6.00):
                  Predict: Cat
            else (Running > 8.50):
              Predict: Duck
        else (Art > 13.50):
          Predict: Duck
  else (Running > 9.50):
    if Swimming <= 4.50:
      if Running <= 10.50:
        Predict: Duck
      else (Running > 10.50):
        if Gardening <= 1.50:
          Predict: Duck
        else (Gardening > 1.50):
          if Art <= 10.00:
            if Art <= 5.00:
              Predict: Cat
            else (Art > 5.00):
              Predict: Duck
          else (Art > 10.00):
            Predict: Cat
    else (Swimming > 4.50):
      if Gardening <= 3.50:
        if Running <= 10.50:
          if Swimming <= 11.50:
            if Art <= 4.50:
              Predict: Dog
            else (Art > 4.50):
              Predict: Duck
          else (Swimming > 11.50):
            Predict: Duck
        else (Running > 10.50):
          Predict: Duck
      else (Gardening > 3.50):
        if Art <= 7.50:
          Predict: Dog
        else (Art > 7.50):
          if Art <= 12.50:
            if Detective Stories <= 10.50:
              Predict: Dog
            else (Detective Stories > 10.50):
              if Running <= 12.00:
                if Gardening <= 4.50:
                  Predict: Duck
                else (Gardening > 4.50):
                  if Detective Stories <= 12.50:
                    Predict: Duck
                  else (Detective Stories > 12.50):
                    Predict: Dog
              else (Running > 12.00):
                if Running <= 13.50:
                  Predict: Duck
                else (Running > 13.50):
                  if Art <= 9.00:
                    Predict: Duck
                  else (Art > 9.00):
                    Predict: Cat
          else (Art > 12.50):
            Predict: Dog

Tree 11 Rules:
if Swimming <= 4.50:
  if Running <= 8.50:
    if Gardening <= 10.50:
      Predict: Cat
    else (Gardening > 10.50):
      if Swimming <= 1.50:
        Predict: Dog
      else (Swimming > 1.50):
        Predict: Cat
  else (Running > 8.50):
    if Art <= 10.00:
      if Gardening <= 5.00:
        Predict: Duck
      else (Gardening > 5.00):
        Predict: Dog
    else (Art > 10.00):
      if Gardening <= 3.50:
        if Swimming <= 0.50:
          Predict: Duck
        else (Swimming > 0.50):
          if Gardening <= 2.50:
            if Swimming <= 2.50:
              if Art <= 12.00:
                Predict: Duck
              else (Art > 12.00):
                Predict: Cat
            else (Swimming > 2.50):
              Predict: Cat
          else (Gardening > 2.50):
            Predict: Duck
      else (Gardening > 3.50):
        Predict: Cat
else (Swimming > 4.50):
  if Running <= 9.50:
    if Running <= 4.50:
      if Gardening <= 10.50:
        if Art <= 7.50:
          if Gardening <= 6.00:
            if Running <= 3.00:
              Predict: Dog
            else (Running > 3.00):
              Predict: Duck
          else (Gardening > 6.00):
            Predict: Dog
        else (Art > 7.50):
          Predict: Cat
      else (Gardening > 10.50):
        Predict: Dog
    else (Running > 4.50):
      if Detective Stories <= 6.50:
        if Running <= 7.50:
          if Gardening <= 8.50:
            Predict: Duck
          else (Gardening > 8.50):
            Predict: Dog
        else (Running > 7.50):
          if Running <= 8.50:
            Predict: Dog
          else (Running > 8.50):
            Predict: Duck
      else (Detective Stories > 6.50):
        if Swimming <= 6.50:
          if Gardening <= 3.00:
            Predict: Duck
          else (Gardening > 3.00):
            Predict: Dog
        else (Swimming > 6.50):
          if Detective Stories <= 10.50:
            Predict: Duck
          else (Detective Stories > 10.50):
            if Art <= 4.50:
              if Gardening <= 4.00:
                Predict: Duck
              else (Gardening > 4.00):
                Predict: Dog
            else (Art > 4.50):
              if Swimming <= 13.50:
                Predict: Duck
              else (Swimming > 13.50):
                if Gardening <= 5.50:
                  Predict: Duck
                else (Gardening > 5.50):
                  Predict: Dog
  else (Running > 9.50):
    if Gardening <= 4.50:
      if Art <= 3.00:
        Predict: Dog
      else (Art > 3.00):
        if Running <= 13.50:
          Predict: Duck
        else (Running > 13.50):
          if Gardening <= 1.00:
            if Art <= 7.50:
              Predict: Dog
            else (Art > 7.50):
              Predict: Duck
          else (Gardening > 1.00):
            Predict: Duck
    else (Gardening > 4.50):
      if Gardening <= 6.50:
        if Swimming <= 11.50:
          Predict: Dog
        else (Swimming > 11.50):
          if Swimming <= 13.50:
            if Detective Stories <= 11.50:
              if Swimming <= 12.50:
                Predict: Duck
              else (Swimming > 12.50):
                Predict: Cat
            else (Detective Stories > 11.50):
              if Swimming <= 12.50:
                Predict: Dog
              else (Swimming > 12.50):
                Predict: Duck
          else (Swimming > 13.50):
            Predict: Dog
      else (Gardening > 6.50):
        if Running <= 10.50:
          if Gardening <= 12.50:
            Predict: Dog
          else (Gardening > 12.50):
            Predict: Duck
        else (Running > 10.50):
          Predict: Dog

Tree 12 Rules:
if Swimming <= 4.50:
  if Detective Stories <= 5.50:
    if Art <= 4.50:
      Predict: Dog
    else (Art > 4.50):
      Predict: Cat
  else (Detective Stories > 5.50):
    if Gardening <= 4.50:
      if Swimming <= 3.50:
        if Running <= 8.50:
          Predict: Cat
        else (Running > 8.50):
          if Detective Stories <= 11.50:
            if Running <= 9.50:
              Predict: Cat
            else (Running > 9.50):
              if Gardening <= 1.50:
                Predict: Duck
              else (Gardening > 1.50):
                Predict: Cat
          else (Detective Stories > 11.50):
            if Detective Stories <= 12.50:
              Predict: Duck
            else (Detective Stories > 12.50):
              Predict: Cat
      else (Swimming > 3.50):
        if Running <= 9.00:
          Predict: Cat
        else (Running > 9.00):
          if Running <= 12.50:
            Predict: Duck
          else (Running > 12.50):
            Predict: Cat
    else (Gardening > 4.50):
      if Running <= 13.00:
        Predict: Cat
      else (Running > 13.00):
        Predict: Dog
else (Swimming > 4.50):
  if Swimming <= 9.50:
    if Running <= 9.50:
      if Gardening <= 5.00:
        Predict: Duck
      else (Gardening > 5.00):
        if Art <= 5.50:
          Predict: Dog
        else (Art > 5.50):
          Predict: Duck
    else (Running > 9.50):
      if Art <= 6.50:
        Predict: Dog
      else (Art > 6.50):
        if Detective Stories <= 13.50:
          if Gardening <= 4.50:
            if Swimming <= 7.50:
              Predict: Duck
            else (Swimming > 7.50):
              Predict: Dog
          else (Gardening > 4.50):
            if Gardening <= 9.50:
              if Detective Stories <= 11.50:
                if Gardening <= 8.00:
                  Predict: Dog
                else (Gardening > 8.00):
                  Predict: Duck
              else (Detective Stories > 11.50):
                Predict: Dog
            else (Gardening > 9.50):
              Predict: Dog
        else (Detective Stories > 13.50):
          if Running <= 10.50:
            Predict: Duck
          else (Running > 10.50):
            if Gardening <= 8.00:
              Predict: Duck
            else (Gardening > 8.00):
              Predict: Dog
  else (Swimming > 9.50):
    if Running <= 4.50:
      if Running <= 2.50:
        if Art <= 9.50:
          if Art <= 4.50:
            if Gardening <= 4.50:
              Predict: Duck
            else (Gardening > 4.50):
              Predict: Dog
          else (Art > 4.50):
            Predict: Duck
        else (Art > 9.50):
          Predict: Cat
      else (Running > 2.50):
        Predict: Cat
    else (Running > 4.50):
      if Art <= 4.50:
        if Detective Stories <= 5.00:
          if Swimming <= 11.00:
            Predict: Dog
          else (Swimming > 11.00):
            Predict: Duck
        else (Detective Stories > 5.00):
          if Swimming <= 13.50:
            if Art <= 3.50:
              Predict: Dog
            else (Art > 3.50):
              if Swimming <= 11.50:
                Predict: Dog
              else (Swimming > 11.50):
                if Swimming <= 12.50:
                  Predict: Duck
                else (Swimming > 12.50):
                  Predict: Dog
          else (Swimming > 13.50):
            if Running <= 12.50:
              if Detective Stories <= 11.00:
                Predict: Dog
              else (Detective Stories > 11.00):
                Predict: Duck
            else (Running > 12.50):
              Predict: Dog
      else (Art > 4.50):
        if Detective Stories <= 13.50:
          if Gardening <= 5.50:
            Predict: Duck
          else (Gardening > 5.50):
            if Art <= 6.50:
              if Swimming <= 10.50:
                Predict: Duck
              else (Swimming > 10.50):
                Predict: Dog
            else (Art > 6.50):
              if Gardening <= 6.50:
                if Detective Stories <= 9.00:
                  Predict: Dog
                else (Detective Stories > 9.00):
                  Predict: Duck
              else (Gardening > 6.50):
                Predict: Duck
        else (Detective Stories > 13.50):
          if Art <= 11.50:
            Predict: Duck
          else (Art > 11.50):
            if Gardening <= 8.50:
              Predict: Duck
            else (Gardening > 8.50):
              Predict: Dog

Tree 13 Rules:
if Swimming <= 4.50:
  if Art <= 4.50:
    if Gardening <= 5.50:
      Predict: Cat
    else (Gardening > 5.50):
      Predict: Dog
  else (Art > 4.50):
    if Running <= 13.50:
      Predict: Cat
    else (Running > 13.50):
      Predict: Duck
else (Swimming > 4.50):
  if Running <= 9.50:
    if Art <= 4.50:
      if Gardening <= 4.50:
        Predict: Duck
      else (Gardening > 4.50):
        if Art <= 0.50:
          if Detective Stories <= 6.50:
            Predict: Duck
          else (Detective Stories > 6.50):
            Predict: Dog
        else (Art > 0.50):
          Predict: Dog
    else (Art > 4.50):
      if Gardening <= 6.50:
        Predict: Duck
      else (Gardening > 6.50):
        if Swimming <= 13.50:
          if Detective Stories <= 10.50:
            if Running <= 4.50:
              Predict: Cat
            else (Running > 4.50):
              Predict: Duck
          else (Detective Stories > 10.50):
            Predict: Duck
        else (Swimming > 13.50):
          if Gardening <= 9.50:
            Predict: Dog
          else (Gardening > 9.50):
            Predict: Cat
  else (Running > 9.50):
    if Art <= 3.50:
      if Swimming <= 11.50:
        Predict: Dog
      else (Swimming > 11.50):
        if Running <= 11.50:
          if Gardening <= 2.50:
            Predict: Duck
          else (Gardening > 2.50):
            Predict: Dog
        else (Running > 11.50):
          Predict: Dog
    else (Art > 3.50):
      if Gardening <= 3.50:
        if Art <= 4.50:
          if Swimming <= 11.00:
            Predict: Dog
          else (Swimming > 11.00):
            Predict: Duck
        else (Art > 4.50):
          Predict: Duck
      else (Gardening > 3.50):
        if Gardening <= 6.50:
          if Swimming <= 10.50:
            if Art <= 8.00:
              Predict: Dog
            else (Art > 8.00):
              if Swimming <= 8.00:
                Predict: Duck
              else (Swimming > 8.00):
                Predict: Dog
          else (Swimming > 10.50):
            if Running <= 13.50:
              if Art <= 9.00:
                Predict: Dog
              else (Art > 9.00):
                Predict: Duck
            else (Running > 13.50):
              if Swimming <= 12.50:
                Predict: Duck
              else (Swimming > 12.50):
                Predict: Cat
        else (Gardening > 6.50):
          if Art <= 5.50:
            if Running <= 10.50:
              Predict: Duck
            else (Running > 10.50):
              Predict: Dog
          else (Art > 5.50):
            Predict: Dog

Tree 14 Rules:
if Gardening <= 4.50:
  if Art <= 9.50:
    if Art <= 3.50:
      if Swimming <= 4.50:
        if Running <= 8.00:
          Predict: Cat
        else (Running > 8.00):
          Predict: Duck
      else (Swimming > 4.50):
        if Swimming <= 11.50:
          if Detective Stories <= 12.50:
            if Running <= 9.00:
              Predict: Duck
            else (Running > 9.00):
              Predict: Dog
          else (Detective Stories > 12.50):
            Predict: Dog
        else (Swimming > 11.50):
          Predict: Duck
    else (Art > 3.50):
      if Swimming <= 4.00:
        Predict: Cat
      else (Swimming > 4.00):
        if Detective Stories <= 3.00:
          if Gardening <= 3.50:
            Predict: Duck
          else (Gardening > 3.50):
            Predict: Dog
        else (Detective Stories > 3.00):
          if Art <= 6.50:
            Predict: Duck
          else (Art > 6.50):
            if Gardening <= 3.50:
              Predict: Duck
            else (Gardening > 3.50):
              if Detective Stories <= 12.00:
                Predict: Dog
              else (Detective Stories > 12.00):
                Predict: Duck
  else (Art > 9.50):
    if Art <= 12.50:
      if Gardening <= 3.50:
        if Running <= 8.50:
          if Running <= 5.00:
            Predict: Cat
          else (Running > 5.00):
            if Swimming <= 7.50:
              Predict: Cat
            else (Swimming > 7.50):
              Predict: Duck
        else (Running > 8.50):
          Predict: Duck
      else (Gardening > 3.50):
        Predict: Cat
    else (Art > 12.50):
      if Detective Stories <= 5.00:
        Predict: Cat
      else (Detective Stories > 5.00):
        if Gardening <= 3.50:
          if Detective Stories <= 10.50:
            if Swimming <= 3.50:
              Predict: Cat
            else (Swimming > 3.50):
              Predict: Duck
          else (Detective Stories > 10.50):
            if Detective Stories <= 12.50:
              Predict: Duck
            else (Detective Stories > 12.50):
              if Art <= 13.50:
                if Running <= 5.00:
                  Predict: Cat
                else (Running > 5.00):
                  Predict: Duck
              else (Art > 13.50):
                if Running <= 5.00:
                  Predict: Cat
                else (Running > 5.00):
                  Predict: Duck
        else (Gardening > 3.50):
          if Detective Stories <= 13.00:
            Predict: Cat
          else (Detective Stories > 13.00):
            if Swimming <= 6.00:
              Predict: Cat
            else (Swimming > 6.00):
              Predict: Duck
else (Gardening > 4.50):
  if Swimming <= 4.50:
    if Running <= 11.50:
      if Detective Stories <= 5.50:
        if Gardening <= 8.50:
          Predict: Cat
        else (Gardening > 8.50):
          Predict: Dog
      else (Detective Stories > 5.50):
        Predict: Cat
    else (Running > 11.50):
      if Running <= 13.00:
        if Detective Stories <= 13.50:
          Predict: Dog
        else (Detective Stories > 13.50):
          Predict: Cat
      else (Running > 13.00):
        Predict: Dog
  else (Swimming > 4.50):
    if Art <= 5.50:
      if Running <= 6.50:
        if Detective Stories <= 4.00:
          Predict: Duck
        else (Detective Stories > 4.00):
          if Gardening <= 8.50:
            if Swimming <= 12.00:
              if Detective Stories <= 13.00:
                Predict: Duck
              else (Detective Stories > 13.00):
                Predict: Dog
            else (Swimming > 12.00):
              Predict: Dog
          else (Gardening > 8.50):
            Predict: Dog
      else (Running > 6.50):
        Predict: Dog
    else (Art > 5.50):
      if Swimming <= 9.50:
        if Running <= 9.00:
          Predict: Duck
        else (Running > 9.00):
          Predict: Dog
      else (Swimming > 9.50):
        if Art <= 12.50:
          if Detective Stories <= 13.50:
            if Running <= 10.00:
              Predict: Duck
            else (Running > 10.00):
              if Detective Stories <= 10.50:
                Predict: Dog
              else (Detective Stories > 10.50):
                if Art <= 7.00:
                  Predict: Dog
                else (Art > 7.00):
                  if Gardening <= 5.50:
                    if Swimming <= 13.50:
                      Predict: Cat
                    else (Swimming > 13.50):
                      Predict: Duck
                  else (Gardening > 5.50):
                    Predict: Duck
          else (Detective Stories > 13.50):
            Predict: Cat
        else (Art > 12.50):
          if Running <= 9.00:
            if Gardening <= 9.50:
              if Running <= 3.00:
                Predict: Cat
              else (Running > 3.00):
                Predict: Duck
            else (Gardening > 9.50):
              Predict: Cat
          else (Running > 9.00):
            Predict: Dog

Tree 15 Rules:
if Art <= 9.50:
  if Art <= 4.50:
    if Swimming <= 11.50:
      if Gardening <= 3.50:
        if Swimming <= 4.50:
          Predict: Cat
        else (Swimming > 4.50):
          Predict: Duck
      else (Gardening > 3.50):
        Predict: Dog
    else (Swimming > 11.50):
      if Detective Stories <= 13.50:
        if Gardening <= 4.00:
          Predict: Duck
        else (Gardening > 4.00):
          if Running <= 5.00:
            if Gardening <= 6.00:
              Predict: Duck
            else (Gardening > 6.00):
              Predict: Dog
          else (Running > 5.00):
            Predict: Dog
      else (Detective Stories > 13.50):
        Predict: Dog
  else (Art > 4.50):
    if Swimming <= 4.50:
      if Swimming <= 1.50:
        if Art <= 5.50:
          Predict: Dog
        else (Art > 5.50):
          if Running <= 6.50:
            Predict: Cat
          else (Running > 6.50):
            Predict: Duck
      else (Swimming > 1.50):
        Predict: Cat
    else (Swimming > 4.50):
      if Gardening <= 4.50:
        if Gardening <= 3.50:
          Predict: Duck
        else (Gardening > 3.50):
          if Running <= 11.00:
            Predict: Duck
          else (Running > 11.00):
            Predict: Dog
      else (Gardening > 4.50):
        if Running <= 9.00:
          if Art <= 6.50:
            if Running <= 5.00:
              Predict: Duck
            else (Running > 5.00):
              Predict: Dog
          else (Art > 6.50):
            Predict: Duck
        else (Running > 9.00):
          Predict: Dog
else (Art > 9.50):
  if Running <= 4.50:
    Predict: Cat
  else (Running > 4.50):
    if Gardening <= 11.50:
      if Detective Stories <= 12.50:
        if Running <= 9.50:
          if Detective Stories <= 10.50:
            if Swimming <= 5.00:
              Predict: Cat
            else (Swimming > 5.00):
              Predict: Duck
          else (Detective Stories > 10.50):
            if Gardening <= 2.50:
              Predict: Duck
            else (Gardening > 2.50):
              if Running <= 5.50:
                Predict: Cat
              else (Running > 5.50):
                if Detective Stories <= 11.50:
                  Predict: Duck
                else (Detective Stories > 11.50):
                  Predict: Cat
        else (Running > 9.50):
          if Detective Stories <= 10.50:
            Predict: Duck
          else (Detective Stories > 10.50):
            if Art <= 13.50:
              if Running <= 13.50:
                if Swimming <= 5.00:
                  if Art <= 12.00:
                    Predict: Cat
                  else (Art > 12.00):
                    Predict: Duck
                else (Swimming > 5.00):
                  Predict: Duck
              else (Running > 13.50):
                Predict: Cat
            else (Art > 13.50):
              Predict: Cat
      else (Detective Stories > 12.50):
        if Gardening <= 3.50:
          Predict: Duck
        else (Gardening > 3.50):
          if Art <= 12.00:
            if Gardening <= 6.50:
              if Detective Stories <= 13.50:
                Predict: Cat
              else (Detective Stories > 13.50):
                Predict: Duck
            else (Gardening > 6.50):
              Predict: Cat
          else (Art > 12.00):
            if Swimming <= 5.50:
              Predict: Cat
            else (Swimming > 5.50):
              Predict: Duck
    else (Gardening > 11.50):
      Predict: Dog

Tree 16 Rules:
if Art <= 4.50:
  if Running <= 4.50:
    if Gardening <= 3.50:
      Predict: Cat
    else (Gardening > 3.50):
      if Art <= 2.50:
        if Running <= 3.00:
          if Running <= 1.50:
            Predict: Duck
          else (Running > 1.50):
            Predict: Dog
        else (Running > 3.00):
          Predict: Duck
      else (Art > 2.50):
        Predict: Dog
  else (Running > 4.50):
    if Running <= 8.50:
      if Detective Stories <= 4.50:
        Predict: Duck
      else (Detective Stories > 4.50):
        if Gardening <= 2.50:
          Predict: Duck
        else (Gardening > 2.50):
          if Gardening <= 8.50:
            if Swimming <= 8.50:
              if Running <= 6.50:
                Predict: Duck
              else (Running > 6.50):
                Predict: Dog
            else (Swimming > 8.50):
              Predict: Dog
          else (Gardening > 8.50):
            Predict: Dog
    else (Running > 8.50):
      if Swimming <= 4.50:
        if Art <= 2.50:
          Predict: Dog
        else (Art > 2.50):
          Predict: Duck
      else (Swimming > 4.50):
        if Gardening <= 0.50:
          Predict: Duck
        else (Gardening > 0.50):
          if Art <= 3.50:
            Predict: Dog
          else (Art > 3.50):
            if Gardening <= 3.00:
              Predict: Duck
            else (Gardening > 3.00):
              Predict: Dog
else (Art > 4.50):
  if Swimming <= 4.50:
    if Running <= 9.50:
      if Detective Stories <= 5.50:
        if Art <= 8.00:
          Predict: Dog
        else (Art > 8.00):
          Predict: Cat
      else (Detective Stories > 5.50):
        Predict: Cat
    else (Running > 9.50):
      Predict: Duck
  else (Swimming > 4.50):
    if Running <= 4.50:
      if Art <= 9.50:
        Predict: Duck
      else (Art > 9.50):
        Predict: Cat
    else (Running > 4.50):
      if Gardening <= 4.50:
        Predict: Duck
      else (Gardening > 4.50):
        if Detective Stories <= 13.50:
          if Swimming <= 8.50:
            if Detective Stories <= 6.50:
              Predict: Duck
            else (Detective Stories > 6.50):
              if Running <= 8.00:
                Predict: Duck
              else (Running > 8.00):
                Predict: Dog
          else (Swimming > 8.50):
            if Detective Stories <= 6.50:
              if Swimming <= 12.00:
                if Art <= 5.50:
                  Predict: Dog
                else (Art > 5.50):
                  Predict: Duck
              else (Swimming > 12.00):
                Predict: Dog
            else (Detective Stories > 6.50):
              if Running <= 11.50:
                if Gardening <= 11.50:
                  Predict: Duck
                else (Gardening > 11.50):
                  if Art <= 10.50:
                    Predict: Duck
                  else (Art > 10.50):
                    Predict: Dog
              else (Running > 11.50):
                if Art <= 9.00:
                  Predict: Dog
                else (Art > 9.00):
                  Predict: Duck
        else (Detective Stories > 13.50):
          Predict: Dog

Tree 17 Rules:
if Art <= 4.50:
  if Running <= 4.50:
    if Swimming <= 5.00:
      Predict: Cat
    else (Swimming > 5.00):
      Predict: Dog
  else (Running > 4.50):
    if Swimming <= 13.50:
      if Gardening <= 4.50:
        if Art <= 1.50:
          if Swimming <= 6.00:
            Predict: Cat
          else (Swimming > 6.00):
            Predict: Dog
        else (Art > 1.50):
          Predict: Duck
      else (Gardening > 4.50):
        if Gardening <= 6.50:
          if Detective Stories <= 4.50:
            Predict: Duck
          else (Detective Stories > 4.50):
            Predict: Dog
        else (Gardening > 6.50):
          Predict: Dog
    else (Swimming > 13.50):
      if Art <= 2.00:
        Predict: Duck
      else (Art > 2.00):
        Predict: Dog
else (Art > 4.50):
  if Running <= 4.50:
    if Art <= 6.50:
      if Swimming <= 7.00:
        Predict: Cat
      else (Swimming > 7.00):
        Predict: Duck
    else (Art > 6.50):
      if Art <= 9.50:
        if Swimming <= 7.00:
          Predict: Cat
        else (Swimming > 7.00):
          Predict: Duck
      else (Art > 9.50):
        Predict: Cat
  else (Running > 4.50):
    if Running <= 10.50:
      if Art <= 9.50:
        if Swimming <= 4.50:
          Predict: Cat
        else (Swimming > 4.50):
          if Gardening <= 4.50:
            Predict: Duck
          else (Gardening > 4.50):
            if Art <= 7.50:
              if Gardening <= 7.00:
                Predict: Dog
              else (Gardening > 7.00):
                if Swimming <= 13.00:
                  if Swimming <= 11.00:
                    if Running <= 7.50:
                      Predict: Dog
                    else (Running > 7.50):
                      Predict: Duck
                  else (Swimming > 11.00):
                    Predict: Duck
                else (Swimming > 13.00):
                  Predict: Dog
            else (Art > 7.50):
              Predict: Duck
      else (Art > 9.50):
        if Running <= 9.50:
          if Gardening <= 2.50:
            if Swimming <= 5.00:
              Predict: Cat
            else (Swimming > 5.00):
              Predict: Duck
          else (Gardening > 2.50):
            if Swimming <= 6.50:
              Predict: Cat
            else (Swimming > 6.50):
              Predict: Duck
        else (Running > 9.50):
          Predict: Duck
    else (Running > 10.50):
      if Gardening <= 4.50:
        if Swimming <= 5.00:
          if Running <= 13.50:
            if Gardening <= 1.50:
              Predict: Duck
            else (Gardening > 1.50):
              Predict: Cat
          else (Running > 13.50):
            Predict: Duck
        else (Swimming > 5.00):
          if Gardening <= 3.50:
            Predict: Duck
          else (Gardening > 3.50):
            if Running <= 12.50:
              Predict: Duck
            else (Running > 12.50):
              Predict: Dog
      else (Gardening > 4.50):
        if Swimming <= 9.50:
          if Swimming <= 4.50:
            if Art <= 8.50:
              Predict: Duck
            else (Art > 8.50):
              Predict: Cat
          else (Swimming > 4.50):
            if Art <= 10.50:
              Predict: Dog
            else (Art > 10.50):
              if Detective Stories <= 11.50:
                if Running <= 12.00:
                  Predict: Duck
                else (Running > 12.00):
                  Predict: Dog
              else (Detective Stories > 11.50):
                Predict: Dog
        else (Swimming > 9.50):
          if Detective Stories <= 8.50:
            if Art <= 7.00:
              Predict: Duck
            else (Art > 7.00):
              Predict: Dog
          else (Detective Stories > 8.50):
            if Detective Stories <= 12.00:
              Predict: Duck
            else (Detective Stories > 12.00):
              Predict: Dog

Tree 18 Rules:
if Gardening <= 4.50:
  if Swimming <= 4.50:
    if Running <= 8.50:
      Predict: Cat
    else (Running > 8.50):
      if Art <= 2.00:
        Predict: Cat
      else (Art > 2.00):
        if Detective Stories <= 5.00:
          Predict: Cat
        else (Detective Stories > 5.00):
          if Running <= 12.50:
            if Art <= 10.00:
              Predict: Duck
            else (Art > 10.00):
              if Detective Stories <= 10.50:
                if Running <= 9.50:
                  Predict: Cat
                else (Running > 9.50):
                  Predict: Duck
              else (Detective Stories > 10.50):
                Predict: Cat
          else (Running > 12.50):
            Predict: Cat
  else (Swimming > 4.50):
    if Gardening <= 3.50:
      if Running <= 3.50:
        if Art <= 9.50:
          Predict: Duck
        else (Art > 9.50):
          Predict: Cat
      else (Running > 3.50):
        if Running <= 13.50:
          Predict: Duck
        else (Running > 13.50):
          if Detective Stories <= 11.00:
            Predict: Duck
          else (Detective Stories > 11.00):
            if Detective Stories <= 13.00:
              Predict: Dog
            else (Detective Stories > 13.00):
              Predict: Duck
    else (Gardening > 3.50):
      if Running <= 12.50:
        Predict: Duck
      else (Running > 12.50):
        Predict: Dog
else (Gardening > 4.50):
  if Swimming <= 4.50:
    if Detective Stories <= 6.50:
      if Gardening <= 6.50:
        if Running <= 8.00:
          Predict: Cat
        else (Running > 8.00):
          Predict: Duck
      else (Gardening > 6.50):
        if Art <= 4.50:
          Predict: Dog
        else (Art > 4.50):
          Predict: Cat
    else (Detective Stories > 6.50):
      Predict: Cat
  else (Swimming > 4.50):
    if Swimming <= 9.50:
      if Art <= 5.50:
        if Running <= 6.50:
          if Art <= 2.50:
            Predict: Dog
          else (Art > 2.50):
            if Gardening <= 11.00:
              Predict: Duck
            else (Gardening > 11.00):
              Predict: Dog
        else (Running > 6.50):
          Predict: Dog
      else (Art > 5.50):
        if Running <= 9.50:
          Predict: Duck
        else (Running > 9.50):
          Predict: Dog
    else (Swimming > 9.50):
      if Detective Stories <= 12.50:
        if Gardening <= 8.50:
          if Detective Stories <= 6.50:
            Predict: Dog
          else (Detective Stories > 6.50):
            if Art <= 4.50:
              if Running <= 5.00:
                Predict: Duck
              else (Running > 5.00):
                Predict: Dog
            else (Art > 4.50):
              if Art <= 9.50:
                Predict: Duck
              else (Art > 9.50):
                if Detective Stories <= 11.50:
                  if Gardening <= 6.50:
                    if Swimming <= 12.00:
                      Predict: Duck
                    else (Swimming > 12.00):
                      if Running <= 13.50:
                        Predict: Duck
                      else (Running > 13.50):
                        Predict: Cat
                  else (Gardening > 6.50):
                    Predict: Cat
                else (Detective Stories > 11.50):
                  Predict: Duck
        else (Gardening > 8.50):
          if Art <= 5.50:
            Predict: Dog
          else (Art > 5.50):
            if Art <= 7.50:
              if Running <= 10.00:
                Predict: Duck
              else (Running > 10.00):
                if Gardening <= 12.00:
                  Predict: Dog
                else (Gardening > 12.00):
                  Predict: Duck
            else (Art > 7.50):
              if Gardening <= 9.50:
                Predict: Dog
              else (Gardening > 9.50):
                if Swimming <= 13.50:
                  Predict: Dog
                else (Swimming > 13.50):
                  Predict: Cat
      else (Detective Stories > 12.50):
        if Art <= 13.50:
          if Swimming <= 10.50:
            if Art <= 6.00:
              Predict: Dog
            else (Art > 6.00):
              Predict: Duck
          else (Swimming > 10.50):
            Predict: Dog
        else (Art > 13.50):
          Predict: Duck

Tree 19 Rules:
if Gardening <= 4.50:
  if Swimming <= 4.50:
    if Detective Stories <= 10.50:
      if Running <= 9.50:
        Predict: Cat
      else (Running > 9.50):
        if Art <= 2.00:
          Predict: Cat
        else (Art > 2.00):
          Predict: Duck
    else (Detective Stories > 10.50):
      if Running <= 13.50:
        if Swimming <= 1.50:
          if Art <= 9.50:
            Predict: Duck
          else (Art > 9.50):
            Predict: Cat
        else (Swimming > 1.50):
          Predict: Cat
      else (Running > 13.50):
        Predict: Duck
  else (Swimming > 4.50):
    if Art <= 11.50:
      if Art <= 2.50:
        if Detective Stories <= 11.00:
          if Swimming <= 11.50:
            Predict: Dog
          else (Swimming > 11.50):
            Predict: Duck
        else (Detective Stories > 11.00):
          Predict: Duck
      else (Art > 2.50):
        Predict: Duck
    else (Art > 11.50):
      if Running <= 4.50:
        Predict: Cat
      else (Running > 4.50):
        Predict: Duck
else (Gardening > 4.50):
  if Art <= 9.50:
    if Running <= 3.50:
      if Swimming <= 5.00:
        if Art <= 4.00:
          Predict: Dog
        else (Art > 4.00):
          Predict: Cat
      else (Swimming > 5.00):
        Predict: Dog
    else (Running > 3.50):
      if Detective Stories <= 4.50:
        if Art <= 1.00:
          Predict: Duck
        else (Art > 1.00):
          if Detective Stories <= 2.50:
            Predict: Dog
          else (Detective Stories > 2.50):
            if Art <= 7.00:
              Predict: Duck
            else (Art > 7.00):
              Predict: Dog
      else (Detective Stories > 4.50):
        if Art <= 4.50:
          if Swimming <= 13.50:
            if Running <= 6.50:
              if Swimming <= 7.50:
                Predict: Dog
              else (Swimming > 7.50):
                if Swimming <= 8.50:
                  Predict: Duck
                else (Swimming > 8.50):
                  Predict: Dog
            else (Running > 6.50):
              Predict: Dog
          else (Swimming > 13.50):
            if Gardening <= 6.50:
              if Running <= 7.00:
                Predict: Duck
              else (Running > 7.00):
                Predict: Dog
            else (Gardening > 6.50):
              Predict: Dog
        else (Art > 4.50):
          if Gardening <= 9.50:
            if Swimming <= 11.50:
              if Running <= 5.00:
                Predict: Duck
              else (Running > 5.00):
                if Running <= 9.00:
                  if Art <= 6.00:
                    Predict: Dog
                  else (Art > 6.00):
                    Predict: Duck
                else (Running > 9.00):
                  if Detective Stories <= 8.00:
                    Predict: Duck
                  else (Detective Stories > 8.00):
                    Predict: Dog
            else (Swimming > 11.50):
              Predict: Dog
          else (Gardening > 9.50):
            if Running <= 10.50:
              if Gardening <= 13.50:
                if Art <= 5.50:
                  Predict: Dog
                else (Art > 5.50):
                  if Swimming <= 8.50:
                    if Gardening <= 11.50:
                      Predict: Duck
                    else (Gardening > 11.50):
                      Predict: Dog
                  else (Swimming > 8.50):
                    Predict: Duck
              else (Gardening > 13.50):
                Predict: Duck
            else (Running > 10.50):
              Predict: Dog
  else (Art > 9.50):
    if Running <= 9.00:
      if Running <= 7.50:
        Predict: Cat
      else (Running > 7.50):
        if Detective Stories <= 11.50:
          Predict: Cat
        else (Detective Stories > 11.50):
          Predict: Duck
    else (Running > 9.00):
      if Detective Stories <= 12.00:
        if Gardening <= 8.00:
          if Detective Stories <= 10.50:
            Predict: Dog
          else (Detective Stories > 10.50):
            if Running <= 13.50:
              Predict: Duck
            else (Running > 13.50):
              Predict: Cat
        else (Gardening > 8.00):
          Predict: Duck
      else (Detective Stories > 12.00):
        if Art <= 11.50:
          Predict: Cat
        else (Art > 11.50):
          Predict: Dog

Aggregated Rules (Across Forest):

Class: Duck

Simple Rules (Single Trees):

Tree 0 Rules:
if Swimming <= 4.50:
  if Detective Stories <= 2.50:
    if Swimming <= 2.50:
      Predict: Dog
    else (Swimming > 2.50):
      Predict: Cat
  else (Detective Stories > 2.50):
    if Running <= 8.50:
      if Swimming <= 0.50:
        if Detective Stories <= 6.00:
          Predict: Dog
        else (Detective Stories > 6.00):
          Predict: Cat
      else (Swimming > 0.50):
        Predict: Cat
    else (Running > 8.50):
      if Detective Stories <= 8.00:
        Predict: Dog
      else (Detective Stories > 8.00):
        if Gardening <= 1.50:
          if Gardening <= 0.50:
            if Art <= 12.00:
              Predict: Duck
            else (Art > 12.00):
              Predict: Cat
          else (Gardening > 0.50):
            Predict: Duck
        else (Gardening > 1.50):
          if Gardening <= 3.50:
            if Swimming <= 2.00:
              Predict: Duck
            else (Swimming > 2.00):
              if Detective Stories <= 11.00:
                if Detective Stories <= 9.50:
                  Predict: Cat
                else (Detective Stories > 9.50):
                  Predict: Duck
              else (Detective Stories > 11.00):
                Predict: Cat
          else (Gardening > 3.50):
            Predict: Cat
else (Swimming > 4.50):
  if Art <= 4.50:
    if Running <= 6.50:
      if Gardening <= 6.50:
        Predict: Duck
      else (Gardening > 6.50):
        if Detective Stories <= 9.00:
          Predict: Dog
        else (Detective Stories > 9.00):
          if Detective Stories <= 13.00:
            if Gardening <= 11.00:
              Predict: Duck
            else (Gardening > 11.00):
              Predict: Dog
          else (Detective Stories > 13.00):
            Predict: Dog
    else (Running > 6.50):
      if Gardening <= 3.50:
        Predict: Duck
      else (Gardening > 3.50):
        Predict: Dog
  else (Art > 4.50):
    if Gardening <= 9.50:
      if Swimming <= 7.50:
        if Detective Stories <= 8.00:
          Predict: Duck
        else (Detective Stories > 8.00):
          if Art <= 6.50:
            Predict: Dog
          else (Art > 6.50):
            if Art <= 13.50:
              if Gardening <= 4.50:
                Predict: Duck
              else (Gardening > 4.50):
                if Swimming <= 6.50:
                  if Art <= 10.00:
                    Predict: Duck
                  else (Art > 10.00):
                    Predict: Dog
                else (Swimming > 6.50):
                  Predict: Dog
            else (Art > 13.50):
              Predict: Duck
      else (Swimming > 7.50):
        if Running <= 13.50:
          if Running <= 4.50:
            if Swimming <= 10.50:
              Predict: Duck
            else (Swimming > 10.50):
              if Detective Stories <= 10.50:
                if Gardening <= 6.50:
                  Predict: Duck
                else (Gardening > 6.50):
                  Predict: Cat
              else (Detective Stories > 10.50):
                Predict: Cat
          else (Running > 4.50):
            if Swimming <= 9.50:
              if Gardening <= 4.50:
                Predict: Duck
              else (Gardening > 4.50):
                if Gardening <= 7.50:
                  Predict: Dog
                else (Gardening > 7.50):
                  Predict: Duck
            else (Swimming > 9.50):
              Predict: Duck
        else (Running > 13.50):
          if Gardening <= 4.00:
            Predict: Duck
          else (Gardening > 4.00):
            if Swimming <= 12.50:
              Predict: Dog
            else (Swimming > 12.50):
              Predict: Cat
    else (Gardening > 9.50):
      if Swimming <= 9.50:
        Predict: Dog
      else (Swimming > 9.50):
        if Detective Stories <= 12.00:
          if Swimming <= 12.50:
            Predict: Duck
          else (Swimming > 12.50):
            if Running <= 9.00:
              Predict: Duck
            else (Running > 9.00):
              Predict: Dog
        else (Detective Stories > 12.00):
          Predict: Dog

Tree 1 Rules:
if Art <= 4.50:
  if Running <= 4.50:
    if Art <= 0.50:
      Predict: Cat
    else (Art > 0.50):
      if Swimming <= 5.00:
        if Detective Stories <= 4.00:
          Predict: Dog
        else (Detective Stories > 4.00):
          Predict: Cat
      else (Swimming > 5.00):
        if Running <= 3.00:
          Predict: Dog
        else (Running > 3.00):
          if Art <= 1.50:
            Predict: Dog
          else (Art > 1.50):
            if Art <= 3.00:
              Predict: Duck
            else (Art > 3.00):
              Predict: Dog
  else (Running > 4.50):
    if Swimming <= 3.50:
      Predict: Cat
    else (Swimming > 3.50):
      if Swimming <= 11.50:
        if Gardening <= 4.50:
          if Detective Stories <= 11.00:
            Predict: Duck
          else (Detective Stories > 11.00):
            Predict: Dog
        else (Gardening > 4.50):
          if Running <= 6.50:
            if Gardening <= 10.50:
              Predict: Duck
            else (Gardening > 10.50):
              Predict: Dog
          else (Running > 6.50):
            Predict: Dog
      else (Swimming > 11.50):
        if Detective Stories <= 5.50:
          Predict: Duck
        else (Detective Stories > 5.50):
          if Running <= 10.50:
            if Gardening <= 4.00:
              Predict: Duck
            else (Gardening > 4.00):
              Predict: Dog
          else (Running > 10.50):
            if Gardening <= 4.00:
              Predict: Duck
            else (Gardening > 4.00):
              Predict: Dog
else (Art > 4.50):
  if Swimming <= 4.50:
    if Running <= 8.50:
      if Swimming <= 0.50:
        if Detective Stories <= 6.00:
          Predict: Dog
        else (Detective Stories > 6.00):
          Predict: Cat
      else (Swimming > 0.50):
        Predict: Cat
    else (Running > 8.50):
      if Running <= 10.50:
        if Detective Stories <= 5.50:
          Predict: Cat
        else (Detective Stories > 5.50):
          Predict: Duck
      else (Running > 10.50):
        if Art <= 8.50:
          Predict: Duck
        else (Art > 8.50):
          Predict: Cat
  else (Swimming > 4.50):
    if Gardening <= 5.50:
      if Running <= 13.50:
        if Swimming <= 12.50:
          Predict: Duck
        else (Swimming > 12.50):
          if Running <= 5.00:
            Predict: Cat
          else (Running > 5.00):
            Predict: Duck
      else (Running > 13.50):
        if Detective Stories <= 10.50:
          Predict: Duck
        else (Detective Stories > 10.50):
          if Art <= 8.50:
            Predict: Dog
          else (Art > 8.50):
            Predict: Cat
    else (Gardening > 5.50):
      if Running <= 4.50:
        Predict: Cat
      else (Running > 4.50):
        if Detective Stories <= 2.00:
          Predict: Dog
        else (Detective Stories > 2.00):
          if Running <= 9.50:
            if Swimming <= 10.50:
              if Art <= 5.50:
                Predict: Dog
              else (Art > 5.50):
                Predict: Duck
            else (Swimming > 10.50):
              Predict: Duck
          else (Running > 9.50):
            if Swimming <= 8.50:
              Predict: Dog
            else (Swimming > 8.50):
              if Running <= 10.50:
                Predict: Dog
              else (Running > 10.50):
                if Gardening <= 13.50:
                  if Detective Stories <= 7.50:
                    if Detective Stories <= 4.50:
                      Predict: Duck
                    else (Detective Stories > 4.50):
                      Predict: Dog
                  else (Detective Stories > 7.50):
                    Predict: Duck
                else (Gardening > 13.50):
                  Predict: Dog

Tree 2 Rules:
if Art <= 4.50:
  if Gardening <= 4.50:
    if Swimming <= 4.50:
      if Running <= 8.00:
        Predict: Cat
      else (Running > 8.00):
        Predict: Duck
    else (Swimming > 4.50):
      if Running <= 13.00:
        if Gardening <= 0.50:
          Predict: Duck
        else (Gardening > 0.50):
          if Running <= 8.50:
            Predict: Duck
          else (Running > 8.50):
            if Detective Stories <= 10.50:
              Predict: Dog
            else (Detective Stories > 10.50):
              Predict: Duck
      else (Running > 13.00):
        Predict: Dog
  else (Gardening > 4.50):
    if Swimming <= 13.50:
      if Running <= 6.50:
        if Running <= 5.50:
          Predict: Dog
        else (Running > 5.50):
          if Art <= 2.50:
            Predict: Dog
          else (Art > 2.50):
            if Swimming <= 10.00:
              if Swimming <= 4.00:
                Predict: Dog
              else (Swimming > 4.00):
                Predict: Duck
            else (Swimming > 10.00):
              Predict: Dog
      else (Running > 6.50):
        Predict: Dog
    else (Swimming > 13.50):
      if Running <= 7.00:
        Predict: Duck
      else (Running > 7.00):
        Predict: Dog
else (Art > 4.50):
  if Art <= 9.50:
    if Swimming <= 4.50:
      if Art <= 8.50:
        if Running <= 10.50:
          Predict: Cat
        else (Running > 10.50):
          Predict: Duck
      else (Art > 8.50):
        if Gardening <= 6.00:
          Predict: Duck
        else (Gardening > 6.00):
          Predict: Cat
    else (Swimming > 4.50):
      if Detective Stories <= 6.50:
        if Swimming <= 13.00:
          if Art <= 5.50:
            Predict: Dog
          else (Art > 5.50):
            if Gardening <= 9.50:
              Predict: Duck
            else (Gardening > 9.50):
              if Swimming <= 10.50:
                Predict: Duck
              else (Swimming > 10.50):
                Predict: Dog
        else (Swimming > 13.00):
          Predict: Dog
      else (Detective Stories > 6.50):
        if Running <= 9.50:
          if Gardening <= 10.50:
            Predict: Duck
          else (Gardening > 10.50):
            if Swimming <= 10.50:
              Predict: Dog
            else (Swimming > 10.50):
              Predict: Duck
        else (Running > 9.50):
          if Art <= 6.50:
            if Gardening <= 4.50:
              Predict: Duck
            else (Gardening > 4.50):
              Predict: Dog
          else (Art > 6.50):
            if Gardening <= 9.50:
              if Running <= 10.50:
                if Swimming <= 8.50:
                  Predict: Duck
                else (Swimming > 8.50):
                  Predict: Dog
              else (Running > 10.50):
                Predict: Duck
            else (Gardening > 9.50):
              Predict: Dog
  else (Art > 9.50):
    if Detective Stories <= 2.50:
      if Swimming <= 7.00:
        Predict: Cat
      else (Swimming > 7.00):
        Predict: Duck
    else (Detective Stories > 2.50):
      if Running <= 4.50:
        Predict: Cat
      else (Running > 4.50):
        if Swimming <= 7.50:
          if Gardening <= 11.50:
            if Gardening <= 2.00:
              if Swimming <= 2.50:
                Predict: Duck
              else (Swimming > 2.50):
                Predict: Cat
            else (Gardening > 2.00):
              Predict: Cat
          else (Gardening > 11.50):
            Predict: Dog
        else (Swimming > 7.50):
          if Swimming <= 9.50:
            if Gardening <= 4.50:
              Predict: Duck
            else (Gardening > 4.50):
              if Running <= 12.00:
                Predict: Duck
              else (Running > 12.00):
                Predict: Dog
          else (Swimming > 9.50):
            Predict: Duck

Tree 3 Rules:
if Running <= 4.50:
  if Gardening <= 10.00:
    if Art <= 5.50:
      if Swimming <= 10.50:
        if Gardening <= 4.00:
          Predict: Cat
        else (Gardening > 4.00):
          if Detective Stories <= 11.50:
            Predict: Cat
          else (Detective Stories > 11.50):
            Predict: Dog
      else (Swimming > 10.50):
        if Detective Stories <= 6.00:
          Predict: Dog
        else (Detective Stories > 6.00):
          Predict: Duck
    else (Art > 5.50):
      if Swimming <= 7.00:
        Predict: Cat
      else (Swimming > 7.00):
        if Running <= 2.50:
          if Gardening <= 2.50:
            Predict: Duck
          else (Gardening > 2.50):
            if Swimming <= 12.50:
              if Art <= 10.00:
                Predict: Duck
              else (Art > 10.00):
                Predict: Cat
            else (Swimming > 12.50):
              Predict: Cat
        else (Running > 2.50):
          Predict: Cat
  else (Gardening > 10.00):
    if Detective Stories <= 9.50:
      Predict: Dog
    else (Detective Stories > 9.50):
      if Art <= 5.50:
        Predict: Dog
      else (Art > 5.50):
        Predict: Cat
else (Running > 4.50):
  if Art <= 4.50:
    if Running <= 6.50:
      if Detective Stories <= 3.50:
        Predict: Duck
      else (Detective Stories > 3.50):
        Predict: Dog
    else (Running > 6.50):
      if Swimming <= 13.50:
        Predict: Dog
      else (Swimming > 13.50):
        if Running <= 12.50:
          if Gardening <= 5.50:
            Predict: Duck
          else (Gardening > 5.50):
            Predict: Dog
        else (Running > 12.50):
          Predict: Dog
  else (Art > 4.50):
    if Running <= 10.50:
      if Gardening <= 4.50:
        if Swimming <= 5.00:
          if Art <= 11.50:
            Predict: Duck
          else (Art > 11.50):
            if Running <= 9.50:
              Predict: Cat
            else (Running > 9.50):
              Predict: Duck
        else (Swimming > 5.00):
          Predict: Duck
      else (Gardening > 4.50):
        if Swimming <= 4.00:
          if Gardening <= 8.00:
            Predict: Cat
          else (Gardening > 8.00):
            if Art <= 7.50:
              Predict: Dog
            else (Art > 7.50):
              Predict: Cat
        else (Swimming > 4.00):
          if Art <= 5.50:
            if Running <= 8.50:
              Predict: Dog
            else (Running > 8.50):
              Predict: Duck
          else (Art > 5.50):
            if Detective Stories <= 3.00:
              Predict: Dog
            else (Detective Stories > 3.00):
              Predict: Duck
    else (Running > 10.50):
      if Gardening <= 4.50:
        if Swimming <= 4.50:
          if Gardening <= 3.50:
            Predict: Cat
          else (Gardening > 3.50):
            if Running <= 13.50:
              Predict: Cat
            else (Running > 13.50):
              Predict: Duck
        else (Swimming > 4.50):
          Predict: Duck
      else (Gardening > 4.50):
        if Art <= 9.50:
          if Gardening <= 6.50:
            if Running <= 13.50:
              Predict: Dog
            else (Running > 13.50):
              Predict: Duck
          else (Gardening > 6.50):
            if Detective Stories <= 4.50:
              if Art <= 7.00:
                Predict: Duck
              else (Art > 7.00):
                Predict: Dog
            else (Detective Stories > 4.50):
              Predict: Dog
        else (Art > 9.50):
          if Swimming <= 5.00:
            Predict: Cat
          else (Swimming > 5.00):
            if Detective Stories <= 12.00:
              if Running <= 12.00:
                Predict: Duck
              else (Running > 12.00):
                if Running <= 13.50:
                  Predict: Dog
                else (Running > 13.50):
                  Predict: Cat
            else (Detective Stories > 12.00):
              Predict: Dog

Tree 4 Rules:
if Swimming <= 4.50:
  if Running <= 13.50:
    if Running <= 9.00:
      if Detective Stories <= 5.50:
        if Running <= 4.00:
          Predict: Cat
        else (Running > 4.00):
          Predict: Dog
      else (Detective Stories > 5.50):
        Predict: Cat
    else (Running > 9.00):
      if Running <= 10.50:
        Predict: Duck
      else (Running > 10.50):
        if Art <= 12.00:
          if Art <= 7.50:
            if Gardening <= 3.00:
              Predict: Cat
            else (Gardening > 3.00):
              if Gardening <= 6.50:
                Predict: Duck
              else (Gardening > 6.50):
                Predict: Dog
          else (Art > 7.50):
            Predict: Cat
        else (Art > 12.00):
          if Gardening <= 3.00:
            Predict: Duck
          else (Gardening > 3.00):
            Predict: Cat
  else (Running > 13.50):
    Predict: Duck
else (Swimming > 4.50):
  if Gardening <= 4.50:
    if Running <= 13.50:
      if Running <= 3.50:
        if Detective Stories <= 11.00:
          Predict: Cat
        else (Detective Stories > 11.00):
          Predict: Duck
      else (Running > 3.50):
        Predict: Duck
    else (Running > 13.50):
      if Art <= 5.50:
        Predict: Dog
      else (Art > 5.50):
        Predict: Duck
  else (Gardening > 4.50):
    if Running <= 9.50:
      if Art <= 6.50:
        if Gardening <= 8.50:
          if Art <= 1.50:
            Predict: Dog
          else (Art > 1.50):
            if Art <= 2.50:
              Predict: Duck
            else (Art > 2.50):
              if Running <= 6.50:
                if Gardening <= 6.50:
                  Predict: Dog
                else (Gardening > 6.50):
                  Predict: Duck
              else (Running > 6.50):
                if Swimming <= 10.00:
                  if Gardening <= 7.00:
                    Predict: Dog
                  else (Gardening > 7.00):
                    Predict: Duck
                else (Swimming > 10.00):
                  Predict: Dog
        else (Gardening > 8.50):
          Predict: Dog
      else (Art > 6.50):
        if Running <= 4.50:
          Predict: Cat
        else (Running > 4.50):
          Predict: Duck
    else (Running > 9.50):
      if Art <= 4.50:
        Predict: Dog
      else (Art > 4.50):
        if Swimming <= 9.50:
          Predict: Dog
        else (Swimming > 9.50):
          if Running <= 13.50:
            if Detective Stories <= 12.00:
              if Swimming <= 12.50:
                Predict: Duck
              else (Swimming > 12.50):
                Predict: Dog
            else (Detective Stories > 12.00):
              Predict: Dog
          else (Running > 13.50):
            if Swimming <= 12.50:
              Predict: Duck
            else (Swimming > 12.50):
              Predict: Cat

Tree 5 Rules:
if Running <= 4.50:
  if Art <= 4.50:
    if Gardening <= 5.50:
      Predict: Duck
    else (Gardening > 5.50):
      Predict: Dog
  else (Art > 4.50):
    if Art <= 7.50:
      if Swimming <= 7.00:
        Predict: Cat
      else (Swimming > 7.00):
        Predict: Duck
    else (Art > 7.50):
      Predict: Cat
else (Running > 4.50):
  if Art <= 4.50:
    if Gardening <= 4.50:
      if Running <= 13.50:
        if Running <= 12.50:
          if Running <= 11.00:
            if Swimming <= 11.50:
              Predict: Dog
            else (Swimming > 11.50):
              Predict: Duck
          else (Running > 11.00):
            Predict: Duck
        else (Running > 12.50):
          Predict: Cat
      else (Running > 13.50):
        Predict: Dog
    else (Gardening > 4.50):
      if Detective Stories <= 2.50:
        if Gardening <= 6.50:
          Predict: Duck
        else (Gardening > 6.50):
          Predict: Dog
      else (Detective Stories > 2.50):
        if Running <= 6.50:
          if Swimming <= 8.50:
            if Gardening <= 10.50:
              Predict: Duck
            else (Gardening > 10.50):
              Predict: Dog
          else (Swimming > 8.50):
            Predict: Dog
        else (Running > 6.50):
          Predict: Dog
  else (Art > 4.50):
    if Swimming <= 4.50:
      if Detective Stories <= 6.50:
        if Art <= 9.50:
          Predict: Duck
        else (Art > 9.50):
          Predict: Cat
      else (Detective Stories > 6.50):
        if Art <= 9.50:
          if Running <= 8.00:
            Predict: Cat
          else (Running > 8.00):
            Predict: Duck
        else (Art > 9.50):
          if Swimming <= 1.50:
            if Gardening <= 2.00:
              if Running <= 9.50:
                Predict: Cat
              else (Running > 9.50):
                Predict: Duck
            else (Gardening > 2.00):
              Predict: Cat
          else (Swimming > 1.50):
            Predict: Cat
    else (Swimming > 4.50):
      if Running <= 10.50:
        if Detective Stories <= 5.50:
          if Swimming <= 10.50:
            Predict: Dog
          else (Swimming > 10.50):
            Predict: Duck
        else (Detective Stories > 5.50):
          Predict: Duck
      else (Running > 10.50):
        if Art <= 6.50:
          if Swimming <= 12.50:
            Predict: Dog
          else (Swimming > 12.50):
            Predict: Duck
        else (Art > 6.50):
          if Gardening <= 4.00:
            Predict: Duck
          else (Gardening > 4.00):
            if Detective Stories <= 12.50:
              if Art <= 9.00:
                Predict: Dog
              else (Art > 9.00):
                if Detective Stories <= 11.50:
                  Predict: Cat
                else (Detective Stories > 11.50):
                  Predict: Duck
            else (Detective Stories > 12.50):
              Predict: Dog

Tree 6 Rules:
if Swimming <= 4.50:
  if Running <= 13.50:
    if Running <= 5.50:
      if Detective Stories <= 2.50:
        if Running <= 1.50:
          Predict: Cat
        else (Running > 1.50):
          Predict: Dog
      else (Detective Stories > 2.50):
        Predict: Cat
    else (Running > 5.50):
      if Art <= 6.50:
        if Gardening <= 6.50:
          if Art <= 2.00:
            Predict: Cat
          else (Art > 2.00):
            Predict: Duck
        else (Gardening > 6.50):
          Predict: Dog
      else (Art > 6.50):
        if Art <= 9.50:
          if Swimming <= 2.00:
            Predict: Duck
          else (Swimming > 2.00):
            Predict: Cat
        else (Art > 9.50):
          if Running <= 9.50:
            Predict: Cat
          else (Running > 9.50):
            if Gardening <= 1.00:
              Predict: Duck
            else (Gardening > 1.00):
              if Running <= 10.50:
                Predict: Duck
              else (Running > 10.50):
                Predict: Cat
  else (Running > 13.50):
    if Swimming <= 1.50:
      Predict: Dog
    else (Swimming > 1.50):
      Predict: Duck
else (Swimming > 4.50):
  if Art <= 4.50:
    if Gardening <= 2.50:
      if Art <= 3.50:
        if Gardening <= 0.50:
          Predict: Duck
        else (Gardening > 0.50):
          if Running <= 8.50:
            Predict: Duck
          else (Running > 8.50):
            Predict: Dog
      else (Art > 3.50):
        if Swimming <= 11.00:
          Predict: Dog
        else (Swimming > 11.00):
          Predict: Duck
    else (Gardening > 2.50):
      if Detective Stories <= 3.50:
        if Gardening <= 6.50:
          Predict: Duck
        else (Gardening > 6.50):
          Predict: Dog
      else (Detective Stories > 3.50):
        if Swimming <= 8.50:
          if Swimming <= 7.50:
            Predict: Dog
          else (Swimming > 7.50):
            if Running <= 8.00:
              Predict: Duck
            else (Running > 8.00):
              Predict: Dog
        else (Swimming > 8.50):
          Predict: Dog
  else (Art > 4.50):
    if Detective Stories <= 1.50:
      if Art <= 9.00:
        Predict: Dog
      else (Art > 9.00):
        Predict: Duck
    else (Detective Stories > 1.50):
      if Running <= 10.50:
        if Gardening <= 10.50:
          if Art <= 9.50:
            if Detective Stories <= 5.50:
              if Running <= 7.50:
                Predict: Duck
              else (Running > 7.50):
                Predict: Dog
            else (Detective Stories > 5.50):
              if Swimming <= 13.50:
                if Running <= 9.50:
                  Predict: Duck
                else (Running > 9.50):
                  if Gardening <= 4.00:
                    Predict: Duck
                  else (Gardening > 4.00):
                    Predict: Dog
              else (Swimming > 13.50):
                if Detective Stories <= 11.50:
                  Predict: Duck
                else (Detective Stories > 11.50):
                  Predict: Dog
          else (Art > 9.50):
            if Running <= 4.00:
              Predict: Cat
            else (Running > 4.00):
              Predict: Duck
        else (Gardening > 10.50):
          Predict: Dog
      else (Running > 10.50):
        if Swimming <= 8.50:
          if Gardening <= 2.00:
            Predict: Duck
          else (Gardening > 2.00):
            Predict: Dog
        else (Swimming > 8.50):
          if Running <= 13.50:
            if Swimming <= 13.50:
              if Detective Stories <= 12.50:
                if Gardening <= 13.50:
                  if Running <= 11.50:
                    if Art <= 8.00:
                      Predict: Dog
                    else (Art > 8.00):
                      Predict: Duck
                  else (Running > 11.50):
                    Predict: Duck
                else (Gardening > 13.50):
                  Predict: Dog
              else (Detective Stories > 12.50):
                Predict: Dog
            else (Swimming > 13.50):
              if Gardening <= 5.50:
                Predict: Duck
              else (Gardening > 5.50):
                Predict: Dog
          else (Running > 13.50):
            Predict: Duck

Tree 7 Rules:
if Running <= 4.50:
  if Art <= 5.50:
    if Swimming <= 10.00:
      if Art <= 0.50:
        Predict: Cat
      else (Art > 0.50):
        Predict: Dog
    else (Swimming > 10.00):
      Predict: Duck
  else (Art > 5.50):
    if Art <= 9.50:
      if Swimming <= 7.00:
        Predict: Cat
      else (Swimming > 7.00):
        Predict: Duck
    else (Art > 9.50):
      Predict: Cat
else (Running > 4.50):
  if Swimming <= 9.50:
    if Art <= 4.50:
      if Swimming <= 7.50:
        if Gardening <= 3.00:
          Predict: Duck
        else (Gardening > 3.00):
          Predict: Dog
      else (Swimming > 7.50):
        Predict: Dog
    else (Art > 4.50):
      if Gardening <= 9.50:
        if Swimming <= 4.50:
          if Detective Stories <= 7.50:
            Predict: Duck
          else (Detective Stories > 7.50):
            if Running <= 13.50:
              if Art <= 9.50:
                Predict: Duck
              else (Art > 9.50):
                Predict: Cat
            else (Running > 13.50):
              Predict: Duck
        else (Swimming > 4.50):
          if Gardening <= 5.00:
            Predict: Duck
          else (Gardening > 5.00):
            if Art <= 10.00:
              if Gardening <= 7.50:
                Predict: Dog
              else (Gardening > 7.50):
                if Gardening <= 8.50:
                  Predict: Duck
                else (Gardening > 8.50):
                  if Art <= 6.50:
                    Predict: Dog
                  else (Art > 6.50):
                    Predict: Duck
            else (Art > 10.00):
              Predict: Dog
      else (Gardening > 9.50):
        Predict: Dog
  else (Swimming > 9.50):
    if Gardening <= 4.50:
      Predict: Duck
    else (Gardening > 4.50):
      if Art <= 4.50:
        Predict: Dog
      else (Art > 4.50):
        if Gardening <= 8.50:
          if Swimming <= 12.50:
            Predict: Duck
          else (Swimming > 12.50):
            if Running <= 13.50:
              if Detective Stories <= 5.50:
                Predict: Dog
              else (Detective Stories > 5.50):
                Predict: Duck
            else (Running > 13.50):
              Predict: Cat
        else (Gardening > 8.50):
          if Swimming <= 11.00:
            if Art <= 8.50:
              Predict: Dog
            else (Art > 8.50):
              Predict: Duck
          else (Swimming > 11.00):
            if Gardening <= 13.50:
              Predict: Dog
            else (Gardening > 13.50):
              if Running <= 11.00:
                Predict: Duck
              else (Running > 11.00):
                Predict: Dog

Tree 8 Rules:
if Swimming <= 4.50:
  if Gardening <= 8.50:
    if Gardening <= 2.50:
      if Detective Stories <= 11.50:
        if Art <= 10.50:
          Predict: Cat
        else (Art > 10.50):
          if Art <= 11.50:
            if Gardening <= 0.50:
              if Running <= 6.00:
                Predict: Cat
              else (Running > 6.00):
                Predict: Duck
            else (Gardening > 0.50):
              Predict: Cat
          else (Art > 11.50):
            Predict: Cat
      else (Detective Stories > 11.50):
        if Running <= 6.50:
          Predict: Cat
        else (Running > 6.50):
          Predict: Duck
    else (Gardening > 2.50):
      if Detective Stories <= 6.50:
        if Running <= 8.00:
          Predict: Cat
        else (Running > 8.00):
          Predict: Duck
      else (Detective Stories > 6.50):
        if Detective Stories <= 10.50:
          if Gardening <= 3.50:
            if Running <= 7.00:
              Predict: Cat
            else (Running > 7.00):
              Predict: Duck
          else (Gardening > 3.50):
            Predict: Cat
        else (Detective Stories > 10.50):
          Predict: Cat
  else (Gardening > 8.50):
    if Running <= 5.00:
      Predict: Cat
    else (Running > 5.00):
      if Art <= 7.50:
        Predict: Dog
      else (Art > 7.50):
        Predict: Cat
else (Swimming > 4.50):
  if Running <= 9.50:
    if Art <= 5.50:
      if Art <= 1.50:
        if Art <= 0.50:
          if Running <= 3.50:
            Predict: Duck
          else (Running > 3.50):
            Predict: Dog
        else (Art > 0.50):
          Predict: Dog
      else (Art > 1.50):
        if Detective Stories <= 4.50:
          Predict: Duck
        else (Detective Stories > 4.50):
          if Gardening <= 4.50:
            Predict: Duck
          else (Gardening > 4.50):
            if Gardening <= 9.50:
              if Detective Stories <= 11.00:
                Predict: Dog
              else (Detective Stories > 11.00):
                if Swimming <= 10.50:
                  Predict: Duck
                else (Swimming > 10.50):
                  Predict: Dog
            else (Gardening > 9.50):
              Predict: Dog
    else (Art > 5.50):
      Predict: Duck
  else (Running > 9.50):
    if Swimming <= 9.50:
      if Detective Stories <= 12.50:
        if Detective Stories <= 10.50:
          if Running <= 13.50:
            if Art <= 9.00:
              Predict: Dog
            else (Art > 9.00):
              if Gardening <= 8.00:
                Predict: Dog
              else (Gardening > 8.00):
                Predict: Duck
          else (Running > 13.50):
            if Art <= 10.50:
              Predict: Dog
            else (Art > 10.50):
              Predict: Duck
        else (Detective Stories > 10.50):
          Predict: Dog
      else (Detective Stories > 12.50):
        if Gardening <= 4.50:
          Predict: Duck
        else (Gardening > 4.50):
          Predict: Dog
    else (Swimming > 9.50):
      if Gardening <= 6.50:
        if Running <= 10.50:
          if Swimming <= 13.00:
            Predict: Duck
          else (Swimming > 13.00):
            Predict: Dog
        else (Running > 10.50):
          if Running <= 12.50:
            Predict: Duck
          else (Running > 12.50):
            if Art <= 4.50:
              Predict: Dog
            else (Art > 4.50):
              if Gardening <= 4.00:
                Predict: Duck
              else (Gardening > 4.00):
                if Detective Stories <= 8.50:
                  Predict: Dog
                else (Detective Stories > 8.50):
                  if Running <= 13.50:
                    Predict: Duck
                  else (Running > 13.50):
                    if Swimming <= 12.50:
                      Predict: Duck
                    else (Swimming > 12.50):
                      Predict: Cat
      else (Gardening > 6.50):
        if Detective Stories <= 4.50:
          if Running <= 11.50:
            Predict: Dog
          else (Running > 11.50):
            Predict: Duck
        else (Detective Stories > 4.50):
          if Gardening <= 13.50:
            if Art <= 9.00:
              Predict: Dog
            else (Art > 9.00):
              if Art <= 12.50:
                Predict: Duck
              else (Art > 12.50):
                Predict: Dog
          else (Gardening > 13.50):
            if Swimming <= 12.50:
              Predict: Duck
            else (Swimming > 12.50):
              Predict: Dog

Tree 9 Rules:
if Running <= 4.50:
  if Running <= 3.50:
    if Art <= 0.50:
      Predict: Duck
    else (Art > 0.50):
      if Swimming <= 5.00:
        Predict: Cat
      else (Swimming > 5.00):
        if Swimming <= 10.50:
          if Detective Stories <= 9.50:
            Predict: Dog
          else (Detective Stories > 9.50):
            Predict: Duck
        else (Swimming > 10.50):
          Predict: Cat
  else (Running > 3.50):
    if Gardening <= 4.50:
      Predict: Cat
    else (Gardening > 4.50):
      if Detective Stories <= 11.00:
        if Art <= 5.50:
          Predict: Duck
        else (Art > 5.50):
          Predict: Cat
      else (Detective Stories > 11.00):
        Predict: Cat
else (Running > 4.50):
  if Art <= 4.50:
    if Running <= 9.00:
      if Art <= 1.50:
        Predict: Dog
      else (Art > 1.50):
        if Gardening <= 9.00:
          if Swimming <= 6.50:
            if Art <= 3.50:
              Predict: Duck
            else (Art > 3.50):
              Predict: Dog
          else (Swimming > 6.50):
            Predict: Duck
        else (Gardening > 9.00):
          Predict: Dog
    else (Running > 9.00):
      if Gardening <= 3.50:
        if Swimming <= 11.50:
          if Running <= 11.50:
            Predict: Dog
          else (Running > 11.50):
            Predict: Cat
        else (Swimming > 11.50):
          Predict: Duck
      else (Gardening > 3.50):
        Predict: Dog
  else (Art > 4.50):
    if Swimming <= 4.50:
      if Art <= 9.50:
        if Art <= 8.50:
          if Detective Stories <= 8.50:
            Predict: Dog
          else (Detective Stories > 8.50):
            Predict: Cat
        else (Art > 8.50):
          Predict: Duck
      else (Art > 9.50):
        if Running <= 9.00:
          Predict: Cat
        else (Running > 9.00):
          if Gardening <= 1.00:
            Predict: Duck
          else (Gardening > 1.00):
            Predict: Cat
    else (Swimming > 4.50):
      if Detective Stories <= 1.50:
        Predict: Dog
      else (Detective Stories > 1.50):
        if Gardening <= 4.50:
          if Art <= 7.50:
            if Gardening <= 3.50:
              Predict: Duck
            else (Gardening > 3.50):
              if Running <= 10.50:
                Predict: Duck
              else (Running > 10.50):
                Predict: Dog
          else (Art > 7.50):
            Predict: Duck
        else (Gardening > 4.50):
          if Running <= 9.50:
            if Detective Stories <= 11.50:
              Predict: Duck
            else (Detective Stories > 11.50):
              if Gardening <= 8.50:
                Predict: Duck
              else (Gardening > 8.50):
                if Detective Stories <= 12.50:
                  Predict: Dog
                else (Detective Stories > 12.50):
                  Predict: Duck
          else (Running > 9.50):
            if Art <= 5.50:
              Predict: Duck
            else (Art > 5.50):
              if Detective Stories <= 4.50:
                Predict: Duck
              else (Detective Stories > 4.50):
                if Swimming <= 9.50:
                  Predict: Dog
                else (Swimming > 9.50):
                  if Detective Stories <= 10.50:
                    Predict: Dog
                  else (Detective Stories > 10.50):
                    if Detective Stories <= 12.50:
                      Predict: Duck
                    else (Detective Stories > 12.50):
                      Predict: Dog

Tree 10 Rules:
if Running <= 4.50:
  if Detective Stories <= 2.50:
    if Gardening <= 8.00:
      Predict: Dog
    else (Gardening > 8.00):
      Predict: Cat
  else (Detective Stories > 2.50):
    if Gardening <= 5.50:
      if Art <= 9.50:
        if Swimming <= 7.00:
          Predict: Cat
        else (Swimming > 7.00):
          if Detective Stories <= 13.00:
            Predict: Duck
          else (Detective Stories > 13.00):
            Predict: Dog
      else (Art > 9.50):
        Predict: Cat
    else (Gardening > 5.50):
      if Art <= 5.00:
        Predict: Dog
      else (Art > 5.00):
        Predict: Cat
else (Running > 4.50):
  if Running <= 9.50:
    if Art <= 5.50:
      if Gardening <= 4.50:
        Predict: Duck
      else (Gardening > 4.50):
        if Swimming <= 8.50:
          if Swimming <= 7.50:
            Predict: Dog
          else (Swimming > 7.50):
            if Art <= 1.50:
              Predict: Dog
            else (Art > 1.50):
              Predict: Duck
        else (Swimming > 8.50):
          Predict: Dog
    else (Art > 5.50):
      if Art <= 9.50:
        Predict: Duck
      else (Art > 9.50):
        if Art <= 13.50:
          if Gardening <= 1.50:
            if Swimming <= 5.00:
              Predict: Cat
            else (Swimming > 5.00):
              Predict: Duck
          else (Gardening > 1.50):
            if Running <= 8.50:
              if Gardening <= 4.00:
                Predict: Cat
              else (Gardening > 4.00):
                if Gardening <= 6.00:
                  if Running <= 5.50:
                    Predict: Cat
                  else (Running > 5.50):
                    Predict: Duck
                else (Gardening > 6.00):
                  Predict: Cat
            else (Running > 8.50):
              Predict: Duck
        else (Art > 13.50):
          Predict: Duck
  else (Running > 9.50):
    if Swimming <= 4.50:
      if Running <= 10.50:
        Predict: Duck
      else (Running > 10.50):
        if Gardening <= 1.50:
          Predict: Duck
        else (Gardening > 1.50):
          if Art <= 10.00:
            if Art <= 5.00:
              Predict: Cat
            else (Art > 5.00):
              Predict: Duck
          else (Art > 10.00):
            Predict: Cat
    else (Swimming > 4.50):
      if Gardening <= 3.50:
        if Running <= 10.50:
          if Swimming <= 11.50:
            if Art <= 4.50:
              Predict: Dog
            else (Art > 4.50):
              Predict: Duck
          else (Swimming > 11.50):
            Predict: Duck
        else (Running > 10.50):
          Predict: Duck
      else (Gardening > 3.50):
        if Art <= 7.50:
          Predict: Dog
        else (Art > 7.50):
          if Art <= 12.50:
            if Detective Stories <= 10.50:
              Predict: Dog
            else (Detective Stories > 10.50):
              if Running <= 12.00:
                if Gardening <= 4.50:
                  Predict: Duck
                else (Gardening > 4.50):
                  if Detective Stories <= 12.50:
                    Predict: Duck
                  else (Detective Stories > 12.50):
                    Predict: Dog
              else (Running > 12.00):
                if Running <= 13.50:
                  Predict: Duck
                else (Running > 13.50):
                  if Art <= 9.00:
                    Predict: Duck
                  else (Art > 9.00):
                    Predict: Cat
          else (Art > 12.50):
            Predict: Dog

Tree 11 Rules:
if Swimming <= 4.50:
  if Running <= 8.50:
    if Gardening <= 10.50:
      Predict: Cat
    else (Gardening > 10.50):
      if Swimming <= 1.50:
        Predict: Dog
      else (Swimming > 1.50):
        Predict: Cat
  else (Running > 8.50):
    if Art <= 10.00:
      if Gardening <= 5.00:
        Predict: Duck
      else (Gardening > 5.00):
        Predict: Dog
    else (Art > 10.00):
      if Gardening <= 3.50:
        if Swimming <= 0.50:
          Predict: Duck
        else (Swimming > 0.50):
          if Gardening <= 2.50:
            if Swimming <= 2.50:
              if Art <= 12.00:
                Predict: Duck
              else (Art > 12.00):
                Predict: Cat
            else (Swimming > 2.50):
              Predict: Cat
          else (Gardening > 2.50):
            Predict: Duck
      else (Gardening > 3.50):
        Predict: Cat
else (Swimming > 4.50):
  if Running <= 9.50:
    if Running <= 4.50:
      if Gardening <= 10.50:
        if Art <= 7.50:
          if Gardening <= 6.00:
            if Running <= 3.00:
              Predict: Dog
            else (Running > 3.00):
              Predict: Duck
          else (Gardening > 6.00):
            Predict: Dog
        else (Art > 7.50):
          Predict: Cat
      else (Gardening > 10.50):
        Predict: Dog
    else (Running > 4.50):
      if Detective Stories <= 6.50:
        if Running <= 7.50:
          if Gardening <= 8.50:
            Predict: Duck
          else (Gardening > 8.50):
            Predict: Dog
        else (Running > 7.50):
          if Running <= 8.50:
            Predict: Dog
          else (Running > 8.50):
            Predict: Duck
      else (Detective Stories > 6.50):
        if Swimming <= 6.50:
          if Gardening <= 3.00:
            Predict: Duck
          else (Gardening > 3.00):
            Predict: Dog
        else (Swimming > 6.50):
          if Detective Stories <= 10.50:
            Predict: Duck
          else (Detective Stories > 10.50):
            if Art <= 4.50:
              if Gardening <= 4.00:
                Predict: Duck
              else (Gardening > 4.00):
                Predict: Dog
            else (Art > 4.50):
              if Swimming <= 13.50:
                Predict: Duck
              else (Swimming > 13.50):
                if Gardening <= 5.50:
                  Predict: Duck
                else (Gardening > 5.50):
                  Predict: Dog
  else (Running > 9.50):
    if Gardening <= 4.50:
      if Art <= 3.00:
        Predict: Dog
      else (Art > 3.00):
        if Running <= 13.50:
          Predict: Duck
        else (Running > 13.50):
          if Gardening <= 1.00:
            if Art <= 7.50:
              Predict: Dog
            else (Art > 7.50):
              Predict: Duck
          else (Gardening > 1.00):
            Predict: Duck
    else (Gardening > 4.50):
      if Gardening <= 6.50:
        if Swimming <= 11.50:
          Predict: Dog
        else (Swimming > 11.50):
          if Swimming <= 13.50:
            if Detective Stories <= 11.50:
              if Swimming <= 12.50:
                Predict: Duck
              else (Swimming > 12.50):
                Predict: Cat
            else (Detective Stories > 11.50):
              if Swimming <= 12.50:
                Predict: Dog
              else (Swimming > 12.50):
                Predict: Duck
          else (Swimming > 13.50):
            Predict: Dog
      else (Gardening > 6.50):
        if Running <= 10.50:
          if Gardening <= 12.50:
            Predict: Dog
          else (Gardening > 12.50):
            Predict: Duck
        else (Running > 10.50):
          Predict: Dog

Tree 12 Rules:
if Swimming <= 4.50:
  if Detective Stories <= 5.50:
    if Art <= 4.50:
      Predict: Dog
    else (Art > 4.50):
      Predict: Cat
  else (Detective Stories > 5.50):
    if Gardening <= 4.50:
      if Swimming <= 3.50:
        if Running <= 8.50:
          Predict: Cat
        else (Running > 8.50):
          if Detective Stories <= 11.50:
            if Running <= 9.50:
              Predict: Cat
            else (Running > 9.50):
              if Gardening <= 1.50:
                Predict: Duck
              else (Gardening > 1.50):
                Predict: Cat
          else (Detective Stories > 11.50):
            if Detective Stories <= 12.50:
              Predict: Duck
            else (Detective Stories > 12.50):
              Predict: Cat
      else (Swimming > 3.50):
        if Running <= 9.00:
          Predict: Cat
        else (Running > 9.00):
          if Running <= 12.50:
            Predict: Duck
          else (Running > 12.50):
            Predict: Cat
    else (Gardening > 4.50):
      if Running <= 13.00:
        Predict: Cat
      else (Running > 13.00):
        Predict: Dog
else (Swimming > 4.50):
  if Swimming <= 9.50:
    if Running <= 9.50:
      if Gardening <= 5.00:
        Predict: Duck
      else (Gardening > 5.00):
        if Art <= 5.50:
          Predict: Dog
        else (Art > 5.50):
          Predict: Duck
    else (Running > 9.50):
      if Art <= 6.50:
        Predict: Dog
      else (Art > 6.50):
        if Detective Stories <= 13.50:
          if Gardening <= 4.50:
            if Swimming <= 7.50:
              Predict: Duck
            else (Swimming > 7.50):
              Predict: Dog
          else (Gardening > 4.50):
            if Gardening <= 9.50:
              if Detective Stories <= 11.50:
                if Gardening <= 8.00:
                  Predict: Dog
                else (Gardening > 8.00):
                  Predict: Duck
              else (Detective Stories > 11.50):
                Predict: Dog
            else (Gardening > 9.50):
              Predict: Dog
        else (Detective Stories > 13.50):
          if Running <= 10.50:
            Predict: Duck
          else (Running > 10.50):
            if Gardening <= 8.00:
              Predict: Duck
            else (Gardening > 8.00):
              Predict: Dog
  else (Swimming > 9.50):
    if Running <= 4.50:
      if Running <= 2.50:
        if Art <= 9.50:
          if Art <= 4.50:
            if Gardening <= 4.50:
              Predict: Duck
            else (Gardening > 4.50):
              Predict: Dog
          else (Art > 4.50):
            Predict: Duck
        else (Art > 9.50):
          Predict: Cat
      else (Running > 2.50):
        Predict: Cat
    else (Running > 4.50):
      if Art <= 4.50:
        if Detective Stories <= 5.00:
          if Swimming <= 11.00:
            Predict: Dog
          else (Swimming > 11.00):
            Predict: Duck
        else (Detective Stories > 5.00):
          if Swimming <= 13.50:
            if Art <= 3.50:
              Predict: Dog
            else (Art > 3.50):
              if Swimming <= 11.50:
                Predict: Dog
              else (Swimming > 11.50):
                if Swimming <= 12.50:
                  Predict: Duck
                else (Swimming > 12.50):
                  Predict: Dog
          else (Swimming > 13.50):
            if Running <= 12.50:
              if Detective Stories <= 11.00:
                Predict: Dog
              else (Detective Stories > 11.00):
                Predict: Duck
            else (Running > 12.50):
              Predict: Dog
      else (Art > 4.50):
        if Detective Stories <= 13.50:
          if Gardening <= 5.50:
            Predict: Duck
          else (Gardening > 5.50):
            if Art <= 6.50:
              if Swimming <= 10.50:
                Predict: Duck
              else (Swimming > 10.50):
                Predict: Dog
            else (Art > 6.50):
              if Gardening <= 6.50:
                if Detective Stories <= 9.00:
                  Predict: Dog
                else (Detective Stories > 9.00):
                  Predict: Duck
              else (Gardening > 6.50):
                Predict: Duck
        else (Detective Stories > 13.50):
          if Art <= 11.50:
            Predict: Duck
          else (Art > 11.50):
            if Gardening <= 8.50:
              Predict: Duck
            else (Gardening > 8.50):
              Predict: Dog

Tree 13 Rules:
if Swimming <= 4.50:
  if Art <= 4.50:
    if Gardening <= 5.50:
      Predict: Cat
    else (Gardening > 5.50):
      Predict: Dog
  else (Art > 4.50):
    if Running <= 13.50:
      Predict: Cat
    else (Running > 13.50):
      Predict: Duck
else (Swimming > 4.50):
  if Running <= 9.50:
    if Art <= 4.50:
      if Gardening <= 4.50:
        Predict: Duck
      else (Gardening > 4.50):
        if Art <= 0.50:
          if Detective Stories <= 6.50:
            Predict: Duck
          else (Detective Stories > 6.50):
            Predict: Dog
        else (Art > 0.50):
          Predict: Dog
    else (Art > 4.50):
      if Gardening <= 6.50:
        Predict: Duck
      else (Gardening > 6.50):
        if Swimming <= 13.50:
          if Detective Stories <= 10.50:
            if Running <= 4.50:
              Predict: Cat
            else (Running > 4.50):
              Predict: Duck
          else (Detective Stories > 10.50):
            Predict: Duck
        else (Swimming > 13.50):
          if Gardening <= 9.50:
            Predict: Dog
          else (Gardening > 9.50):
            Predict: Cat
  else (Running > 9.50):
    if Art <= 3.50:
      if Swimming <= 11.50:
        Predict: Dog
      else (Swimming > 11.50):
        if Running <= 11.50:
          if Gardening <= 2.50:
            Predict: Duck
          else (Gardening > 2.50):
            Predict: Dog
        else (Running > 11.50):
          Predict: Dog
    else (Art > 3.50):
      if Gardening <= 3.50:
        if Art <= 4.50:
          if Swimming <= 11.00:
            Predict: Dog
          else (Swimming > 11.00):
            Predict: Duck
        else (Art > 4.50):
          Predict: Duck
      else (Gardening > 3.50):
        if Gardening <= 6.50:
          if Swimming <= 10.50:
            if Art <= 8.00:
              Predict: Dog
            else (Art > 8.00):
              if Swimming <= 8.00:
                Predict: Duck
              else (Swimming > 8.00):
                Predict: Dog
          else (Swimming > 10.50):
            if Running <= 13.50:
              if Art <= 9.00:
                Predict: Dog
              else (Art > 9.00):
                Predict: Duck
            else (Running > 13.50):
              if Swimming <= 12.50:
                Predict: Duck
              else (Swimming > 12.50):
                Predict: Cat
        else (Gardening > 6.50):
          if Art <= 5.50:
            if Running <= 10.50:
              Predict: Duck
            else (Running > 10.50):
              Predict: Dog
          else (Art > 5.50):
            Predict: Dog

Tree 14 Rules:
if Gardening <= 4.50:
  if Art <= 9.50:
    if Art <= 3.50:
      if Swimming <= 4.50:
        if Running <= 8.00:
          Predict: Cat
        else (Running > 8.00):
          Predict: Duck
      else (Swimming > 4.50):
        if Swimming <= 11.50:
          if Detective Stories <= 12.50:
            if Running <= 9.00:
              Predict: Duck
            else (Running > 9.00):
              Predict: Dog
          else (Detective Stories > 12.50):
            Predict: Dog
        else (Swimming > 11.50):
          Predict: Duck
    else (Art > 3.50):
      if Swimming <= 4.00:
        Predict: Cat
      else (Swimming > 4.00):
        if Detective Stories <= 3.00:
          if Gardening <= 3.50:
            Predict: Duck
          else (Gardening > 3.50):
            Predict: Dog
        else (Detective Stories > 3.00):
          if Art <= 6.50:
            Predict: Duck
          else (Art > 6.50):
            if Gardening <= 3.50:
              Predict: Duck
            else (Gardening > 3.50):
              if Detective Stories <= 12.00:
                Predict: Dog
              else (Detective Stories > 12.00):
                Predict: Duck
  else (Art > 9.50):
    if Art <= 12.50:
      if Gardening <= 3.50:
        if Running <= 8.50:
          if Running <= 5.00:
            Predict: Cat
          else (Running > 5.00):
            if Swimming <= 7.50:
              Predict: Cat
            else (Swimming > 7.50):
              Predict: Duck
        else (Running > 8.50):
          Predict: Duck
      else (Gardening > 3.50):
        Predict: Cat
    else (Art > 12.50):
      if Detective Stories <= 5.00:
        Predict: Cat
      else (Detective Stories > 5.00):
        if Gardening <= 3.50:
          if Detective Stories <= 10.50:
            if Swimming <= 3.50:
              Predict: Cat
            else (Swimming > 3.50):
              Predict: Duck
          else (Detective Stories > 10.50):
            if Detective Stories <= 12.50:
              Predict: Duck
            else (Detective Stories > 12.50):
              if Art <= 13.50:
                if Running <= 5.00:
                  Predict: Cat
                else (Running > 5.00):
                  Predict: Duck
              else (Art > 13.50):
                if Running <= 5.00:
                  Predict: Cat
                else (Running > 5.00):
                  Predict: Duck
        else (Gardening > 3.50):
          if Detective Stories <= 13.00:
            Predict: Cat
          else (Detective Stories > 13.00):
            if Swimming <= 6.00:
              Predict: Cat
            else (Swimming > 6.00):
              Predict: Duck
else (Gardening > 4.50):
  if Swimming <= 4.50:
    if Running <= 11.50:
      if Detective Stories <= 5.50:
        if Gardening <= 8.50:
          Predict: Cat
        else (Gardening > 8.50):
          Predict: Dog
      else (Detective Stories > 5.50):
        Predict: Cat
    else (Running > 11.50):
      if Running <= 13.00:
        if Detective Stories <= 13.50:
          Predict: Dog
        else (Detective Stories > 13.50):
          Predict: Cat
      else (Running > 13.00):
        Predict: Dog
  else (Swimming > 4.50):
    if Art <= 5.50:
      if Running <= 6.50:
        if Detective Stories <= 4.00:
          Predict: Duck
        else (Detective Stories > 4.00):
          if Gardening <= 8.50:
            if Swimming <= 12.00:
              if Detective Stories <= 13.00:
                Predict: Duck
              else (Detective Stories > 13.00):
                Predict: Dog
            else (Swimming > 12.00):
              Predict: Dog
          else (Gardening > 8.50):
            Predict: Dog
      else (Running > 6.50):
        Predict: Dog
    else (Art > 5.50):
      if Swimming <= 9.50:
        if Running <= 9.00:
          Predict: Duck
        else (Running > 9.00):
          Predict: Dog
      else (Swimming > 9.50):
        if Art <= 12.50:
          if Detective Stories <= 13.50:
            if Running <= 10.00:
              Predict: Duck
            else (Running > 10.00):
              if Detective Stories <= 10.50:
                Predict: Dog
              else (Detective Stories > 10.50):
                if Art <= 7.00:
                  Predict: Dog
                else (Art > 7.00):
                  if Gardening <= 5.50:
                    if Swimming <= 13.50:
                      Predict: Cat
                    else (Swimming > 13.50):
                      Predict: Duck
                  else (Gardening > 5.50):
                    Predict: Duck
          else (Detective Stories > 13.50):
            Predict: Cat
        else (Art > 12.50):
          if Running <= 9.00:
            if Gardening <= 9.50:
              if Running <= 3.00:
                Predict: Cat
              else (Running > 3.00):
                Predict: Duck
            else (Gardening > 9.50):
              Predict: Cat
          else (Running > 9.00):
            Predict: Dog

Tree 15 Rules:
if Art <= 9.50:
  if Art <= 4.50:
    if Swimming <= 11.50:
      if Gardening <= 3.50:
        if Swimming <= 4.50:
          Predict: Cat
        else (Swimming > 4.50):
          Predict: Duck
      else (Gardening > 3.50):
        Predict: Dog
    else (Swimming > 11.50):
      if Detective Stories <= 13.50:
        if Gardening <= 4.00:
          Predict: Duck
        else (Gardening > 4.00):
          if Running <= 5.00:
            if Gardening <= 6.00:
              Predict: Duck
            else (Gardening > 6.00):
              Predict: Dog
          else (Running > 5.00):
            Predict: Dog
      else (Detective Stories > 13.50):
        Predict: Dog
  else (Art > 4.50):
    if Swimming <= 4.50:
      if Swimming <= 1.50:
        if Art <= 5.50:
          Predict: Dog
        else (Art > 5.50):
          if Running <= 6.50:
            Predict: Cat
          else (Running > 6.50):
            Predict: Duck
      else (Swimming > 1.50):
        Predict: Cat
    else (Swimming > 4.50):
      if Gardening <= 4.50:
        if Gardening <= 3.50:
          Predict: Duck
        else (Gardening > 3.50):
          if Running <= 11.00:
            Predict: Duck
          else (Running > 11.00):
            Predict: Dog
      else (Gardening > 4.50):
        if Running <= 9.00:
          if Art <= 6.50:
            if Running <= 5.00:
              Predict: Duck
            else (Running > 5.00):
              Predict: Dog
          else (Art > 6.50):
            Predict: Duck
        else (Running > 9.00):
          Predict: Dog
else (Art > 9.50):
  if Running <= 4.50:
    Predict: Cat
  else (Running > 4.50):
    if Gardening <= 11.50:
      if Detective Stories <= 12.50:
        if Running <= 9.50:
          if Detective Stories <= 10.50:
            if Swimming <= 5.00:
              Predict: Cat
            else (Swimming > 5.00):
              Predict: Duck
          else (Detective Stories > 10.50):
            if Gardening <= 2.50:
              Predict: Duck
            else (Gardening > 2.50):
              if Running <= 5.50:
                Predict: Cat
              else (Running > 5.50):
                if Detective Stories <= 11.50:
                  Predict: Duck
                else (Detective Stories > 11.50):
                  Predict: Cat
        else (Running > 9.50):
          if Detective Stories <= 10.50:
            Predict: Duck
          else (Detective Stories > 10.50):
            if Art <= 13.50:
              if Running <= 13.50:
                if Swimming <= 5.00:
                  if Art <= 12.00:
                    Predict: Cat
                  else (Art > 12.00):
                    Predict: Duck
                else (Swimming > 5.00):
                  Predict: Duck
              else (Running > 13.50):
                Predict: Cat
            else (Art > 13.50):
              Predict: Cat
      else (Detective Stories > 12.50):
        if Gardening <= 3.50:
          Predict: Duck
        else (Gardening > 3.50):
          if Art <= 12.00:
            if Gardening <= 6.50:
              if Detective Stories <= 13.50:
                Predict: Cat
              else (Detective Stories > 13.50):
                Predict: Duck
            else (Gardening > 6.50):
              Predict: Cat
          else (Art > 12.00):
            if Swimming <= 5.50:
              Predict: Cat
            else (Swimming > 5.50):
              Predict: Duck
    else (Gardening > 11.50):
      Predict: Dog

Tree 16 Rules:
if Art <= 4.50:
  if Running <= 4.50:
    if Gardening <= 3.50:
      Predict: Cat
    else (Gardening > 3.50):
      if Art <= 2.50:
        if Running <= 3.00:
          if Running <= 1.50:
            Predict: Duck
          else (Running > 1.50):
            Predict: Dog
        else (Running > 3.00):
          Predict: Duck
      else (Art > 2.50):
        Predict: Dog
  else (Running > 4.50):
    if Running <= 8.50:
      if Detective Stories <= 4.50:
        Predict: Duck
      else (Detective Stories > 4.50):
        if Gardening <= 2.50:
          Predict: Duck
        else (Gardening > 2.50):
          if Gardening <= 8.50:
            if Swimming <= 8.50:
              if Running <= 6.50:
                Predict: Duck
              else (Running > 6.50):
                Predict: Dog
            else (Swimming > 8.50):
              Predict: Dog
          else (Gardening > 8.50):
            Predict: Dog
    else (Running > 8.50):
      if Swimming <= 4.50:
        if Art <= 2.50:
          Predict: Dog
        else (Art > 2.50):
          Predict: Duck
      else (Swimming > 4.50):
        if Gardening <= 0.50:
          Predict: Duck
        else (Gardening > 0.50):
          if Art <= 3.50:
            Predict: Dog
          else (Art > 3.50):
            if Gardening <= 3.00:
              Predict: Duck
            else (Gardening > 3.00):
              Predict: Dog
else (Art > 4.50):
  if Swimming <= 4.50:
    if Running <= 9.50:
      if Detective Stories <= 5.50:
        if Art <= 8.00:
          Predict: Dog
        else (Art > 8.00):
          Predict: Cat
      else (Detective Stories > 5.50):
        Predict: Cat
    else (Running > 9.50):
      Predict: Duck
  else (Swimming > 4.50):
    if Running <= 4.50:
      if Art <= 9.50:
        Predict: Duck
      else (Art > 9.50):
        Predict: Cat
    else (Running > 4.50):
      if Gardening <= 4.50:
        Predict: Duck
      else (Gardening > 4.50):
        if Detective Stories <= 13.50:
          if Swimming <= 8.50:
            if Detective Stories <= 6.50:
              Predict: Duck
            else (Detective Stories > 6.50):
              if Running <= 8.00:
                Predict: Duck
              else (Running > 8.00):
                Predict: Dog
          else (Swimming > 8.50):
            if Detective Stories <= 6.50:
              if Swimming <= 12.00:
                if Art <= 5.50:
                  Predict: Dog
                else (Art > 5.50):
                  Predict: Duck
              else (Swimming > 12.00):
                Predict: Dog
            else (Detective Stories > 6.50):
              if Running <= 11.50:
                if Gardening <= 11.50:
                  Predict: Duck
                else (Gardening > 11.50):
                  if Art <= 10.50:
                    Predict: Duck
                  else (Art > 10.50):
                    Predict: Dog
              else (Running > 11.50):
                if Art <= 9.00:
                  Predict: Dog
                else (Art > 9.00):
                  Predict: Duck
        else (Detective Stories > 13.50):
          Predict: Dog

Tree 17 Rules:
if Art <= 4.50:
  if Running <= 4.50:
    if Swimming <= 5.00:
      Predict: Cat
    else (Swimming > 5.00):
      Predict: Dog
  else (Running > 4.50):
    if Swimming <= 13.50:
      if Gardening <= 4.50:
        if Art <= 1.50:
          if Swimming <= 6.00:
            Predict: Cat
          else (Swimming > 6.00):
            Predict: Dog
        else (Art > 1.50):
          Predict: Duck
      else (Gardening > 4.50):
        if Gardening <= 6.50:
          if Detective Stories <= 4.50:
            Predict: Duck
          else (Detective Stories > 4.50):
            Predict: Dog
        else (Gardening > 6.50):
          Predict: Dog
    else (Swimming > 13.50):
      if Art <= 2.00:
        Predict: Duck
      else (Art > 2.00):
        Predict: Dog
else (Art > 4.50):
  if Running <= 4.50:
    if Art <= 6.50:
      if Swimming <= 7.00:
        Predict: Cat
      else (Swimming > 7.00):
        Predict: Duck
    else (Art > 6.50):
      if Art <= 9.50:
        if Swimming <= 7.00:
          Predict: Cat
        else (Swimming > 7.00):
          Predict: Duck
      else (Art > 9.50):
        Predict: Cat
  else (Running > 4.50):
    if Running <= 10.50:
      if Art <= 9.50:
        if Swimming <= 4.50:
          Predict: Cat
        else (Swimming > 4.50):
          if Gardening <= 4.50:
            Predict: Duck
          else (Gardening > 4.50):
            if Art <= 7.50:
              if Gardening <= 7.00:
                Predict: Dog
              else (Gardening > 7.00):
                if Swimming <= 13.00:
                  if Swimming <= 11.00:
                    if Running <= 7.50:
                      Predict: Dog
                    else (Running > 7.50):
                      Predict: Duck
                  else (Swimming > 11.00):
                    Predict: Duck
                else (Swimming > 13.00):
                  Predict: Dog
            else (Art > 7.50):
              Predict: Duck
      else (Art > 9.50):
        if Running <= 9.50:
          if Gardening <= 2.50:
            if Swimming <= 5.00:
              Predict: Cat
            else (Swimming > 5.00):
              Predict: Duck
          else (Gardening > 2.50):
            if Swimming <= 6.50:
              Predict: Cat
            else (Swimming > 6.50):
              Predict: Duck
        else (Running > 9.50):
          Predict: Duck
    else (Running > 10.50):
      if Gardening <= 4.50:
        if Swimming <= 5.00:
          if Running <= 13.50:
            if Gardening <= 1.50:
              Predict: Duck
            else (Gardening > 1.50):
              Predict: Cat
          else (Running > 13.50):
            Predict: Duck
        else (Swimming > 5.00):
          if Gardening <= 3.50:
            Predict: Duck
          else (Gardening > 3.50):
            if Running <= 12.50:
              Predict: Duck
            else (Running > 12.50):
              Predict: Dog
      else (Gardening > 4.50):
        if Swimming <= 9.50:
          if Swimming <= 4.50:
            if Art <= 8.50:
              Predict: Duck
            else (Art > 8.50):
              Predict: Cat
          else (Swimming > 4.50):
            if Art <= 10.50:
              Predict: Dog
            else (Art > 10.50):
              if Detective Stories <= 11.50:
                if Running <= 12.00:
                  Predict: Duck
                else (Running > 12.00):
                  Predict: Dog
              else (Detective Stories > 11.50):
                Predict: Dog
        else (Swimming > 9.50):
          if Detective Stories <= 8.50:
            if Art <= 7.00:
              Predict: Duck
            else (Art > 7.00):
              Predict: Dog
          else (Detective Stories > 8.50):
            if Detective Stories <= 12.00:
              Predict: Duck
            else (Detective Stories > 12.00):
              Predict: Dog

Tree 18 Rules:
if Gardening <= 4.50:
  if Swimming <= 4.50:
    if Running <= 8.50:
      Predict: Cat
    else (Running > 8.50):
      if Art <= 2.00:
        Predict: Cat
      else (Art > 2.00):
        if Detective Stories <= 5.00:
          Predict: Cat
        else (Detective Stories > 5.00):
          if Running <= 12.50:
            if Art <= 10.00:
              Predict: Duck
            else (Art > 10.00):
              if Detective Stories <= 10.50:
                if Running <= 9.50:
                  Predict: Cat
                else (Running > 9.50):
                  Predict: Duck
              else (Detective Stories > 10.50):
                Predict: Cat
          else (Running > 12.50):
            Predict: Cat
  else (Swimming > 4.50):
    if Gardening <= 3.50:
      if Running <= 3.50:
        if Art <= 9.50:
          Predict: Duck
        else (Art > 9.50):
          Predict: Cat
      else (Running > 3.50):
        if Running <= 13.50:
          Predict: Duck
        else (Running > 13.50):
          if Detective Stories <= 11.00:
            Predict: Duck
          else (Detective Stories > 11.00):
            if Detective Stories <= 13.00:
              Predict: Dog
            else (Detective Stories > 13.00):
              Predict: Duck
    else (Gardening > 3.50):
      if Running <= 12.50:
        Predict: Duck
      else (Running > 12.50):
        Predict: Dog
else (Gardening > 4.50):
  if Swimming <= 4.50:
    if Detective Stories <= 6.50:
      if Gardening <= 6.50:
        if Running <= 8.00:
          Predict: Cat
        else (Running > 8.00):
          Predict: Duck
      else (Gardening > 6.50):
        if Art <= 4.50:
          Predict: Dog
        else (Art > 4.50):
          Predict: Cat
    else (Detective Stories > 6.50):
      Predict: Cat
  else (Swimming > 4.50):
    if Swimming <= 9.50:
      if Art <= 5.50:
        if Running <= 6.50:
          if Art <= 2.50:
            Predict: Dog
          else (Art > 2.50):
            if Gardening <= 11.00:
              Predict: Duck
            else (Gardening > 11.00):
              Predict: Dog
        else (Running > 6.50):
          Predict: Dog
      else (Art > 5.50):
        if Running <= 9.50:
          Predict: Duck
        else (Running > 9.50):
          Predict: Dog
    else (Swimming > 9.50):
      if Detective Stories <= 12.50:
        if Gardening <= 8.50:
          if Detective Stories <= 6.50:
            Predict: Dog
          else (Detective Stories > 6.50):
            if Art <= 4.50:
              if Running <= 5.00:
                Predict: Duck
              else (Running > 5.00):
                Predict: Dog
            else (Art > 4.50):
              if Art <= 9.50:
                Predict: Duck
              else (Art > 9.50):
                if Detective Stories <= 11.50:
                  if Gardening <= 6.50:
                    if Swimming <= 12.00:
                      Predict: Duck
                    else (Swimming > 12.00):
                      if Running <= 13.50:
                        Predict: Duck
                      else (Running > 13.50):
                        Predict: Cat
                  else (Gardening > 6.50):
                    Predict: Cat
                else (Detective Stories > 11.50):
                  Predict: Duck
        else (Gardening > 8.50):
          if Art <= 5.50:
            Predict: Dog
          else (Art > 5.50):
            if Art <= 7.50:
              if Running <= 10.00:
                Predict: Duck
              else (Running > 10.00):
                if Gardening <= 12.00:
                  Predict: Dog
                else (Gardening > 12.00):
                  Predict: Duck
            else (Art > 7.50):
              if Gardening <= 9.50:
                Predict: Dog
              else (Gardening > 9.50):
                if Swimming <= 13.50:
                  Predict: Dog
                else (Swimming > 13.50):
                  Predict: Cat
      else (Detective Stories > 12.50):
        if Art <= 13.50:
          if Swimming <= 10.50:
            if Art <= 6.00:
              Predict: Dog
            else (Art > 6.00):
              Predict: Duck
          else (Swimming > 10.50):
            Predict: Dog
        else (Art > 13.50):
          Predict: Duck

Tree 19 Rules:
if Gardening <= 4.50:
  if Swimming <= 4.50:
    if Detective Stories <= 10.50:
      if Running <= 9.50:
        Predict: Cat
      else (Running > 9.50):
        if Art <= 2.00:
          Predict: Cat
        else (Art > 2.00):
          Predict: Duck
    else (Detective Stories > 10.50):
      if Running <= 13.50:
        if Swimming <= 1.50:
          if Art <= 9.50:
            Predict: Duck
          else (Art > 9.50):
            Predict: Cat
        else (Swimming > 1.50):
          Predict: Cat
      else (Running > 13.50):
        Predict: Duck
  else (Swimming > 4.50):
    if Art <= 11.50:
      if Art <= 2.50:
        if Detective Stories <= 11.00:
          if Swimming <= 11.50:
            Predict: Dog
          else (Swimming > 11.50):
            Predict: Duck
        else (Detective Stories > 11.00):
          Predict: Duck
      else (Art > 2.50):
        Predict: Duck
    else (Art > 11.50):
      if Running <= 4.50:
        Predict: Cat
      else (Running > 4.50):
        Predict: Duck
else (Gardening > 4.50):
  if Art <= 9.50:
    if Running <= 3.50:
      if Swimming <= 5.00:
        if Art <= 4.00:
          Predict: Dog
        else (Art > 4.00):
          Predict: Cat
      else (Swimming > 5.00):
        Predict: Dog
    else (Running > 3.50):
      if Detective Stories <= 4.50:
        if Art <= 1.00:
          Predict: Duck
        else (Art > 1.00):
          if Detective Stories <= 2.50:
            Predict: Dog
          else (Detective Stories > 2.50):
            if Art <= 7.00:
              Predict: Duck
            else (Art > 7.00):
              Predict: Dog
      else (Detective Stories > 4.50):
        if Art <= 4.50:
          if Swimming <= 13.50:
            if Running <= 6.50:
              if Swimming <= 7.50:
                Predict: Dog
              else (Swimming > 7.50):
                if Swimming <= 8.50:
                  Predict: Duck
                else (Swimming > 8.50):
                  Predict: Dog
            else (Running > 6.50):
              Predict: Dog
          else (Swimming > 13.50):
            if Gardening <= 6.50:
              if Running <= 7.00:
                Predict: Duck
              else (Running > 7.00):
                Predict: Dog
            else (Gardening > 6.50):
              Predict: Dog
        else (Art > 4.50):
          if Gardening <= 9.50:
            if Swimming <= 11.50:
              if Running <= 5.00:
                Predict: Duck
              else (Running > 5.00):
                if Running <= 9.00:
                  if Art <= 6.00:
                    Predict: Dog
                  else (Art > 6.00):
                    Predict: Duck
                else (Running > 9.00):
                  if Detective Stories <= 8.00:
                    Predict: Duck
                  else (Detective Stories > 8.00):
                    Predict: Dog
            else (Swimming > 11.50):
              Predict: Dog
          else (Gardening > 9.50):
            if Running <= 10.50:
              if Gardening <= 13.50:
                if Art <= 5.50:
                  Predict: Dog
                else (Art > 5.50):
                  if Swimming <= 8.50:
                    if Gardening <= 11.50:
                      Predict: Duck
                    else (Gardening > 11.50):
                      Predict: Dog
                  else (Swimming > 8.50):
                    Predict: Duck
              else (Gardening > 13.50):
                Predict: Duck
            else (Running > 10.50):
              Predict: Dog
  else (Art > 9.50):
    if Running <= 9.00:
      if Running <= 7.50:
        Predict: Cat
      else (Running > 7.50):
        if Detective Stories <= 11.50:
          Predict: Cat
        else (Detective Stories > 11.50):
          Predict: Duck
    else (Running > 9.00):
      if Detective Stories <= 12.00:
        if Gardening <= 8.00:
          if Detective Stories <= 10.50:
            Predict: Dog
          else (Detective Stories > 10.50):
            if Running <= 13.50:
              Predict: Duck
            else (Running > 13.50):
              Predict: Cat
        else (Gardening > 8.00):
          Predict: Duck
      else (Detective Stories > 12.00):
        if Art <= 11.50:
          Predict: Cat
        else (Art > 11.50):
          Predict: Dog

Aggregated Rules (Across Forest):

Finished rule extraction. Output saved to 'extracted_rules.txt'.
